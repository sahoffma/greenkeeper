# GA-015

| Feld | Wert |
|------|------|
| **ID** | GA-015 |
| **Titel** | Produktbasierter Düngerbestand — technische Umstellung |
| **Status** | 📋 Geplant |
| **Priorität** | Hoch |
| **Erstellt** | 2026-08-03 |
| **Zuletzt geändert** | 2026-08-03 |
| **Verantwortlich** | — |
| **Verwandte Dokumente** | [DL-033](../decisions/dl-033.md); [DL-034](../decisions/dl-034.md); [DL-020](../decisions/dl-020.md); [DL-025](../decisions/dl-025.md); [DL-030](../decisions/dl-030.md); [DL-031](../decisions/dl-031.md); [GA-012](./ga-012.md); [GM-008](../model/gm-008.md); [home-experience](../playbook/home-experience.md); [conversation-model](../playbook/conversation-model.md) |
| **Kurzbeschreibung** | Technischer Migrations- und Umsetzungsplan: von gebindebasierten `fertilizer_containers` zu produktbasiertem Bestand gemäß DL-033 — ohne FIFO, mit find-or-create Zugängen, Startseiten-Düngungsflow und Ausrüstung als Read-only-Bestand. |

---

## 1. Status

📋 **Geplant** — Architektur- und Migrationsplanung. **Phase 1 abgeschlossen** (`0d5d529`, `fertilizerProductStockCore.ts`). **Phase 2** in diesem Dokument als Persistenzvertrag spezifiziert; noch keine SQL-Umsetzung.

Produktvertrag: [DL-033](../decisions/dl-033.md) (veröffentlicht, `e14522d`); Bewegungsgründe: [DL-034](../decisions/dl-034.md); Domain-Core Phase 1: `0d5d529`.

---

## 2. Ausgangslage

Greenkeeper implementiert Düngerbestand heute als **Gebinde-Modell**:

| Ebene | Ist-Stand (Repository `bc49546` / `e14522d`) |
|-------|-----------------------------------------------|
| **Tabelle Inventory Item** | `public.fertilizer_containers` — Kommentar/Migration: „physisches Gebinde“ |
| **Tabelle Movement** | `public.fertilizer_stock_movements` — append-only, `movement_at` kanonisch |
| **Creation** | `create_fertilizer_inventory_core_from_confirmed_packages` — **ein Item pro Packung** in Schleife |
| **Zugang** | Initiale positive Movement pro neuem Item (`initial_stock`, `purchase`, `gift_received`) |
| **Bestandsliste** | Client: `fetchFertilizerStockList` — **ein Listeneintrag pro Container**, Saldo je Item |
| **Düngung** | Route `/ausruestung/duenger/:inventoryItemId/anwenden`, RPC `apply_fertilizer_inventory_item_to_areas` |
| **Constraint Düngung** | Unique Index `fertilizer_stock_movements_application_batch_idx` — **genau ein Movement pro Batch** (kompatibel mit DL-033) |
| **Unique Produktbestand** | **Kein** Unique Constraint auf `(user_id, saved_product_profile_id, base_unit)` — nur Indizes |

[DL-033](../decisions/dl-033.md) definiert das **Zielbild Version 1**: ein Inventory Item = **Produktbestand**, kein Gebinde, keine FIFO, Startseite als Tätigkeitseinstieg.

---

## 3. Zielvertrag (DL-033, technisch)

| Regel | Ziel |
|-------|------|
| Inventory Item | Ein **kompatibler mengenmäßiger Produktbestand** pro Schlüssel |
| Gebinde | **Nicht** modelliert; Packungsgröße nur Zugangsmengen-Eingabe |
| Zugang | Positive Movement auf **bestehendem oder neu angelegtem** Produktbestand |
| Düngung | **Genau eine** negative Movement; **ein** Batch; Mehrflächen unverändert |
| Bestand | Summe Movements mit `movement_at IS NOT NULL`; **kein** `currentQuantity` |
| FIFO / Gebindestatus | **Nicht** in Version 1 |
| Startseite | Primärer Einstieg „Düngung erfassen“ |
| Ausrüstung | Bestand/Nachschlage; **kein** primärer „Anwenden“-Button |
| Parallele Logik | **Verboten** — ein Flow, ein RPC |

---

## 4. Ist-Architektur (Detail)

### 4.1 Datenmodell

**`fertilizer_containers` (Inventory Item)**

- PK `id`; `user_id`; `saved_product_profile_id` (FK `product_profiles`, `profile_status = saved`)
- `access_kind` (`authenticated_user` | `session`); `base_unit` (`kg` | `ml`)
- `package_size_value` / `package_size_unit` — nominale Packung
- Legacy: `product_id`, `recognition_candidate_id`, `label`
- Kein `product_form` auf Container — Form über Join `product_profiles.product_form`
- Keine Spalte `product_version_id`
- Saldo: abgeleitet aus Movements

**`fertilizer_stock_movements`**

- `container_id` → Item; `quantity_delta`; `unit`; `movement_type`; `movement_origin`
- `movement_at` — kanonisch für Saldo
- `application_batch_id` — FK zu `fertilizer_application_batches` (unique wenn gesetzt)
- Immutability: Trigger `prevent_fertilizer_stock_movement_update/delete`

**Application / Journal**

- `fertilizer_application_batches` — 1 pro Düngung; `inventory_item_id`, Idempotenz
- `fertilizer_application_areas` — je Fläche
- `activities` + `fertilization_details` — je Fläche; Snapshots
- RPC: `apply_fertilizer_inventory_item_to_areas(p_inventory_item_id, p_saved_product_profile_id, …)`

**Saved Product Profile**

- `product_profiles.profile_status = 'saved'` — **immutable** (Trigger `prevent_saved_product_profile_mutation`)
- Repräsentiert konkrete Produktversion ([DL-018](../decisions/dl-018.md))
- `product_form` (`granular` | `liquid`) auf Profil

### 4.2 Erstellung und Zugang

- **RPC:** `create_fertilizer_inventory_core_from_confirmed_packages(p_saved_product_profile_id, packages[], reason, …)`
- Schleife: **pro Packung** INSERT Container + INSERT initiale positive Movement
- Receipt/Idempotenz: `fertilizer_inventory_creation_receipts`
- Client: `fertilizerInventoryCreation*.ts`, Capture-Integration

### 4.3 Bestandsanzeige

- `fetchFertilizerStockList()` — lädt alle Container, berechnet Saldo je Item
- `FertilizerCategoryPage` — Listeneintrag je Item, Text „Gebinde …“, Link **„Anwenden“**
- Keine serverseitige Aggregation nach Produktbestandsschlüssel

### 4.4 Düngung

- `FertilizerApplicationPage` — `useParams().inventoryItemId` Pflicht
- Flow-Core: `fertilizerApplicationFlowCore.ts`, Multi-Area: `fertilizerMultiAreaApplication*.ts`
- Submit: `applyFertilizerInventoryItemToAreas` → RPC mit `p_inventory_item_id`
- Eligibility: Core-gebundenes Item (`saved_product_profile_id`, `access_kind`, `base_unit`)

### 4.5 Startseite

- `HomeScreen`, `HomeVoiceSection` — Sprache/Text dokumentiert ([home-experience](../playbook/home-experience.md))
- **Kein** dedizierter CTA „Düngung erfassen“ mit Produktauswahl
- `homeVoiceFlow.ts` — Intent-Struktur vorhanden, Düngung noch nicht an Mehrflächen-RPC angebunden

---

## 5. Abweichungen von DL-033

| # | Ist | Soll (DL-033) |
|---|-----|---------------|
| 1 | Ein Container = ein Gebinde | Ein Container = **Produktbestand** |
| 2 | Mehrere Items pro kompatiblem Produkt möglich | **Genau ein** anwendbarer Produktbestand pro Schlüssel |
| 3 | Kauf erzeugt N Items bei N Packungen | Kauf erzeugt **eine** positive Movement auf bestehendem Item |
| 4 | Bestandsliste itemweise | **Produktbestandsliste** (ein Eintrag pro Schlüssel) |
| 5 | Düngung startet in Ausrüstung mit Item-ID | Startseite wählt **Produktbestand** |
| 6 | UI „Gebinde“, „Anwenden“ pro Item | Kein Gebindetext; **kein** Apply-CTA in Ausrüstung |
| 7 | GA-012 beschreibt FIFO/Mehrgebinde | DL-033 **verwirft** FIFO für v1 — GA-012 Mehrgebinde-Regeln sind **nicht** produktive Richtung v1 |

**Bereits kompatibel:** Ein Movement pro Batch; Mehrflächen-RPC; Ledger; Idempotenz; Atomarität; Overdraft-Schutz im RPC.

---

## 6. Produktbestandsschlüssel

### 6.1 Fachlicher Schlüssel (DL-033)

```
(user_id, saved_product_profile_id, form, base_unit)
```

### 6.2 Persistierte Felder (Ist)

| Komponente | Persistenz | Quelle |
|------------|------------|--------|
| `user_id` | ✅ | `fertilizer_containers.user_id` |
| `saved_product_profile_id` | ✅ | `fertilizer_containers.saved_product_profile_id` |
| `form` | ⚠️ indirekt | `product_profiles.product_form` (immutable wenn `saved`) |
| `base_unit` | ✅ | `fertilizer_containers.base_unit` |

**Keine** Spalte `product_version_id`. **Keine** neue Spalte in Phase 1 erfinden, sofern Gate A grün.

### 6.3 Empfehlung Unique Constraint (Gate A)

```sql
-- Konzeptionell (Umsetzung Phase 2):
UNIQUE (user_id, saved_product_profile_id, base_unit)
WHERE access_kind = 'authenticated_user'
  AND saved_product_profile_id IS NOT NULL
  AND archived_at IS NULL
  AND stock_kind = 'product_stock'  -- optional, siehe Migration
```

**Begründung:** `saved_product_profile_id` bindet **immutable** an genau eine `product_form` ([DL-018](../decisions/dl-018.md), Trigger auf `saved`). `base_unit` ist redundant zu Form nur insofern, als DB bereits `granular→kg`, `liquid→ml` erzwingt — **beide** im Schlüssel dokumentieren, Unique technisch auf `(user_id, saved_product_profile_id, base_unit)`.

**Session-Scope** (`access_kind = session`): separater Schlüssel mit `session_access_hash` — nicht Teil des authenticated Produktbestands.

---

## 7. Versionsanker

### Ist-Stand

- **`saved_product_profile_id`** verweist auf `product_profiles` mit `profile_status = 'saved'`
- Saved Profiles sind **unveränderlich** (`prevent_saved_product_profile_mutation`)
- Neue Herstellerdeklaration → **neues** Saved Profile ([DL-018](../decisions/dl-018.md)) — **keine** Zusammenführung

### Beantwortung Gate A

| Frage | Antwort |
|-------|---------|
| Reicht `saved_product_profile_id` als Produkt- **und** Versionsanker? | **Ja**, für v1 — Saved Profile = konkrete unveränderliche Produktversion |
| Kann Saved Profile später verändert werden? | **Nein** (DB-Trigger) |
| Braucht es `product_version_id`? | **Nein** in v1 — offene Ergänzung nur falls später explizite Versionstabelle eingeführt wird |

### Offene Frage (Gate A, dokumentiert)

Ob **zwei** Saved Profiles fachlich „dasselbe Produkt“ sein können, ohne dass der Nutzer es erkennt — dann entstehen **zwei** Produktbestände (**korrekt** nach DL-018/DL-033). Keine automatische Merge-Heuristik.

---

## 8. Zielarchitektur

### 8.1 Produktbestand

```
Produktbestand (1 × fertilizer_containers-Zeile pro Schlüssel)
  └── Movements (append-only, movement_at gesetzt)
        ├── + initial_stock | purchase | gift_received
        └── − application | gift_given | disposed | inventory_correction
              (application nur mit Application Batch / Journal)
```

Bewegungssemantik: [DL-034](../decisions/dl-034.md). Ist-Enum nutzt teils `fertilization`, `gifted_away`, `disposal` — Ausrichtung in Phase 2/6.

- Kein `currentQuantity`
- Keine Gebindeobjekte, kein FIFO
- `package_size_*` optional, nur Capture-Hilfe

### 8.2 Zugang

```
find_or_create_product_stock(user, saved_product_profile_id, base_unit)
  → atomar: SELECT … FOR UPDATE | INSERT
  → append_positive_movement(amount, reason)
```

- Idempotenz über Receipt + Movement-Key (bestehendes Muster)
- Race: Unique Constraint + Retry auf Unique Violation → Re-SELECT

### 8.3 Anwendung

```
Startseite → Produktbestand wählen → Flächen/Modus/Menge → Bestätigung
  → apply_fertilizer_inventory_item_to_areas(p_inventory_item_id = kanonische Item-ID, …)
  → 1 Batch, N Activities, 1 negative Movement
```

- **RPC-Semantik unverändert** — nur Einstieg und Item-Auflösung ändern sich
- `p_inventory_item_id` bleibt — Wert = **Produktbestands-Item**

### 8.4 Ausrüstung

- Read-only Liste: Produktname, Saldo, Base Unit
- Kein „Anwenden“; optional Produkt-/Profil-Detail
- Capture/Erfassung bleibt separater Zugang (Phase 6)

### 8.5 Journal

- Unverändert: je Area Activity; Snapshots; keine sichtbare Bestandskomplexität

---

## 9. Zugangsbuchungen (Plan)

### Betroffene RPCs

| RPC | Heute | Ziel |
|-----|-------|------|
| `create_fertilizer_inventory_core_from_confirmed_packages` | N Items | **Ersetzen/ erweitern** durch `record_fertilizer_product_stock_intake` (Arbeitstitel) |
| Legacy `save_fertilizer_capture` | Legacy-Container | Übergang unverändert; Core-Pfad migrieren |

### Neuer Vertrag (Phase 2, konzeptionell)

**Input:** `saved_product_profile_id`, `quantity`, `base_unit`, `reason` (`initial_stock`|`purchase`|`gift_received`), `idempotency_key`, optional `package_size_hint`

**Ablauf:**

1. Profil laden + validieren (`saved`, Form↔Unit)
2. Produktbestand find-or-create (Unique + Lock)
3. Positive Movement append
4. Receipt speichern

**Packungsgröße:** nur Request-Metadatum für UI; **nicht** auf Item persistieren als Identität (optional weiter als nullable Metadatum der **Movement**-Notiz).

---

## 10. Düngungsanwendungen (Plan)

| Frage | Empfehlung |
|-------|------------|
| RPC ändern? | **Nein** — `apply_fertilizer_inventory_item_to_areas` bleibt |
| `p_inventory_item_id`? | **Ja** — intern korrekt; Client löst aus Produktauswahl auf |
| Route ersetzen? | **Ja:** `/aktivitaet/duengung` oder `/home/duengung` statt `/ausruestung/duenger/:id/anwenden` |
| Übergangsroute? | **Ja:** alte Route redirect oder Fehlerseite mit Hinweis bis Phase 7 |
| Zwei Flows? | **Nein** — `FertilizerApplicationPage` parameterlos; Produktauswahl Schritt 1 |
| Flow-Core | Entkoppeln von `inventoryItemId` in URL; Draft trägt `productStockId` nach Auswahl |
| Idempotenz | Unverändert batch-scoped |
| Bestand zwischen Auswahl und Submit | RPC prüft Saldo **erneut** in Transaktion (bestehend) |

---

## 11. Startseitenintegration

- `homeVoiceFlow` / Startseiten-Unterhaltung = **Open Conversation** (Orchestrator); Ausrüstungs-Capture = **Scoped Capture** — ein Flow-Core, **keine** zweite Conversation- oder Bestandslogik ([CM-015](../playbook/conversation-model.md#cm-015--offene-unterhaltung-und-geführte-erfassung))

### Empfohlene Reihenfolge (v1)

1. **Deterministischer CTA** „Düngung erfassen“ auf Startseite (Button/Kachel)
2. Produktauswahl aus verfügbarem Bestand
3. Wiederverwendung `FertilizerApplicationPage`-Logik (Flächen, Modi, Bestätigung)
4. **Später:** Sprache/Text → **dieselbe** State-Machine / RPC ([home-experience](../playbook/home-experience.md) HE-002)

**Begründung:** Manifest/Home-Experience priorisiert Sprache langfristig, aber technischer Umbau ohne NL reduziert Risiko ([DL-033](../decisions/dl-033.md) offene Frage 9). Keine parallele NL-Sonderlogik in Phasen 1–5.

### Flow-Schritte

1. Startseite → CTA
2. Produktbestand wählen (nur `balance > 0`, application-eligible)
3. Areas wählen (bestehend)
4. Modus + Menge (bestehend)
5. Zusammenfassung (bestehend)
6. Atomar speichern
7. Erfolg → Journal/Home

---

## 12. Ausrüstungsbereich

| Komponente | Aktion |
|------------|--------|
| `FertilizerCategoryPage` | Apply-Link entfernen; Gebinde-Metadaten entfernen; **Produktbestandszeilen** |
| `fetchFertilizerStockList` | Serverseitig aggregieren oder Client gruppiert nach Schlüssel |
| `FertilizerStockListItem` | Typ: `productStockId` statt Container-Liste; kein `packageSize` in Primärdarstellung |
| `fertilizerApplicationPath` | Entfernen aus Ausrüstung (Phase 7) |

**Übergang:** Wenn Migration noch Duplikate hat, zeigt UI **einen** Eintrag pro Schlüssel mit **aggregiertem** Saldo (read-side), bis Phase 3 kanonisiert.

---

## 13. Datenmigration (Strategie)

### Prinzipien

- **Additiv zuerst** — keine destruktive Löschung von Movements
- **Ledger unveränderlich** — kein UPDATE auf `fertilizer_stock_movements`
- **Historische FKs** — `fertilization_details.inventory_item_id`, `application_batches.inventory_item_id` **nicht** umschreiben
- **Rollback** — Migrationsschritte idempotent + reversibel wo möglich (Archivierung statt DELETE)

### Duplikat-Gruppen (mehrere kompatible Container)

**Erkennung:** Gleicher `(user_id, saved_product_profile_id, base_unit)`, Core-gebunden, nicht archiviert.

**Kanonisches Item wählen** (deterministisch):

1. Höchster Saldo > 0
2. Sonst ältestes `created_at`
3. Tie-Breaker: `lower(id::text)`

**Superseded Items:**

- Spalte `superseded_by_container_id uuid` (additiv) **oder** `archived_at` + `archive_reason = 'superseded_by_product_stock_migration'`
- Movements **bleiben** am Original-Container

**Saldo-Konsolidierung (empfohlen, Gate B):**

- Pro superseded Item mit Saldo ≠ 0: **Paar** von Movements in **einer** Migrationstransaktion:
  - `inventory_correction` −Saldo auf superseded Item
  - `purchase` oder `migration` +Saldo auf kanonischem Item
- **Summe global unverändert**; Ledger append-only
- Alternativ (konservativer Übergang): Read-Aggregation ohne Buchung bis Saldo natürlich verbraucht — **langsamer**, weniger Risiko

**Application-eligible nach Migration:** nur kanonische Items; superseded archiviert.

### Legacy ohne Core-Bindung

- Nicht application-eligible (bestehend)
- Nicht in Produktbestandsliste
- Separater Migrations-/Upgrade-Pfad ([DL-030](../decisions/dl-030.md))

### Verbrauchte Alt-Items (Saldo 0)

- Archivieren; kein Merge nötig

### Inkompatible Items

- Nicht zusammenführen; bleiben einzeln bis manuelle Klärung

---

## 14. Kompatibilität

| Phase | Alt-Client | Neuer Client |
|-------|------------|--------------|
| Nach Phase 2 RPC | Creation erzeugt weiter Items pro Packung | Neuer Intake-RPC |
| Nach Phase 4 UI | Item-Liste + Apply | Produktliste ohne Apply |
| Nach Phase 5 Flow | Deep-Link Item-Route | Startseiten-Flow |

**Kompatibilitätsfenster:** Alte Route bleibt bis Phase 7; redirect auf Startseiten-Flow wenn Item kanonisch, sonst Hinweis.

**RPC-Entfernung:** erst Gate E — nach Migration + Clean-HEAD + Dev-DB.

---

## 15. Idempotenz

- **Zugang:** Receipt-Tabelle analog `fertilizer_inventory_creation_receipts`; Fingerprint inkl. `saved_product_profile_id`, `quantity`, `reason`, `idempotency_key`
- **Düngung:** unverändert `fertilizer_application_batches` + `request_fingerprint`
- **Migration:** Migrations-Receipt oder `migration_batch_id` pro konsolidiertem Item-Paar — Wiederholung safe

---

## 16. Konkurrenz und Locks

| Vorgang | Mechanismus |
|---------|-------------|
| Parallele Zugänge | `SELECT … FOR UPDATE` auf kanonischem Item **oder** Advisory Lock auf `(user_id, profile_id, base_unit)`; Unique als Backstop |
| Parallele Düngungen | Bestehend: Item-Lock im RPC + Saldo-Reprüfung |
| Migration | Batch-weise; keine User-Writes während Item-Konsolidierung pro Gruppe (Maintenance-Flag optional) |

---

## 17. Teststrategie

### Domain (Phase 1)

- Gleicher Schlüssel; inkompatible Form/Unit/Profil
- Positiver/negativer Saldo; Präzision; unzureichender Bestand
- Find-or-create Semantik

### Persistenz (Phase 2)

- Erster Zugang erstellt Item; zweiter nutzt dasselbe
- Parallele Zugänge → kein Duplikat
- Idempotenz Replay/Konflikt
- Düngung: genau 1 Movement; Rollback; RLS; Immutability

### Migration (Phase 3)

- 1 / N kompatible Alt-Items; historische Anwendungen; verbraucht; Legacy; inkompatibel
- Movement-Summen invariant; wiederholbare Migration

### UI (Phase 4–5)

- Ausrüstung ohne Apply; Produktbestand sichtbar
- Startseiten-CTA; Ein-/Mehrflächen; Pflegegruppe; Fehler erhält Eingaben
- Kein Gebindetext

### Clean-HEAD

- Jede Phase committbar unabhängig vom lokalen WIP
- Dev-DB-Tests nur mit Opt-in ([GA-010](./ga-010.md))

---

## 18. Phasenplan

### Phase 1 — Produktbestands-Domain und Datenvertrag ✅

| | |
|---|---|
| **Status** | ✅ Abgeschlossen (`0d5d529`) |
| **Ziel** | Domain-Core für Schlüssel und positive Zugänge |
| **Vertrag** | DL-033 Schlüssel; DL-034 positive Reasons |
| **Dateien** | `src/lib/fertilizerProductStockCore.ts`, `src/lib/fertilizerProductStockCore.test.ts` |
| **Tabellen/RPCs** | Keine |
| **Tests** | 40 Unit-Tests (Identität, Intake, Form/Unit, Abgrenzung) |
| **Abnahme** | Core grün; Gate A Dokumentation final |
| **Commit** | `feat: define product-based fertilizer stock intake core` |

### Phase 2 — Produktbestands-Persistenz (Spezifikation)

| | |
|---|---|
| **Status** | 📋 Spezifikation in §25 — Implementierung folgt |
| **Ziel** | Additive Migration + atomarer Intake-RPC |
| **Vertrag** | Partieller Unique Index; get-or-create + positive Movement |
| **Migration** | `stock_kind`; partieller Unique; Receipt-Tabelle; RPC `record_fertilizer_product_stock_intake` |
| **Dateien** | Migration SQL; `fertilizerProductStockIntake*.ts`; Dev-DB Harness |
| **Tests** | DB: Duplikat-Verhinderung, Idempotenz, Concurrency, RLS |
| **Abnahme** | Gate C grün; Dev-DB grün |
| **Risiken** | Race — Unique + Lock; bestehende Duplikate |
| **Abhängigkeit** | Phase 1 ✅ |
| **Dokumentations-Commit** | `docs: define fertilizer stock persistence phase` |
| **Implementierungs-Commit** | `feat: add product stock intake persistence` |

### Phase 3 — Bestehende Daten

| | |
|---|---|
| **Ziel** | Konsolidierung Duplikate; kanonische Items |
| **Vertrag** | Ledger-Summen invariant; historische FKs unverändert |
| **Migration** | Script/RPC `migrate_fertilizer_containers_to_product_stock` |
| **Tests** | Fixture-Gruppen; Summen; Batch-Referenzen lesbar |
| **Abnahme** | Gate B grün; Reproduzierbarkeit |
| **Rollback** | Archivierung rückgängig machbar wenn keine Korrektur-Movements |
| **Commit** | `feat: migrate container inventory to product stock` |

### Phase 4 — Read-Runtime und Ausrüstung

| | |
|---|---|
| **Ziel** | Produktbestandsliste; Apply entfernen |
| **Dateien** | `fertilizerInventory.ts`, `fertilizerInventoryStockListCore.ts`, `FertilizerCategoryPage.tsx` |
| **Tests** | Unit Projection; UI-Snapshot/Component |
| **Abnahme** | Kein Apply-Button; kein Gebindetext in Primär-UI |
| **Commit** | `feat: show product stock in equipment` |

### Phase 5 — Startseiten-Düngungsflow

| | |
|---|---|
| **Ziel** | CTA + Produktauswahl + bestehender Mehrflächen-Flow |
| **Route** | Neu: z. B. `/home/duengung`; Alt: Redirect |
| **Dateien** | `HomeScreen.tsx`, `FertilizerApplicationPage.tsx`, `fertilizerApplicationFlowCore.ts`, `App.tsx` |
| **RPC** | Unverändert `apply_fertilizer_inventory_item_to_areas` |
| **Abnahme** | Gate D; E2E optional später |
| **Commit** | `feat: start fertilizer application from home` |

### Phase 6 — Kauf, Abgänge und weitere Bestandsbewegungen

| | |
|---|---|
| **Ziel** | Capture/Creation und Abgangs-RPCs nutzen Intake-Vertrag ([DL-034](../decisions/dl-034.md)) |
| **Dateien** | `fertilizerInventoryCreation*.ts`, Capture UI, Abgangs-Intake |
| **Abnahme** | Zweiter Kauf → +Movement; Verschenken/Entsorgung → −Movement mit Bestandsprüfung; kein neues Item pro Ereignis |
| **Commit** | `feat: route fertilizer intake to product stock` |

Optional **Onboarding-Initialerfassung** (`initial_stock` nach Flächen): **sprachgeführter** Dialog ([Onboarding](../playbook/onboarding.md#sprachgeführte-erfassung-vorhandener-düngerbestände), [CM-014](../playbook/conversation-model.md#cm-014--sprachgeführter-initialbestand-im-onboarding)), überspringbar — kann parallel zu Phase 6 oder als Folgeschritt umgesetzt werden.

### Phase 7 — Bereinigung

| | |
|---|---|
| **Ziel** | Alte Route, Typen, Creation-Schleife, Docs |
| **Abnahme** | Gate E; Clean-HEAD |
| **Commit** | `chore: remove container-based fertilizer apply path` |

---

## 19. Risiken

| Risiko | Schwere | Wahrscheinlichkeit | Gegenmaßnahme | Phase |
|--------|---------|-------------------|---------------|-------|
| Versionsanker unklar | Hoch | Niedrig | Saved Profile immutable — Gate A | 1 |
| Doppelte Produktbestände | Hoch | Mittel | Unique + find-or-create | 2 |
| Movement-Umhängen nötig | Hoch | Mittel | Korrektur-Paare statt UPDATE | 3 |
| Batch-FK bricht | Hoch | Niedrig | Historische IDs unverändert | 3 |
| Alt/Neu-Client | Mittel | Mittel | Kompatibilitätsfenster | 4–7 |
| Item-Route Bookmarks | Niedrig | Hoch | Redirect | 5 |
| Mehrere Alt-Items | Mittel | Hoch | Migrationsvertrag | 3 |
| Race Zugang | Hoch | Mittel | Lock + Unique | 2 |
| Race Düngung | Mittel | Niedrig | Bestehend RPC | 5 |
| Legacy-Bestände | Mittel | Mittel | Eligibility-Filter | 3–4 |
| Falsche Merge | Kritisch | Niedrig | Kein Merge inkompatibel | 3 |
| Big-Bang | Hoch | Mittel | 7 Phasen | alle |
| Zweiter Flow | Hoch | Mittel | Ein Page, ein RPC | 5 |
| Sprache zu früh | Mittel | Mittel | CTA zuerst | 5 vs später |

**Blockierend vor Start Phase 2:** Gate A + Gate C Entwurf. **Blockierend Phase 3 Prod:** Gate B.

---

## 20. Offene Entscheidungen

| ID | Frage | Gate |
|----|-------|------|
| OD-1 | Korrektur-Paare vs. Read-Aggregation für Duplikate | B |
| OD-2 | Spalte `superseded_by_container_id` vs. nur `archived_at` | B |
| OD-3 | Exakte Route Startseiten-Flow | D |
| OD-4 | Denormalisiertes `product_form` auf Container für Unique | A |
| OD-5 | Wann `create_fertilizer_inventory_core_from_confirmed_packages` deprecaten | E |
| OD-6 | GA-012 Mehrgebinde-Abschnitt dokumentarisch anpassen | 7 |

---

## 21. Abnahmekriterien (Gesamt)

- [ ] Ein Produktbestand pro Schlüssel (Unique + Verhalten)
- [ ] Käufe erhöhen bestehenden Bestand
- [ ] Düngung: 1 Batch, 1 Movement, N Activities
- [ ] Startseite startet Flow; Ausrüstung ohne Apply
- [ ] Kein FIFO-Codepfad aktiv
- [ ] Ledger-Summen nach Migration invariant
- [ ] Historische Journal-Einträge lesbar
- [ ] Dev-DB + Unit grün; Clean-HEAD

---

## 22. Empfohlene Umsetzungsreihenfolge

```
Gate A → Phase 1 → Phase 2 → Gate C → Phase 3 → Gate B
  → Phase 4 → Phase 5 → Gate D → Phase 6 → Phase 7 → Gate E
```

Sprache/Text (**homeVoiceFlow**) **nach** Gate D — eigener Sprint, gleicher Flow-Core.

---

## 23. Betroffene Dateien und Komponenten

### Domain / Runtime

- `src/lib/fertilizerProductStockCore.ts` (neu)
- `src/lib/fertilizerInventoryStockListCore.ts`
- `src/lib/fertilizerInventory.ts`
- `src/lib/fertilizerInventoryCreationCore.ts`
- `src/lib/fertilizerInventoryCreationRpcCore.ts`
- `src/lib/fertilizerApplicationFlowCore.ts`
- `src/lib/fertilizerMultiAreaApplication.ts`
- `src/types/fertilizerInventory.ts`

### UI

- `src/pages/FertilizerCategoryPage.tsx`
- `src/pages/FertilizerApplicationPage.tsx`
- `src/pages/HomeScreen.tsx`
- `src/App.tsx`
- `src/lib/fertilizerRoutes.ts`

### SQL / RPC (später)

- `supabase/migrations/20250806_fertilizer_inventory_creation_core.sql` (ersetzt)
- `supabase/migrations/20250809_fertilizer_multi_area_application_atomic.sql` (Referenz, minimal)
- Neue Migrationen Phase 2–3

### Tests

- `src/lib/fertilizer*.test.ts`
- `src/lib/fertilizerInventoryCreationDatabaseTestHarness.ts`
- `src/lib/fertilizerApplicationDatabaseTestHarness.ts`

---

## 24. Nicht Teil dieses Umbaus

- FIFO, LIFO, Gebindestatus
- Sprach-/NL-Parsing für Düngung (eigener Block)
- Chargen, MHD, Lagerort
- `currentQuantity`-Spalte
- Product-Version-Tabelle (optional später)
- Equipment außer Düngerbestand
- E2E-Vollausbau (optional nach Phase 5)

---

## Entscheidungsgates

### Gate A — Produktbestandsschlüssel

**Freigabe Phase 2 wenn:** Unique-Felder festgelegt; Saved Profile als Versionsanker bestätigt; Form↔Unit-Regel dokumentiert.

### Gate B — Altbestände

**Freigabe produktive Migration wenn:** Duplikat-Algorithmus; Movement-Strategie (Korrektur-Paar vs. Aggregat); Batch-FK-Policy; Rollback.

### Gate C — Zugangsrace und Intake-Persistenz

**Freigabe Intake-RPC-Implementierung wenn:**

- Partielle Unique-/Index-Strategie festgelegt (§25.5)
- Get-or-create atomar spezifiziert (§25.6–25.7)
- Idempotenzvertrag Receipt + Movement dokumentiert (§25.9)
- Transaktionsgrenze und RPC-Empfehlung dokumentiert (§25.10)
- Schutz historischer Daten explizit (§25.11)
- Teststrategie für DB-Harness dokumentiert (§25.15)

**Freigabe durch dieses Dokument (Phase-2-Spezifikation):** Gate C **Spezifikation** — nicht Implementierung.

### Gate D — Startseitenflow

**Freigabe Entfernung Apply wenn:** CTA + Produktwahl + voller Flow + Fehler/Erfolg.

### Gate E — Legacy-Entfernung

**Freigabe Löschung alter Pfade wenn:** Migration abgeschlossen; keine produktive Nutzung; Tests grün.

---

## 25. Phase 2 — Persistenzvertrag (Spezifikation)

Dieser Abschnitt ist der **belastbare Persistenzvertrag** vor jeder SQL- oder RPC-Änderung. Er baut auf dem veröffentlichten Domain-Core ([`fertilizerProductStockCore.ts`](../../src/lib/fertilizerProductStockCore.ts), `0d5d529`) auf.

### 25.1 Ziel

Phase 2 implementiert **persistente positive Zugänge** auf kanonischem Produktbestand:

- höchstens **ein aktiver kanonischer Bestand** pro `(user_id, saved_product_profile_id, base_unit)`
- **find-or-create** atomar in der Datenbank
- **eine unveränderliche positive Movement** pro bestätigtem Zugang
- **Idempotenz** gegen Retry/Doppelklick/Netzwerkwiederholung
- **keine** destruktive Änderung historischer Items oder Movements

### 25.2 Aktueller Persistenz-Ist-Stand

#### Tabellen und Beziehungen

| Tabelle | Rolle heute | Phase-2-Relevanz |
|---------|-------------|------------------|
| `public.fertilizer_containers` | Inventory Item (Legacy-Name „Gebinde“) | Wird **weiterverwendet** als Item-Zeile; kanonische Produktbestände sind **Zeilen desselben Typs** mit neuer Semantik |
| `public.fertilizer_stock_movements` | Append-only Ledger (`container_id` → Item) | **Unveränderte Wahrheit** — Saldo = Summe wirksamer Movements |
| `public.product_profiles` | Saved Product Profile (`profile_status = saved`, immutable) | Versionsanker; Form↔Unit per Trigger |
| `public.fertilizer_inventory_creation_receipts` | Idempotenz für **Packungs-Creation** | Bleibt für Alt-Pfad; **nicht** für Produkt-Intake wiederverwenden |
| `public.fertilizer_application_batches` | Düngungs-Batch + Idempotenz | Unverändert (Phase 2+) |
| `public.fertilizer_capture_receipts` | Legacy-Capture | Unverändert |

**Foreign Keys:** `fertilizer_containers.saved_product_profile_id` → `product_profiles.id`; Movements → Container; Application-Batches → Container.

#### Spalten Inventory Item (`fertilizer_containers`)

| Spalte | Ist | Kanonischer Produktbestand |
|--------|-----|----------------------------|
| `user_id` | ✅ | ✅ Schlüsselbestandteil |
| `saved_product_profile_id` | ✅ Core-Pfad | ✅ Schlüsselbestandteil |
| `base_unit` | ✅ `kg`/`ml` | ✅ Schlüsselbestandteil |
| `access_kind` | ✅ | `authenticated_user` für persistierten Nutzerbestand |
| `package_size_value` / `package_size_unit` | ✅ nominale Packung | **Keine Identität** — bei kanonischem Item **NULL** (empfohlen) |
| `archived_at` | ✅ Soft-Archive | Aus **aktivem** Unique ausgeschlossen |
| `product_id`, `recognition_candidate_id` | Legacy | Nicht auf kanonischem Core-Pfad |
| `stock_kind` | ❌ fehlt | **Neu (additiv):** `product_stock` vs. Legacy |

**Kein** persistiertes `current_quantity` / `currentQuantity` auf Item-Ebene (bestätigt in Migrationen und [DL-025](../decisions/dl-025.md)).

#### Spalten Movement (`fertilizer_stock_movements`)

| Spalte | Ist | Intake-Vertrag |
|--------|-----|----------------|
| `quantity_delta` | signed numeric(18,4) | **Strikt positiv** für Zugang |
| `unit` | text / `kg`/`ml` Core | Muss Item-`base_unit` entsprechen |
| `movement_type` | Enum inkl. positiver Reasons | `initial_stock`, `purchase`, `gift_received` |
| `movement_at` | timestamptz (Core) | **Pflicht** für wirksamen Bestand |
| `movement_date` | date (Legacy) | Parallel gesetzt aus `movement_at` |
| `inventory_idempotency_key` | text | **Movement-Ebene** Idempotenz (bestehend) |
| `capture_idempotency_key` | Legacy | Nicht für Core-Intake |
| `application_batch_id` | Düngung | Nicht für Intake |

**Wirksamer Bestand:** Summe `quantity_delta` WHERE `movement_at IS NOT NULL` (RPC `append_fertilizer_inventory_core_movement`, Repository-Reads). Legacy-Zeilen ohne `movement_at` zählen **nicht** zum Core-Saldo.

#### RPCs und Mechanismen heute

| RPC / Mechanismus | Verhalten | Abweichung von DL-033 |
|-------------------|-----------|------------------------|
| `create_fertilizer_inventory_core_from_confirmed_packages` | Schleife: **1 Item + 1 Movement pro Packung** | Mehrere Items pro Produkt möglich |
| `append_fertilizer_inventory_core_movement` | `SELECT … FOR UPDATE` Item; Saldo-Check; Idempotenz auf `inventory_idempotency_key` | Wiederverwendbar für Intake-Movement |
| Creation Receipt | Advisory Lock + `(scope, idempotency_key)` Unique + Payload-Fingerprint | Muster für Intake-Receipt |

#### Indizes / Constraints heute

- **Kein** Unique auf `(user_id, saved_product_profile_id, base_unit)`
- Nicht-Unique: `fertilizer_containers_auth_saved_profile_idx (user_id, saved_product_profile_id)`
- Movement-Idempotenz: Unique `(user_id, inventory_idempotency_key)` WHERE authenticated + key gesetzt
- Immutability: Trigger verbietet UPDATE/DELETE auf Movements

#### Bestandsberechnung heute

- **DB:** `sum(quantity_delta) WHERE container_id = … AND movement_at IS NOT NULL`
- **Client:** `computeInventoryItemBalance` über Movement-Liste; Repository filtert `movement_at IS NOT NULL`
- **Kein** `currentQuantity`-Feld als Source of Truth

### 25.3 Kanonische Persistenzidentität

Persistiert identisch zum Domain-Core (Phase 1):

```
(user_id, saved_product_profile_id, base_unit)
```

| Regel | Persistenz |
|-------|------------|
| Saved Product Profile | FK + Trigger `profile_status = saved`; **Versionsanker** |
| Product Form | **Nicht** im Unique-Key; abgeleitet aus Profil (Trigger `validate_fertilizer_container_base_unit`) |
| Unterschiedliche Profile | **Getrennte** Items — keine Zusammenführung |
| `kg` vs. `ml` | **Getrennte** Items — keine Umrechnung |
| Package Size | **Nicht** im Schlüssel; optional NULL auf kanonischem Item |
| Gebinde-/Container-ID | PK `id` ist Laufzeit-Referenz, **keine** fachliche Packungsidentität |

### 25.4 Aktiver kanonischer Bestand

**Definition „aktiv“ (technisch, für Unique-Partial-Index):**

```text
access_kind = 'authenticated_user'
AND user_id IS NOT NULL
AND saved_product_profile_id IS NOT NULL
AND base_unit IS NOT NULL
AND archived_at IS NULL
AND stock_kind = 'product_stock'   -- neue Spalte, siehe §25.5
```

**Session-Scope** (`access_kind = session`): eigener Bestand mit `session_access_hash` — **nicht** Teil des authenticated Unique-Vertrags; separater partieller Index `(session_access_hash, saved_product_profile_id, base_unit)` optional analog.

**Legacy-Gebinde-Items** (bestehende Creation-Schleife): `stock_kind IS NULL` oder `stock_kind = 'physical_package'` — **ausgenommen** vom kanonischen Unique, bleiben lesbar bis Phase 3.

### 25.5 Unique-/Index-Strategie

#### Option A — Sofortiger vollständiger Unique Constraint

```sql
-- konzeptionell
UNIQUE (user_id, saved_product_profile_id, base_unit)
```

| Pro | Contra |
|-----|--------|
| Einfachste Semantik | **Scheitert**, sobald ≥2 aktive Core-Items denselben Schlüssel haben (heute durch Creation-Schleife **wahrscheinlich**) |
| Kein Zusatzfeld nötig | Blockiert Phase-2-Implementierung ohne vorherige Datenbereinigung |

**Bewertung:** Nur zulässig bei **garantiert konfliktfreien** Bestandsdaten — im Repository **nicht** als Startannahme.

#### Option B — Partieller Unique Index auf aktive kanonische Items ✅ **Empfohlen**

```sql
-- konzeptionell (Implementierung Phase 2)
CREATE UNIQUE INDEX … ON fertilizer_containers (user_id, saved_product_profile_id, base_unit)
WHERE access_kind = 'authenticated_user'
  AND saved_product_profile_id IS NOT NULL
  AND base_unit IS NOT NULL
  AND archived_at IS NULL
  AND stock_kind = 'product_stock';
```

| Pro | Contra |
|-----|--------|
| Legacy-Duplikate und Gebinde-Items **koexistieren** | Erfordert additive Spalte `stock_kind` |
| Phase 2 kann **sofort** neue kanonische Items schützen | Read-Seite muss Duplikate bis Phase 3 aggregieren/filtern |
| Archivierte Items ausgenommen | Zwei Semantiken auf einer Tabelle dokumentiert halten |

**Bewertung:** **Bevorzugt** — erfüllt Schutz neuer Schreibpfade ohne Phase-3-Vorarbeit.

#### Option C — Constraint erst nach Datenmigration (Phase 3)

| Pro | Contra |
|-----|--------|
| Kein Risiko durch Alt-Duplikate beim Constraint | **Kein** DB-Backstop für parallele Erst-Zugänge in Phase 2 |
| Einfacher Index nach Bereinigung | Race-Schutz nur über Anwendungslogik — **unzureichend** |

**Bewertung:** Als **alleinige** Phase-2-Strategie **verworfen**. Option C bleibt für **Verschärfung** nach Phase 3 (z. B. Entfernen `stock_kind`-Filter wenn nur noch kanonische aktive Zeilen existieren).

#### Empfehlung

**Option B** in Phase 2 implementieren. Option A erst prüfen, wenn Phase-3-Migrationsreport **null** Konflikte auf aktiven kanonischen Zeilen zeigt.

**Additive Spalte:**

| Spalte | Typ | Werte | Default |
|--------|-----|-------|---------|
| `stock_kind` | `text` CHECK | `product_stock`, `physical_package` | `NULL` (= historisches/legacy Item) |

Neue Intake-RPC setzt `stock_kind = 'product_stock'`, `package_size_* = NULL`.

**Voraussichtliche Constraint-Verletzer heute (Analyse, keine DB-Abfrage in diesem Schritt):**

- Mehrere `fertilizer_containers` mit gleichem `(user_id, saved_product_profile_id, base_unit)`, Core-gebunden, nicht archiviert — **erzeugt durch** `create_fertilizer_inventory_core_from_confirmed_packages`
- Legacy-Items ohne `saved_product_profile_id` — **nicht** im Partial Index
- Session-Items — separater Scope

### 25.6 Get-or-Create-Vertrag

**Fachlicher Ablauf** (serverseitig, eine Transaktion):

1. **Domain-Validierung** — spiegelt `validateFertilizerProductStockIntake` (Menge, Reason, IDs); Profil laden (`saved`, Form↔Unit).
2. **Kanonische Identität** — `(user_id, saved_product_profile_id, base_unit)`.
3. **Idempotenz-Receipt** — siehe §25.9; bei Replay → gespeichertes Ergebnis.
4. **Get-or-create Item:**
   - `INSERT INTO fertilizer_containers (…, stock_kind = 'product_stock')`
   - `ON CONFLICT` auf partiellem Unique **DO NOTHING**
   - `SELECT … FROM fertilizer_containers WHERE <Schlüssel> AND <aktiv-Kriterien> FOR UPDATE`
5. **Positive Movement** — über `append_fertilizer_inventory_core_movement` **oder** äquivalentes Inline-Insert mit denselben Checks (Saldo, Unit, Präzision, Immutability).
6. **Ergebnis** — `{ inventoryItemId, movementId, stockIdentity, quantityDelta, reason }` im Receipt persistieren.

**Regeln:**

- Ein Kauf erzeugt **kein** neues Item, wenn kanonisches Item existiert — nur Movement.
- Mehrere Käufe → mehrere Movements, **ein** Item.
- Kein clientseitiges „SELECT dann INSERT“ ohne DB-Schutz.

### 25.7 Race Conditions

| Szenario | Schutz |
|----------|--------|
| Zwei parallele **Erst**-Zugänge desselben Schlüssels | Partieller **Unique Index** + `INSERT … ON CONFLICT DO NOTHING` + anschließendes `SELECT … FOR UPDATE` |
| Parallele Zugänge auf **bestehendem** Item | `FOR UPDATE` auf Item-Zeile (bestehend in `append_*`) |
| Parallele **Idempotenz**-Retries | Receipt Unique + Advisory Lock (Muster Creation-RPC) + Movement Unique auf `inventory_idempotency_key` |
| Unique Violation nach Concurrent Insert | Catch → Re-SELECT Item → Movement append (Idempotenz-Key verhindert Doppel-Movement) |

**Warum clientseitiges Find-then-Insert nicht reicht:** Zwei Clients können beide „nicht gefunden“ lesen und zwei Items einfügen — nur ein **datenbankseitiger** Unique-Backstop verhindert das.

**Locks:** Keine manuelle Advisory-Lock-Pflicht für Get-or-Create **zusätzlich** zum Unique, sofern Transaktion + `FOR UPDATE` auf dem gefundenen Item. **Empfohlen:** Advisory Lock auf `(user_id, idempotency_key)` für Receipt-Serialisierung (bewährt in `create_fertilizer_inventory_core_from_confirmed_packages`).

### 25.8 Positive Zugangsbuchung (Persistenz)

#### Unterstützte Reasons (Domain = DB-Enum heute)

| Domain (Phase 1) | Persistiertes `movement_type` |
|------------------|-------------------------------|
| `initial_stock` | `initial_stock` |
| `purchase` | `purchase` |
| `gift_received` | `gift_received` |

Negative Domain-Namen ([DL-034](../decisions/dl-034.md) Ziel: `application`, `gift_given`, `disposed`) bleiben in Phase 2 **unimplementiert**; DB nutzt weiter `fertilization`, `gifted_away`, `disposal` — **keine Enum-Migration in Phase 2**.

#### Eigenschaften

| Anforderung | Persistenz |
|-------------|------------|
| Strikt positive Menge | RPC-Check `quantity_delta > 0` (analog append) |
| Max. 4 Nachkommastellen | DB + Domain; **keine** Rundung |
| Keine kg↔ml-Umrechnung | Item-Trigger + Movement-Unit-Check |
| Kein Restmengen-Update | Kein UPDATE auf Item-Menge |
| Kein Gebinde/FIFO | Kein `package_size` als Bestand; kein FIFO-Metadatum |
| Eine Movement pro Zugang | 1 Receipt → 1 Movement (Idempotenz) |

#### Spalten-Wiederverwendung

| Spalte | Phase 2 |
|--------|---------|
| `fertilizer_containers.user_id`, `saved_product_profile_id`, `base_unit`, `access_kind` | ✅ weiterverwenden |
| `fertilizer_containers.package_size_*` | Legacy/Gebinde; kanonisch **NULL** |
| `fertilizer_stock_movements.quantity_delta`, `unit`, `movement_type`, `movement_at` | ✅ weiterverwenden |
| `inventory_idempotency_key` | ✅ Movement-Idempotenz |
| `source_event_ref`, `note` | optional |
| **Neu:** `stock_kind` auf Container | ✅ kanonische Markierung |
| **Neu:** `fertilizer_product_stock_intake_receipts` | ✅ Request-Idempotenz |

#### Überholt (fachlich), vorerst beibehalten

- Creation-Schleife „1 Packung = 1 Item“ — Alt-RPC bleibt bis Phase 6/7
- `package_size_*` auf Legacy-Items
- Enum-Namen `fertilization` / `gifted_away` / `disposal`

#### Direction-Spalte

**Nicht erforderlich** — Vorzeichen in `quantity_delta` genügt; Zugangs-RPC akzeptiert nur positive Werte.

#### Zeitstempel

| Feld | Vertrag |
|------|---------|
| `movement_at` | **Pflicht** bei Core-Movements; Default `now()` UTC |
| `created_at` | Insert-Zeitpunkt |
| `movement_date` | Abgeleitet aus `movement_at` (Legacy-Kompatibilität) |

### 25.9 Idempotenzvertrag

#### Schichten

| Ebene | Mechanismus | Scope |
|-------|-------------|-------|
| **Request / Operation** | Neue Tabelle `fertilizer_product_stock_intake_receipts` | `(user_id, idempotency_key)` authenticated |
| **Movement** | `inventory_idempotency_key` Unique (bestehend) | `(user_id, inventory_idempotency_key)` |
| **Payload-Integrität** | `payload_fingerprint` SHA-256 auf kanonischem JSON | pro Receipt |

#### Client-Key

- Vom Client, Conversation-Layer oder Command-Handler erzeugt
- **Nicht** nutzerübergreifend wiederverwendbar
- Empfohlenes Prefix: `product-stock-intake:` + opaque Token
- Max. Länge analog Creation (256)

#### Verhalten

| Fall | Ergebnis |
|------|----------|
| Gleicher Key + gleicher Payload (Fingerprint) | **Replay:** gespeichertes `result_jsonb` zurückgeben; **keine** zweite Movement |
| Gleicher Key + abweichender Payload | **Konflikt:** `INVENTORY_INTAKE_IDEMPOTENCY_CONFLICT` (maschinenlesbar) |
| Verschiedene Keys, gleicher Zugang | **Zwei** Movements (zwei bewusste Buchungen) |

#### Movement-Key-Ableitung

Deterministisch aus Receipt: z. B. `product-stock-intake:{receipt_id}` — verhindert Doppel-Insert innerhalb derselben Operation auch bei partieller Retry.

#### Einheitlicher Vertrag

Onboarding (`initial_stock`), Startseiten-Conversation und Ausrüstungs-Intake **müssen denselben RPC** und dieselbe Receipt-Semantik nutzen — keine UI-only-Idempotenz.

### 25.10 Transaktionsgrenze

**Atomare Einheit (eine PostgreSQL-Transaktion):**

1. Receipt anlegen oder sperren (`FOR UPDATE`)
2. Payload-Fingerprint prüfen
3. Get-or-create kanonisches Item
4. Idempotenz auf Movement-Ebene prüfen
5. Positive Movement einfügen
6. Receipt `result_jsonb` + `completed_at` setzen
7. Commit

**Empfehlung:** **Eine SECURITY DEFINER RPC** `record_fertilizer_product_stock_intake` — analog Creation-RPC, intern ggf. `append_fertilizer_inventory_core_movement` aufrufen.

| Mechanismus | Rolle |
|-------------|-------|
| RPC | Einziger Schreib-Einstieg für Produkt-Intake |
| Transaktion | Alles oder nichts |
| `ON CONFLICT` | Get-or-create Item |
| Partieller Unique | Race-Backstop |
| `FOR UPDATE` | Item-Zeile für Movement-Serialisierung |
| Advisory Lock | Receipt-Scope Serialisierung |
| **Kein** separater Client-Multi-Step | Verboten |

**Locks:** Row-Lock via `FOR UPDATE` ausreichend; **keine** zusätzliche manuelle Lock-Hierarchie ohne Messbedarf.

### 25.11 Bestehende historische Daten — Schutz in Phase 2

Phase 2 **darf nicht:**

| Verbot | Begründung |
|--------|------------|
| Alte Items löschen | Historische FKs (`application_batches`, Journal) |
| Movements umhängen | Ledger immutable ([DL-019](../decisions/dl-019.md)) |
| Mengen zusammenführen | Phase 3 |
| Profile automatisch vereinigen | DL-018 |
| Movement Reasons umbenennen | Enum-Migration Phase 6+ |
| `currentQuantity` übernehmen | Nicht vorhanden / widerspricht DL-025 |
| Legacy-RPC entfernen | Kompatibilität |

**Read-Pfade** dürfen weiter alle Items listen; UI-Aggregation erst Phase 4.

### 25.12 Kompatibilitätsrisiken und Phasenzuordnung

| Konflikt | Beschreibung | Phase |
|----------|--------------|-------|
| **K-1** Mehrere Items pro Schlüssel | Creation-Schleife bei Mehrfachkauf | Phase 3 Konsolidierung |
| **K-2** Legacy ohne Saved Profile | Nicht application-eligible | Phase 3 / Legacy-Upgrade |
| **K-3** `package_size` auf Core-Items | Gebinde-Semantik in Daten | Phase 3 archivieren/supersede |
| **K-4** Saldo-Abweichung Movement-Summe vs. UI-Annahme | Kein `currentQuantity` — Rechenweg prüfen | Phase 4 Read-Model |
| **K-5** Movements ohne `movement_at` | Nicht im Core-Saldo | Phase 3 Legacy-Upgrade ([DL-028](../decisions/dl-028.md)) |
| **K-6** Enum `fertilization` vs. Domain `application` | Benennungslücke | Phase 6 |
| **K-7** Parallele Alt- und Neu-RPC | Doppelte Items wenn Alt-Pfad weiter genutzt | Phase 6 Routing; Phase 7 Entfernung |
| **K-8** Session- vs. User-Bestand | Getrennte Scopes | Phase 2 Spec: authenticated zuerst |
| **K-9** Archivierte Items mit Saldo | Unique ausgeschlossen | Phase 3 |
| **K-10** Inkompatible Form/Unit in Alt-Daten | Trigger verhindert Neues; Alt bleibt | Manuell / Phase 3 |

### 25.13 Nicht Teil von Phase 2

- Migration/Konsolidierung historischer Duplikate (Phase 3)
- UI, Read-Aggregation, Equipment (Phase 4)
- Startseiten-Flow (Phase 5)
- Capture/Creation-Umstellung (Phase 6)
- Negative Bewegungen / Düngungs-RPC-Änderung
- Enum-Umbenennung
- Legacy-RPC-Entfernung (Phase 7)
- FIFO, Gebinde-UI, `currentQuantity`-Spalte

### 25.14 Spätere Implementierungsschritte (nach dieser Spezifikation)

1. Migration: `stock_kind`; partieller Unique; Receipt-Tabelle; RLS/Grants
2. RPC `record_fertilizer_product_stock_intake` + interne Hilfsfunktionen
3. TypeScript: `fertilizerProductStockIntakeRpcCore.ts`, Repository-Anbindung
4. Dev-DB Harness-Tests (Gate C)
5. **Kein** produktiver Client-Umschalter bis Harness grün

### 25.15 Teststrategie (Implementierung)

#### Unique Identity

- Erster Zugang → genau 1 Item (`stock_kind = product_stock`)
- Zweiter Zugang → **dieselbe** `inventory_item_id`
- Anderer User / Profile / Base Unit → anderes Item

#### Concurrency

- Zwei parallele Erst-Zugänge → 1 Item, 2 Movements (wenn verschiedene Idempotency Keys) bzw. 1 Movement (gleicher Key)
- Kein unkontrollierter Unique-Verstoß nach außen

#### Idempotency

- Gleicher Key + Payload → 1 Movement, identisches Ergebnis
- Gleicher Key + anderer Payload → Konflikt
- Verschiedene Keys → verschiedene Movements

#### Quantity

- Positiv gültig; 0/negativ/NaN/∞ abgelehnt; >4 Dezimalstellen abgelehnt; keine Rundung; keine kg↔ml

#### Compatibility

- Legacy-Items unverändert nach Intake-Test
- Historische Movements unverändert
- Keine Journal-/Batch-FK-Änderung

#### Security

- RLS: Nutzer nur eigener Bestand
- Idempotency Key nicht nutzerübergreifend

### 25.16 Gate-Kriterien — Abschluss Phase 2

**Dokumentation (dieser Commit):**

- [x] Ist-Stand und Zielidentität dokumentiert
- [x] Unique-Strategie mit Empfehlung
- [x] Get-or-create + Race + Idempotenz + Transaktion spezifiziert
- [x] Schutz historischer Daten und Risikomatrix
- [x] Teststrategie und Phasengrenze

**Implementierung (folgender Commit):**

- [ ] Migration additive deploybar
- [ ] RPC + Dev-DB Harness grün
- [ ] Gate C Implementierung freigegeben
- [ ] Kein produktiver UI-Switch

---

## 26. Widerspruchsprüfung (Repository)

| Aussage / Ort | Klassifikation | Anmerkung |
|---------------|----------------|-----------|
| „Ein Inventory Item = physisches Gebinde“ — [DL-024](../decisions/dl-024.md), [DL-025](../decisions/dl-025.md), [GA-014](./ga-014.md), [GA-012](./ga-012.md) FIFO | **Historischer Kontext** | DL-033/DL-024 Header: **abgelöst** für v1 |
| „Kauf erzeugt N Items bei N Packungen“ — Creation-RPC, §4.2 | **Produktiver Ist-Widerspruch** | Phase 2 Intake-RPC behebt **Neuschreibpfad**; Alt-RPC bis Phase 6 |
| „Kein currentQuantity“ — DL-025, DL-033, Core, SQL | **Weiterhin korrekt** | |
| „Client Find-then-Insert“ — nicht dokumentiert als Ziel | **Verworfen für Phase 2** | Explizit RPC-only |
| „Mehrere aktive Items pro Produkt gewollt“ — GA-012 FIFO | **Historisch / ersetzt** | DL-033: genau ein Produktbestand |
| „Retry darf zweite Movement erzeugen“ | **Verworfen** | Receipt + Movement-Idempotenz |
| „Idempotenz nur UI“ — Creation-Muster | **Ersetzt durch DB-Receipt** | Phase 2 übernimmt Muster |
| „Historische Movements umhängen“ — nirgends als Ziel | **Verworfen** | Phase 3 nur Korrektur-**Paare** |
| „kg↔ml Umrechnung“ | **Verworfen** | Trigger + Domain |
| „Automatische Profilzusammenführung“ | **Verworfen** | DL-018 |
| [DL-030](../decisions/dl-030.md) FIFO-Verweis im Header | **Historischer Kontext** | Body verweist auf DL-033 Produktbestand |
| `fertilizerProductStockCore.ts` | **Weiterhin korrekt** | Phase 1 Vertrag |

Historische Dokumente werden **nicht** gelöscht; Widersprüche werden über DL-033/GA-015 Phase-Zuordnung aufgelöst.

---

## Verwandte Dokumente

- [DL-033 — Produktbasierter Düngerbestand](../decisions/dl-033.md)
- [DL-031 — Mehrflächen-Düngeranwendung](../decisions/dl-031.md)
- [GA-012 — Berechneter Bestand](./ga-012.md) (Mehrgebinde/FIFO: **nicht** v1-Richtung)
- [GM-008 — Vier Ebenen](../model/gm-008.md)
- [home-experience](../playbook/home-experience.md)
