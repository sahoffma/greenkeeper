# GA-015

| Feld | Wert |
|------|------|
| **ID** | GA-015 |
| **Titel** | Produktbasierter Düngerbestand — technische Umstellung |
| **Status** | 📋 Geplant |
| **Priorität** | Hoch |
| **Erstellt** | 2026-08-03 |
| **Zuletzt geändert** | 2026-08-03 |
| **Verantwortlich** | — |
| **Verwandte Dokumente** | [DL-033](../decisions/dl-033.md); [DL-034](../decisions/dl-034.md); [DL-020](../decisions/dl-020.md); [DL-025](../decisions/dl-025.md); [DL-030](../decisions/dl-030.md); [DL-031](../decisions/dl-031.md); [GA-012](./ga-012.md); [GM-008](../model/gm-008.md); [home-experience](../playbook/home-experience.md); [conversation-model](../playbook/conversation-model.md) |
| **Kurzbeschreibung** | Technischer Migrations- und Umsetzungsplan: von gebindebasierten `fertilizer_containers` zu produktbasiertem Bestand gemäß DL-033 — ohne FIFO, mit find-or-create Zugängen, Startseiten-Düngungsflow und Ausrüstung als Read-only-Bestand. |

---

## 1. Status

📋 **Geplant** — Architektur- und Migrationsplanung. **Phase 1 abgeschlossen** (`0d5d529`). **Phase 2 abgeschlossen** (`86ef0a2`, `14d3252` — Intake-RPC, Dev-DB-Harness). **Phase 3 abgeschlossen** (`2aa59b9` — Legacy-Migration-RPC, Movement Reason **`legacy_balance_migration`** in [DL-034](../decisions/dl-034.md)). **Phase 4 abgeschlossen** (`a0ca9cd`, `0322e18` — aktives Read Model, Ausrüstungsliste). **Phase 5 abgeschlossen** (`2715b4b` — Startseiten-Düngungsflow, §28). **Phase 6 abgeschlossen** (`f4f6a66`, `ab667dc`, `4f3254b` — Capture→Intake, Outbound-RPC, Profilvalidierung, §29). **Phase 7 — freigegebener Cleanup-Umfang abgeschlossen** (`5054e05`, `ebc1b70`, `150c87d`, `fe0f1af` — Schritt A, B1, D; §30.17); RPC-Drop und vollständiger Creation-Stack-Cleanup **blockiert** (OD-P7-1, OD-P7-3).

Produktvertrag: [DL-033](../decisions/dl-033.md); Bewegungsgründe: [DL-034](../decisions/dl-034.md); Mehrflächen-Flow: [DL-031](../decisions/dl-031.md).

---

## 2. Ausgangslage

Greenkeeper implementiert Düngerbestand heute als **Gebinde-Modell**:

| Ebene | Ist-Stand (Repository `bc49546` / `e14522d`) |
|-------|-----------------------------------------------|
| **Tabelle Inventory Item** | `public.fertilizer_containers` — Kommentar/Migration: „physisches Gebinde“ |
| **Tabelle Movement** | `public.fertilizer_stock_movements` — append-only, `movement_at` kanonisch |
| **Creation** | `create_fertilizer_inventory_core_from_confirmed_packages` — **ein Item pro Packung** in Schleife |
| **Zugang** | Initiale positive Movement pro neuem Item (`initial_stock`, `purchase`, `gift_received`) |
| **Bestandsliste** | Client: `fetchFertilizerStockList` — **ein Listeneintrag pro Container**, Saldo je Item |
| **Düngung** | Route `/ausruestung/duenger/:inventoryItemId/anwenden`, RPC `apply_fertilizer_inventory_item_to_areas` |
| **Constraint Düngung** | Unique Index `fertilizer_stock_movements_application_batch_idx` — **genau ein Movement pro Batch** (kompatibel mit DL-033) |
| **Unique Produktbestand** | **Kein** Unique Constraint auf `(user_id, saved_product_profile_id, base_unit)` — nur Indizes |

[DL-033](../decisions/dl-033.md) definiert das **Zielbild Version 1**: ein Inventory Item = **Produktbestand**, kein Gebinde, keine FIFO, Startseite als Tätigkeitseinstieg.

---

## 3. Zielvertrag (DL-033, technisch)

| Regel | Ziel |
|-------|------|
| Inventory Item | Ein **kompatibler mengenmäßiger Produktbestand** pro Schlüssel |
| Gebinde | **Nicht** modelliert; Packungsgröße nur Zugangsmengen-Eingabe |
| Zugang | Positive Movement auf **bestehendem oder neu angelegtem** Produktbestand |
| Düngung | **Genau eine** negative Movement; **ein** Batch; Mehrflächen unverändert |
| Bestand | Summe Movements mit `movement_at IS NOT NULL`; **kein** `currentQuantity` |
| FIFO / Gebindestatus | **Nicht** in Version 1 |
| Startseite | Primärer Einstieg „Düngung erfassen“ |
| Ausrüstung | Bestand/Nachschlage; **kein** primärer „Anwenden“-Button |
| Parallele Logik | **Verboten** — ein Flow, ein RPC |

---

## 4. Ist-Architektur (Detail)

### 4.1 Datenmodell

**`fertilizer_containers` (Inventory Item)**

- PK `id`; `user_id`; `saved_product_profile_id` (FK `product_profiles`, `profile_status = saved`)
- `access_kind` (`authenticated_user` | `session`); `base_unit` (`kg` | `ml`)
- `package_size_value` / `package_size_unit` — nominale Packung
- Legacy: `product_id`, `recognition_candidate_id`, `label`
- Kein `product_form` auf Container — Form über Join `product_profiles.product_form`
- Keine Spalte `product_version_id`
- Saldo: abgeleitet aus Movements

**`fertilizer_stock_movements`**

- `container_id` → Item; `quantity_delta`; `unit`; `movement_type`; `movement_origin`
- `movement_at` — kanonisch für Saldo
- `application_batch_id` — FK zu `fertilizer_application_batches` (unique wenn gesetzt)
- Immutability: Trigger `prevent_fertilizer_stock_movement_update/delete`

**Application / Journal**

- `fertilizer_application_batches` — 1 pro Düngung; `inventory_item_id`, Idempotenz
- `fertilizer_application_areas` — je Fläche
- `activities` + `fertilization_details` — je Fläche; Snapshots
- RPC: `apply_fertilizer_inventory_item_to_areas(p_inventory_item_id, p_saved_product_profile_id, …)`

**Saved Product Profile**

- `product_profiles.profile_status = 'saved'` — **immutable** (Trigger `prevent_saved_product_profile_mutation`)
- Repräsentiert konkrete Produktversion ([DL-018](../decisions/dl-018.md))
- `product_form` (`granular` | `liquid`) auf Profil

### 4.2 Erstellung und Zugang

- **RPC:** `create_fertilizer_inventory_core_from_confirmed_packages(p_saved_product_profile_id, packages[], reason, …)`
- Schleife: **pro Packung** INSERT Container + INSERT initiale positive Movement
- Receipt/Idempotenz: `fertilizer_inventory_creation_receipts`
- Client: `fertilizerInventoryCreation*.ts`, Capture-Integration

### 4.3 Bestandsanzeige

- `fetchFertilizerStockList()` — lädt alle Container, berechnet Saldo je Item
- `FertilizerCategoryPage` — Listeneintrag je Item, Text „Gebinde …“, Link **„Anwenden“**
- Keine serverseitige Aggregation nach Produktbestandsschlüssel

### 4.4 Düngung

- `FertilizerApplicationPage` — `useParams().inventoryItemId` Pflicht
- Flow-Core: `fertilizerApplicationFlowCore.ts`, Multi-Area: `fertilizerMultiAreaApplication*.ts`
- Submit: `applyFertilizerInventoryItemToAreas` → RPC mit `p_inventory_item_id`
- Eligibility: Core-gebundenes Item (`saved_product_profile_id`, `access_kind`, `base_unit`)

### 4.5 Startseite

- `HomeScreen`, `HomeVoiceSection` — Sprache/Text dokumentiert ([home-experience](../playbook/home-experience.md))
- **Kein** dedizierter CTA „Düngung erfassen“ mit Produktauswahl
- `homeVoiceFlow.ts` — Intent-Struktur vorhanden, Düngung noch nicht an Mehrflächen-RPC angebunden

---

## 5. Abweichungen von DL-033

| # | Ist | Soll (DL-033) |
|---|-----|---------------|
| 1 | Ein Container = ein Gebinde | Ein Container = **Produktbestand** |
| 2 | Mehrere Items pro kompatiblem Produkt möglich | **Genau ein** anwendbarer Produktbestand pro Schlüssel |
| 3 | Kauf erzeugt N Items bei N Packungen | Kauf erzeugt **eine** positive Movement auf bestehendem Item |
| 4 | Bestandsliste itemweise | **Produktbestandsliste** (ein Eintrag pro Schlüssel) |
| 5 | Düngung startet in Ausrüstung mit Item-ID | Startseite wählt **Produktbestand** |
| 6 | UI „Gebinde“, „Anwenden“ pro Item | Kein Gebindetext; **kein** Apply-CTA in Ausrüstung |
| 7 | GA-012 beschreibt FIFO/Mehrgebinde | DL-033 **verwirft** FIFO für v1 — GA-012 Mehrgebinde-Regeln sind **nicht** produktive Richtung v1 |

**Bereits kompatibel:** Ein Movement pro Batch; Mehrflächen-RPC; Ledger; Idempotenz; Atomarität; Overdraft-Schutz im RPC.

---

## 6. Produktbestandsschlüssel

### 6.1 Fachlicher Schlüssel (DL-033)

```
(user_id, saved_product_profile_id, form, base_unit)
```

### 6.2 Persistierte Felder (Ist)

| Komponente | Persistenz | Quelle |
|------------|------------|--------|
| `user_id` | ✅ | `fertilizer_containers.user_id` |
| `saved_product_profile_id` | ✅ | `fertilizer_containers.saved_product_profile_id` |
| `form` | ⚠️ indirekt | `product_profiles.product_form` (immutable wenn `saved`) |
| `base_unit` | ✅ | `fertilizer_containers.base_unit` |

**Keine** Spalte `product_version_id`. **Keine** neue Spalte in Phase 1 erfinden, sofern Gate A grün.

### 6.3 Empfehlung Unique Constraint (Gate A)

```sql
-- Konzeptionell (Umsetzung Phase 2):
UNIQUE (user_id, saved_product_profile_id, base_unit)
WHERE access_kind = 'authenticated_user'
  AND saved_product_profile_id IS NOT NULL
  AND archived_at IS NULL
  AND stock_kind = 'product_stock'  -- optional, siehe Migration
```

**Begründung:** `saved_product_profile_id` bindet **immutable** an genau eine `product_form` ([DL-018](../decisions/dl-018.md), Trigger auf `saved`). `base_unit` ist redundant zu Form nur insofern, als DB bereits `granular→kg`, `liquid→ml` erzwingt — **beide** im Schlüssel dokumentieren, Unique technisch auf `(user_id, saved_product_profile_id, base_unit)`.

**Session-Scope** (`access_kind = session`): separater Schlüssel mit `session_access_hash` — nicht Teil des authenticated Produktbestands.

---

## 7. Versionsanker

### Ist-Stand

- **`saved_product_profile_id`** verweist auf `product_profiles` mit `profile_status = 'saved'`
- Saved Profiles sind **unveränderlich** (`prevent_saved_product_profile_mutation`)
- Neue Herstellerdeklaration → **neues** Saved Profile ([DL-018](../decisions/dl-018.md)) — **keine** Zusammenführung

### Beantwortung Gate A

| Frage | Antwort |
|-------|---------|
| Reicht `saved_product_profile_id` als Produkt- **und** Versionsanker? | **Ja**, für v1 — Saved Profile = konkrete unveränderliche Produktversion |
| Kann Saved Profile später verändert werden? | **Nein** (DB-Trigger) |
| Braucht es `product_version_id`? | **Nein** in v1 — offene Ergänzung nur falls später explizite Versionstabelle eingeführt wird |

### Offene Frage (Gate A, dokumentiert)

Ob **zwei** Saved Profiles fachlich „dasselbe Produkt“ sein können, ohne dass der Nutzer es erkennt — dann entstehen **zwei** Produktbestände (**korrekt** nach DL-018/DL-033). Keine automatische Merge-Heuristik.

---

## 8. Zielarchitektur

### 8.1 Produktbestand

```
Produktbestand (1 × fertilizer_containers-Zeile pro Schlüssel)
  └── Movements (append-only, movement_at gesetzt)
        ├── + initial_stock | purchase | gift_received
        └── − application | gift_given | disposed | inventory_correction
              (application nur mit Application Batch / Journal)
```

Bewegungssemantik: [DL-034](../decisions/dl-034.md). Ist-Enum nutzt teils `fertilization`, `gifted_away`, `disposal` — Ausrichtung in Phase 2/6.

- Kein `currentQuantity`
- Keine Gebindeobjekte, kein FIFO
- `package_size_*` optional, nur Capture-Hilfe

### 8.2 Zugang

```
find_or_create_product_stock(user, saved_product_profile_id, base_unit)
  → atomar: SELECT … FOR UPDATE | INSERT
  → append_positive_movement(amount, reason)
```

- Idempotenz über Receipt + Movement-Key (bestehendes Muster)
- Race: Unique Constraint + Retry auf Unique Violation → Re-SELECT

### 8.3 Anwendung

```
Startseite → Produktbestand wählen → Flächen/Modus/Menge → Bestätigung
  → apply_fertilizer_inventory_item_to_areas(p_inventory_item_id = kanonische Item-ID, …)
  → 1 Batch, N Activities, 1 negative Movement
```

- **RPC-Semantik unverändert** — nur Einstieg und Item-Auflösung ändern sich
- `p_inventory_item_id` bleibt — Wert = **Produktbestands-Item**

### 8.4 Ausrüstung

- Read-only Liste: Produktname, Saldo, Base Unit
- Kein „Anwenden“; optional Produkt-/Profil-Detail
- Capture/Erfassung bleibt separater Zugang (Phase 6)

### 8.5 Journal

- Unverändert: je Area Activity; Snapshots; keine sichtbare Bestandskomplexität

---

## 9. Zugangsbuchungen (Plan)

### Betroffene RPCs

| RPC | Heute | Ziel |
|-----|-------|------|
| `create_fertilizer_inventory_core_from_confirmed_packages` | N Items | **Ersetzen/ erweitern** durch `record_fertilizer_product_stock_intake` (Arbeitstitel) |
| Legacy `save_fertilizer_capture` | Legacy-Container | Übergang unverändert; Core-Pfad migrieren |

### Neuer Vertrag (Phase 2, konzeptionell)

**Input:** `saved_product_profile_id`, `quantity`, `base_unit`, `reason` (`initial_stock`|`purchase`|`gift_received`), `idempotency_key`, optional `package_size_hint`

**Ablauf:**

1. Profil laden + validieren (`saved`, Form↔Unit)
2. Produktbestand find-or-create (Unique + Lock)
3. Positive Movement append
4. Receipt speichern

**Packungsgröße:** nur Request-Metadatum für UI; **nicht** auf Item persistieren als Identität (optional weiter als nullable Metadatum der **Movement**-Notiz).

---

## 10. Düngungsanwendungen (Plan)

| Frage | Empfehlung |
|-------|------------|
| RPC ändern? | **Nein** — `apply_fertilizer_inventory_item_to_areas` bleibt |
| `p_inventory_item_id`? | **Ja** — intern korrekt; Client löst aus Produktauswahl auf |
| Route ersetzen? | **Ja:** `/duengung` statt `/ausruestung/duenger/:id/anwenden` (§28.8) |
| Übergangsroute? | **Ja:** alte Route redirect oder Fehlerseite mit Hinweis bis Phase 7 |
| Zwei Flows? | **Nein** — `FertilizerApplicationPage` parameterlos; Produktauswahl Schritt 1 |
| Flow-Core | Entkoppeln von `inventoryItemId` in URL; Draft trägt `productStockId` nach Auswahl |
| Idempotenz | Unverändert batch-scoped |
| Bestand zwischen Auswahl und Submit | RPC prüft Saldo **erneut** in Transaktion (bestehend) |

---

## 11. Startseitenintegration

- `homeVoiceFlow` / Startseiten-Unterhaltung = **Open Conversation** (Orchestrator); Ausrüstungs-Capture = **Scoped Capture** — ein Flow-Core, **keine** zweite Conversation- oder Bestandslogik ([CM-015](../playbook/conversation-model.md#cm-015--offene-unterhaltung-und-geführte-erfassung))

### Empfohlene Reihenfolge (v1)

1. **Deterministischer CTA** „Düngung erfassen“ auf Startseite (Button/Kachel)
2. Produktauswahl aus verfügbarem Bestand
3. Wiederverwendung `FertilizerApplicationPage`-Logik (Flächen, Modi, Bestätigung)
4. **Später:** Sprache/Text → **dieselbe** State-Machine / RPC ([home-experience](../playbook/home-experience.md) HE-002)

**Begründung:** Manifest/Home-Experience priorisiert Sprache langfristig, aber technischer Umbau ohne NL reduziert Risiko ([DL-033](../decisions/dl-033.md) offene Frage 9). Keine parallele NL-Sonderlogik in Phasen 1–5.

### Flow-Schritte

1. Startseite → CTA
2. Produktbestand wählen (nur `balance > 0`, application-eligible)
3. Areas wählen (bestehend)
4. Modus + Menge (bestehend)
5. Zusammenfassung (bestehend)
6. Atomar speichern
7. Erfolg → Journal/Home

---

## 12. Ausrüstungsbereich

| Komponente | Aktion |
|------------|--------|
| `FertilizerCategoryPage` | Apply-Link entfernen; Gebinde-Metadaten entfernen; **Produktbestandszeilen** |
| `fetchFertilizerStockList` | Serverseitig aggregieren oder Client gruppiert nach Schlüssel |
| `FertilizerStockListItem` | Typ: `productStockId` statt Container-Liste; kein `packageSize` in Primärdarstellung |
| `fertilizerApplicationPath` | Entfernen aus Ausrüstung (Phase 7) |

**Übergang:** Wenn Migration noch Duplikate hat, zeigt UI **einen** Eintrag pro Schlüssel mit **aggregiertem** Saldo (read-side), bis Phase 3 kanonisiert.

---

## 13. Datenmigration (Strategie — Überblick)

> **Detailvertrag:** §27 (Phase 3). Dieser Abschnitt bleibt als Kurzüberblick; bei Widersprüchen gilt §27.

### Prinzipien

- **Additiv zuerst** — keine destruktive Löschung von Movements oder Items
- **Ledger unveränderlich** — kein UPDATE/DELETE/Umhängen auf `fertilizer_stock_movements` ([DL-019](../decisions/dl-019.md))
- **Historische FKs** — `fertilization_details`, `application_batches`, Journal-Snapshots **nicht** umschreiben
- **Rollback** — Archivierung + Mapping-Receipt; keine Löschung

### Duplikat-Gruppen

**Erkennung:** Gleicher Migrationsgruppenschlüssel `(user_id, saved_product_profile_id, base_unit)` — **nur** über identische `saved_product_profile_id`, nicht über Produktname oder Package Size (§27.3).

**Kanonisches Ziel:** vorhandenes aktives `product_stock`-Item **oder** neu angelegtes kanonisches Item (Phase-2-Intake-Muster).

**Legacy-Items nach Migration:** `archived_at` gesetzt; `superseded_by_container_id` → kanonisches Item; historische Movements **bleiben** am Legacy-Container.

**Saldoübernahme (empfohlen, §27.6):** Einmalige **positive Übernahmebewegung** auf dem kanonischen Item; **keine** Korrektur-Paare auf Legacy-Items; **keine** Read-Aggregation als Ersatz für Buchung.

**Verworfen für Phase 3:** Korrektur-Paare (`inventory_correction` − / `purchase` + auf zwei Items), die in frühen Entwürfen diskutiert wurden — widersprechen DL-034 und erhöhen Doppelzählungsrisiko.

### Legacy ohne Core-Bindung

- Nicht application-eligible (bestehend)
- Klasse E — keine automatische Produktbestands-Migration ([DL-030](../decisions/dl-030.md), Legacy-Upgrade-Pfad separat)

### Verbrauchte Alt-Items (Saldo 0)

- Supersede/Archivierung ohne Übernahmebewegung (Klasse J)

### Inkompatible Items

- Nicht zusammenführen; Klassen F–H blockiert (§27.12)

---

## 14. Kompatibilität

| Phase | Alt-Client | Neuer Client |
|-------|------------|--------------|
| Nach Phase 2 RPC | Creation erzeugt weiter Items pro Packung | Neuer Intake-RPC |
| Nach Phase 4 UI | Item-Liste + Apply | Produktliste ohne Apply |
| Nach Phase 5 Flow | Deep-Link Item-Route | Startseiten-Flow |

**Kompatibilitätsfenster:** Alte Route bleibt bis Phase 7; redirect auf Startseiten-Flow wenn Item kanonisch, sonst Hinweis.

**RPC-Entfernung:** erst Gate E — nach Migration + Clean-HEAD + Dev-DB.

---

## 15. Idempotenz

- **Zugang:** Receipt-Tabelle analog `fertilizer_inventory_creation_receipts`; Fingerprint inkl. `saved_product_profile_id`, `quantity`, `reason`, `idempotency_key`
- **Düngung:** unverändert `fertilizer_application_batches` + `request_fingerprint`
- **Migration:** Migrations-Receipt oder `migration_batch_id` pro konsolidiertem Item-Paar — Wiederholung safe

---

## 16. Konkurrenz und Locks

| Vorgang | Mechanismus |
|---------|-------------|
| Parallele Zugänge | `SELECT … FOR UPDATE` auf kanonischem Item **oder** Advisory Lock auf `(user_id, profile_id, base_unit)`; Unique als Backstop |
| Parallele Düngungen | Bestehend: Item-Lock im RPC + Saldo-Reprüfung |
| Migration | Batch-weise; keine User-Writes während Item-Konsolidierung pro Gruppe (Maintenance-Flag optional) |

---

## 17. Teststrategie

### Domain (Phase 1)

- Gleicher Schlüssel; inkompatible Form/Unit/Profil
- Positiver/negativer Saldo; Präzision; unzureichender Bestand
- Find-or-create Semantik

### Persistenz (Phase 2)

- Erster Zugang erstellt Item; zweiter nutzt dasselbe
- Parallele Zugänge → kein Duplikat
- Idempotenz Replay/Konflikt
- Düngung: genau 1 Movement; Rollback; RLS; Immutability

### Migration (Phase 3)

- 1 / N kompatible Alt-Items; historische Anwendungen; verbraucht; Legacy; inkompatibel
- Movement-Summen invariant; wiederholbare Migration

### UI (Phase 4–5)

- Ausrüstung ohne Apply; Produktbestand sichtbar
- Startseiten-CTA; Ein-/Mehrflächen; Pflegegruppe; Fehler erhält Eingaben
- Kein Gebindetext

### Clean-HEAD

- Jede Phase committbar unabhängig vom lokalen WIP
- Dev-DB-Tests nur mit Opt-in ([GA-010](./ga-010.md))

---

## 18. Phasenplan

### Phase 1 — Produktbestands-Domain und Datenvertrag ✅

| | |
|---|---|
| **Status** | ✅ Abgeschlossen (`0d5d529`) |
| **Ziel** | Domain-Core für Schlüssel und positive Zugänge |
| **Vertrag** | DL-033 Schlüssel; DL-034 positive Reasons |
| **Dateien** | `src/lib/fertilizerProductStockCore.ts`, `src/lib/fertilizerProductStockCore.test.ts` |
| **Tabellen/RPCs** | Keine |
| **Tests** | 40 Unit-Tests (Identität, Intake, Form/Unit, Abgrenzung) |
| **Abnahme** | Core grün; Gate A Dokumentation final |
| **Commit** | `feat: define product-based fertilizer stock intake core` |

### Phase 2 — Produktbestands-Persistenz (Spezifikation)

| | |
|---|---|
| **Status** | 📋 Spezifikation in §25 — Implementierung folgt |
| **Ziel** | Additive Migration + atomarer Intake-RPC |
| **Vertrag** | Partieller Unique Index; get-or-create + positive Movement |
| **Migration** | `stock_kind`; partieller Unique; Receipt-Tabelle; RPC `record_fertilizer_product_stock_intake` |
| **Dateien** | Migration SQL; `fertilizerProductStockIntake*.ts`; Dev-DB Harness |
| **Tests** | DB: Duplikat-Verhinderung, Idempotenz, Concurrency, RLS |
| **Abnahme** | Gate C grün; Dev-DB grün |
| **Risiken** | Race — Unique + Lock; bestehende Duplikate |
| **Abhängigkeit** | Phase 1 ✅ |
| **Dokumentations-Commit** | `docs: define fertilizer stock persistence phase` |
| **Implementierungs-Commit** | `feat: add product stock intake persistence` |

### Phase 3 — Bestehende Daten (Spezifikation)

| | |
|---|---|
| **Status** | 📋 Spezifikation in §27 — Implementierung folgt |
| **Ziel** | Sichere Klassifikation und Migration bestehender Bestände vor Read-Model-Umstellung |
| **Vertrag** | Ledger unverändert; Saldoübernahme auditierbar; keine Profilzusammenführung |
| **Migration** | Dry Run → RPC/Script `migrate_fertilizer_product_stock_group` (Arbeitstitel); Mapping-Receipt-Tabelle |
| **Tests** | Klassifikation A–J; Idempotenz; Concurrency; Legacy-Schreibschutz |
| **Abnahme** | Gate B grün; Dry-Run-Bericht reviewed; Dev-DB grün |
| **Rollback** | Mapping-Receipt + Supersede rückverfolgbar; keine Movement-Löschung |
| **Dokumentations-Commit** | `docs: define fertilizer stock legacy migration phase` |
| **Implementierungs-Commit** | `feat: migrate container inventory to product stock` (später) |

### Phase 4 — Read-Runtime und Ausrüstung

| | |
|---|---|
| **Status** | ✅ Umgesetzt — aktives Read Model liest ausschließlich aktive kanonische `product_stock`-Items |
| **Ziel** | Produktbestandsliste; supersedete/archivierte Legacy-Items ausgeschlossen; keine Doppelzählung |
| **SQL** | `list_active_fertilizer_product_stock`, `get_active_fertilizer_product_stock_item` (`20250812_fertilizer_product_stock_read_model.sql`) |
| **Dateien** | `fertilizerProductStockReadCore.ts`, `fertilizerInventory.ts`, `FertilizerCategoryPage.tsx` |
| **Read-Filter** | `stock_kind = 'product_stock'`, `archived_at IS NULL`, `superseded_by_container_id IS NULL`, gültiges Saved Profile, `base_unit IN ('kg','ml')` |
| **Saldo** | Summe kanonischer Movements mit `movement_at IS NOT NULL`; `legacy_balance_migration` einmal gezählt; Legacy-Movements nicht im aktiven Saldo |
| **Gruppierung** | `(user_id, saved_product_profile_id, base_unit)` — kein Merge über Produktname oder Package Size |
| **Historisch** | Legacy-Items und Movements unverändert lesbar; Journal-/Application-Referenzen unverändert |
| **Tests** | Unit (`fertilizerProductStockReadCore.test.ts`); DB (`fertilizerProductStockReadDatabase.test.ts`) |
| **Commit** | `feat: switch fertilizer stock read model to canonical items` |

### Phase 5 — Startseiten-Düngungsflow ✅

| | |
|---|---|
| **Status** | ✅ Abgeschlossen (`2715b4b`) — Vertrag §28 |
| **Ziel** | Startseite als primärer Einstieg „Düngung erfassen“; Produktbestand wählen; bestehender Mehrflächen-Flow |
| **Route** | `/duengung`, `/duengung/:inventoryItemId`; Alt-Route Redirect bis Phase 7 |
| **RPC** | Unverändert `apply_fertilizer_inventory_item_to_areas`; Read: `list_active_fertilizer_product_stock`, `get_active_fertilizer_product_stock_item` |
| **Dateien** | `HomeScreen.tsx`, `FertilizerApplicationPage.tsx`, `fertilizerApplicationFlowCore.ts`, `fertilizerRoutes.ts`, `FertilizerCategoryPage.tsx`, `App.tsx` |
| **SQL/Migration** | Keine |
| **Tests** | Unit (`fertilizerApplicationFlowCore.test.ts`); Regression Application-/Read-Core |
| **Abnahme** | Gate D (§28.17); E2E **nicht** Teil der Kernphase |
| **Commit** | `feat: start fertilizer application from home` |

### Phase 6 — Kauf, Abgänge und vereinheitlichter Capture-/Intake-Vertrag ✅

| | |
|---|---|
| **Status** | ✅ Abgeschlossen — §29 |
| **Ziel** | Alle positiven Zugänge über `record_fertilizer_product_stock_intake`; reale Abgänge und Korrekturen über Outbound-RPC; Capture speichert **ein** kanonisches Item pro Ereignis |
| **Dateien** | `fertilizerCaptureInventorySaveCore.ts`, `fertilizerProductStockIntake*.ts`, `fertilizerProductStockOutbound*.ts`, `fertilizerStockEventFlowCore.ts`, `FertilizerCategoryPage.tsx`, `FertilizerCaptureFlow.tsx`, `fertilizerRoutes.ts` |
| **SQL/Migration** | `20250813_fertilizer_product_stock_outbound.sql`, `20250814_fertilizer_product_stock_outbound_profile_validation.sql` |
| **Abnahme** | Gate F (§29.17) |
| **Commits** | `f4f6a66` feat, `ab667dc` test, `4f3254b` fix |

Optional **Onboarding-Initialerfassung** (`initial_stock` nach Flächen): **sprachgeführter** Dialog ([Onboarding](../playbook/onboarding.md#sprachgeführte-erfassung-vorhandener-düngerbestände), [CM-014](../playbook/conversation-model.md#cm-014--sprachgeführter-initialbestand-im-onboarding)), überspringbar — **nicht** Teil von Phase 6 oder Phase 7.

### Phase 7 — Bereinigung (technisch)

| | |
|---|---|
| **Status** | ✅ **Freigegebener Cleanup-Umfang abgeschlossen** (§30.17) — Schritt A, B1, D veröffentlicht; RPC-Drop und vollständiger Creation-Stack-Cleanup **blockiert** |
| **Ziel** | Veraltete Creation-RPC, Legacy-Capture-RPC, tote Package-Creation-Pfade, Alt-Apply-Kompatibilität und obsolete Adapter entfernen — **ohne** Phase-6-Vertrag zu ändern |
| **Abnahme** | Gate E (§30.12) — **teilweise** für freigegebenen Umfang; vollständige Gate-E-Freigabe erst nach Schritt C |
| **Commit(s)** | `5054e05` (Scope), `ebc1b70` (Schritt A), `150c87d` (B1), `fe0f1af` (D) |

---

## 19. Risiken

| Risiko | Schwere | Wahrscheinlichkeit | Gegenmaßnahme | Phase |
|--------|---------|-------------------|---------------|-------|
| Versionsanker unklar | Hoch | Niedrig | Saved Profile immutable — Gate A | 1 |
| Doppelte Produktbestände | Hoch | Mittel | Unique + find-or-create | 2 |
| Doppelzählung nach Migration | Hoch | Mittel | Übernahmebewegung + Supersede; Read nur kanonisch | 3–4 |
| Batch-FK bricht | Hoch | Niedrig | Historische IDs unverändert | 3 |
| Alt/Neu-Client | Mittel | Mittel | Kompatibilitätsfenster | 4–7 |
| Item-Route Bookmarks | Niedrig | Hoch | Redirect | 5 |
| Mehrere Alt-Items | Mittel | Hoch | Migrationsvertrag | 3 |
| Race Zugang | Hoch | Mittel | Lock + Unique | 2 |
| Race Düngung | Mittel | Niedrig | Bestehend RPC | 5 |
| Legacy-Bestände | Mittel | Mittel | Eligibility-Filter | 3–4 |
| Falsche Merge | Kritisch | Niedrig | Kein Merge inkompatibel | 3 |
| Big-Bang | Hoch | Mittel | 7 Phasen | alle |
| Zweiter Flow | Hoch | Mittel | Ein Page, ein RPC | 5 |
| Sprache zu früh | Mittel | Mittel | CTA zuerst | 5 vs später |

**Blockierend vor Start Phase 2:** Gate A + Gate C Entwurf. **Blockierend Phase 3 Prod:** Gate B.

---

## 20. Offene Entscheidungen

| ID | Frage | Gate | Status |
|----|-------|------|--------|
| OD-1 | Korrektur-Paare vs. Read-Aggregation vs. Übernahmebewegung | B | ✅ **Entschieden:** Übernahmebewegung (§27.6) |
| OD-2 | `superseded_by_container_id` vs. nur `archived_at` vs. Mapping-Tabelle | B | ✅ **Entschieden:** Mapping-Tabelle + `superseded_by` + `archived_at` (§27.8) |
| OD-3 | Exakte Route Startseiten-Flow | D | ✅ **Entschieden:** `/duengung` und `/duengung/:inventoryItemId` (§28.8) |
| OD-4 | Denormalisiertes `product_form` auf Container für Unique | A | Entschieden: nicht nötig |
| OD-5 | Wann `create_fertilizer_inventory_core_from_confirmed_packages` deprecaten | E | ✅ **Phase 6:** deprecate für neue Saves (§29); **Phase 7:** Entfernung |
| OD-6 | GA-012 Mehrgebinde-Abschnitt dokumentarisch anpassen | 7 | Offen |
| **OD-M1** | Eigener technischer Movement Reason für Saldoübernahme | B | ✅ **Entschieden:** `legacy_balance_migration` ([DL-034](../decisions/dl-034.md)) |

---

## 21. Abnahmekriterien (Gesamt)

- [ ] Ein Produktbestand pro Schlüssel (Unique + Verhalten)
- [ ] Käufe erhöhen bestehenden Bestand
- [ ] Düngung: 1 Batch, 1 Movement, N Activities
- [ ] Startseite startet Flow; Ausrüstung ohne Apply
- [ ] Kein FIFO-Codepfad aktiv
- [ ] Ledger-Summen nach Migration invariant
- [ ] Historische Journal-Einträge lesbar
- [ ] Dev-DB + Unit grün; Clean-HEAD

---

## 22. Empfohlene Umsetzungsreihenfolge

```
Gate A → Phase 1 → Phase 2 → Gate C → Phase 3 → Gate B
  → Phase 4 → Phase 5 → Gate D → Phase 6 → Gate F → Phase 7 → Gate E
```

Sprache/Text (**homeVoiceFlow**) **nach** Gate F — eigener Sprint, gleicher Intake-/Outbound-Core (§29.13).

---

## 23. Betroffene Dateien und Komponenten

### Domain / Runtime

- `src/lib/fertilizerProductStockCore.ts` (neu)
- `src/lib/fertilizerInventoryStockListCore.ts`
- `src/lib/fertilizerInventory.ts`
- `src/lib/fertilizerInventoryCreationCore.ts`
- `src/lib/fertilizerInventoryCreationRpcCore.ts`
- `src/lib/fertilizerApplicationFlowCore.ts`
- `src/lib/fertilizerMultiAreaApplication.ts`
- `src/types/fertilizerInventory.ts`

### UI

- `src/pages/FertilizerCategoryPage.tsx`
- `src/pages/FertilizerApplicationPage.tsx`
- `src/pages/HomeScreen.tsx`
- `src/App.tsx`
- `src/lib/fertilizerRoutes.ts`

### SQL / RPC (später)

- `supabase/migrations/20250806_fertilizer_inventory_creation_core.sql` (ersetzt)
- `supabase/migrations/20250809_fertilizer_multi_area_application_atomic.sql` (Referenz, minimal)
- Neue Migrationen Phase 2–3

### Tests

- `src/lib/fertilizer*.test.ts`
- `src/lib/fertilizerInventoryCreationDatabaseTestHarness.ts`
- `src/lib/fertilizerApplicationDatabaseTestHarness.ts`

---

## 24. Nicht Teil dieses Umbaus

- FIFO, LIFO, Gebindestatus
- Sprach-/NL-Parsing für Düngung (eigener Block)
- Chargen, MHD, Lagerort
- `currentQuantity`-Spalte
- Product-Version-Tabelle (optional später)
- Equipment außer Düngerbestand
- E2E-Vollausbau (optional nach Phase 5)

---

## Entscheidungsgates

### Gate A — Produktbestandsschlüssel

**Freigabe Phase 2 wenn:** Unique-Felder festgelegt; Saved Profile als Versionsanker bestätigt; Form↔Unit-Regel dokumentiert.

### Gate B — Altbestände

**Freigabe produktive Migration wenn:**

- Klassifikation A–J dokumentiert (§27.4)
- Saldoübernahmevertrag festgelegt (§27.6); Movement Reason **`legacy_balance_migration`** verbindlich ([DL-034](../decisions/dl-034.md))
- Mapping-/Supersede-Strategie festgelegt (§27.8)
- Idempotenz- und Transaktionsgrenze dokumentiert (§27.9–27.10)
- Legacy-Schreibschutzanforderungen dokumentiert (§27.11)
- Konfliktmatrix und Dry-Run-Gate dokumentiert (§27.12–27.13)
- Teststrategie dokumentiert (§27.14)
- Batch-/Journal-FK-Policy: keine Umschreibung
- **Dry-Run-Bericht** für Zielumgebung reviewed — **keine** Schreibmigration ohne Bericht

**Freigabe durch dieses Dokument (Phase-3-Spezifikation):** Gate B **Spezifikation** — nicht Implementierung.

### Gate C — Zugangsrace und Intake-Persistenz

**Freigabe Intake-RPC-Implementierung wenn:**

- Partielle Unique-/Index-Strategie festgelegt (§25.5)
- Get-or-create atomar spezifiziert (§25.6–25.7)
- Idempotenzvertrag Receipt + Movement dokumentiert (§25.9)
- Transaktionsgrenze und RPC-Empfehlung dokumentiert (§25.10)
- Schutz historischer Daten explizit (§25.11)
- Teststrategie für DB-Harness dokumentiert (§25.15)

**Freigabe durch dieses Dokument (Phase-2-Spezifikation):** Gate C **Spezifikation** — nicht Implementierung.

### Gate D — Startseitenflow

**Freigabe Entfernung Apply in Ausrüstung wenn:** CTA auf Startseite + Produktwahl + voller Mehrflächen-Flow + Fehler/Erfolg + Alt-Route-Redirect für kanonische Items — Detailkriterien §28.17.

### Gate E — Legacy-Entfernung

**Freigabe Löschung alter Pfade wenn:** Migration abgeschlossen; keine produktive Nutzung; Tests grün.

---

## 25. Phase 2 — Persistenzvertrag (Spezifikation)

Dieser Abschnitt ist der **belastbare Persistenzvertrag** vor jeder SQL- oder RPC-Änderung. Er baut auf dem veröffentlichten Domain-Core ([`fertilizerProductStockCore.ts`](../../src/lib/fertilizerProductStockCore.ts), `0d5d529`) auf.

### 25.1 Ziel

Phase 2 implementiert **persistente positive Zugänge** auf kanonischem Produktbestand:

- höchstens **ein aktiver kanonischer Bestand** pro `(user_id, saved_product_profile_id, base_unit)`
- **find-or-create** atomar in der Datenbank
- **eine unveränderliche positive Movement** pro bestätigtem Zugang
- **Idempotenz** gegen Retry/Doppelklick/Netzwerkwiederholung
- **keine** destruktive Änderung historischer Items oder Movements

### 25.2 Aktueller Persistenz-Ist-Stand

#### Tabellen und Beziehungen

| Tabelle | Rolle heute | Phase-2-Relevanz |
|---------|-------------|------------------|
| `public.fertilizer_containers` | Inventory Item (Legacy-Name „Gebinde“) | Wird **weiterverwendet** als Item-Zeile; kanonische Produktbestände sind **Zeilen desselben Typs** mit neuer Semantik |
| `public.fertilizer_stock_movements` | Append-only Ledger (`container_id` → Item) | **Unveränderte Wahrheit** — Saldo = Summe wirksamer Movements |
| `public.product_profiles` | Saved Product Profile (`profile_status = saved`, immutable) | Versionsanker; Form↔Unit per Trigger |
| `public.fertilizer_inventory_creation_receipts` | Idempotenz für **Packungs-Creation** | Bleibt für Alt-Pfad; **nicht** für Produkt-Intake wiederverwenden |
| `public.fertilizer_application_batches` | Düngungs-Batch + Idempotenz | Unverändert (Phase 2+) |
| `public.fertilizer_capture_receipts` | Legacy-Capture | Unverändert |

**Foreign Keys:** `fertilizer_containers.saved_product_profile_id` → `product_profiles.id`; Movements → Container; Application-Batches → Container.

#### Spalten Inventory Item (`fertilizer_containers`)

| Spalte | Ist | Kanonischer Produktbestand |
|--------|-----|----------------------------|
| `user_id` | ✅ | ✅ Schlüsselbestandteil |
| `saved_product_profile_id` | ✅ Core-Pfad | ✅ Schlüsselbestandteil |
| `base_unit` | ✅ `kg`/`ml` | ✅ Schlüsselbestandteil |
| `access_kind` | ✅ | `authenticated_user` für persistierten Nutzerbestand |
| `package_size_value` / `package_size_unit` | ✅ nominale Packung | **Keine Identität** — bei kanonischem Item **NULL** (empfohlen) |
| `archived_at` | ✅ Soft-Archive | Aus **aktivem** Unique ausgeschlossen |
| `product_id`, `recognition_candidate_id` | Legacy | Nicht auf kanonischem Core-Pfad |
| `stock_kind` | ❌ fehlt | **Neu (additiv):** `product_stock` vs. Legacy |

**Kein** persistiertes `current_quantity` / `currentQuantity` auf Item-Ebene (bestätigt in Migrationen und [DL-025](../decisions/dl-025.md)).

#### Spalten Movement (`fertilizer_stock_movements`)

| Spalte | Ist | Intake-Vertrag |
|--------|-----|----------------|
| `quantity_delta` | signed numeric(18,4) | **Strikt positiv** für Zugang |
| `unit` | text / `kg`/`ml` Core | Muss Item-`base_unit` entsprechen |
| `movement_type` | Enum inkl. positiver Reasons | `initial_stock`, `purchase`, `gift_received` |
| `movement_at` | timestamptz (Core) | **Pflicht** für wirksamen Bestand |
| `movement_date` | date (Legacy) | Parallel gesetzt aus `movement_at` |
| `inventory_idempotency_key` | text | **Movement-Ebene** Idempotenz (bestehend) |
| `capture_idempotency_key` | Legacy | Nicht für Core-Intake |
| `application_batch_id` | Düngung | Nicht für Intake |

**Wirksamer Bestand:** Summe `quantity_delta` WHERE `movement_at IS NOT NULL` (RPC `append_fertilizer_inventory_core_movement`, Repository-Reads). Legacy-Zeilen ohne `movement_at` zählen **nicht** zum Core-Saldo.

#### RPCs und Mechanismen heute

| RPC / Mechanismus | Verhalten | Abweichung von DL-033 |
|-------------------|-----------|------------------------|
| `create_fertilizer_inventory_core_from_confirmed_packages` | Schleife: **1 Item + 1 Movement pro Packung** | Mehrere Items pro Produkt möglich |
| `append_fertilizer_inventory_core_movement` | `SELECT … FOR UPDATE` Item; Saldo-Check; Idempotenz auf `inventory_idempotency_key` | Wiederverwendbar für Intake-Movement |
| Creation Receipt | Advisory Lock + `(scope, idempotency_key)` Unique + Payload-Fingerprint | Muster für Intake-Receipt |

#### Indizes / Constraints heute

- **Kein** Unique auf `(user_id, saved_product_profile_id, base_unit)`
- Nicht-Unique: `fertilizer_containers_auth_saved_profile_idx (user_id, saved_product_profile_id)`
- Movement-Idempotenz: Unique `(user_id, inventory_idempotency_key)` WHERE authenticated + key gesetzt
- Immutability: Trigger verbietet UPDATE/DELETE auf Movements

#### Bestandsberechnung heute

- **DB:** `sum(quantity_delta) WHERE container_id = … AND movement_at IS NOT NULL`
- **Client:** `computeInventoryItemBalance` über Movement-Liste; Repository filtert `movement_at IS NOT NULL`
- **Kein** `currentQuantity`-Feld als Source of Truth

### 25.3 Kanonische Persistenzidentität

Persistiert identisch zum Domain-Core (Phase 1):

```
(user_id, saved_product_profile_id, base_unit)
```

| Regel | Persistenz |
|-------|------------|
| Saved Product Profile | FK + Trigger `profile_status = saved`; **Versionsanker** |
| Product Form | **Nicht** im Unique-Key; abgeleitet aus Profil (Trigger `validate_fertilizer_container_base_unit`) |
| Unterschiedliche Profile | **Getrennte** Items — keine Zusammenführung |
| `kg` vs. `ml` | **Getrennte** Items — keine Umrechnung |
| Package Size | **Nicht** im Schlüssel; optional NULL auf kanonischem Item |
| Gebinde-/Container-ID | PK `id` ist Laufzeit-Referenz, **keine** fachliche Packungsidentität |

### 25.4 Aktiver kanonischer Bestand

**Definition „aktiv“ (technisch, für Unique-Partial-Index):**

```text
access_kind = 'authenticated_user'
AND user_id IS NOT NULL
AND saved_product_profile_id IS NOT NULL
AND base_unit IS NOT NULL
AND archived_at IS NULL
AND stock_kind = 'product_stock'   -- neue Spalte, siehe §25.5
```

**Session-Scope** (`access_kind = session`): eigener Bestand mit `session_access_hash` — **nicht** Teil des authenticated Unique-Vertrags; separater partieller Index `(session_access_hash, saved_product_profile_id, base_unit)` optional analog.

**Legacy-Gebinde-Items** (bestehende Creation-Schleife): `stock_kind IS NULL` oder `stock_kind = 'physical_package'` — **ausgenommen** vom kanonischen Unique, bleiben lesbar bis Phase 3.

### 25.5 Unique-/Index-Strategie

#### Option A — Sofortiger vollständiger Unique Constraint

```sql
-- konzeptionell
UNIQUE (user_id, saved_product_profile_id, base_unit)
```

| Pro | Contra |
|-----|--------|
| Einfachste Semantik | **Scheitert**, sobald ≥2 aktive Core-Items denselben Schlüssel haben (heute durch Creation-Schleife **wahrscheinlich**) |
| Kein Zusatzfeld nötig | Blockiert Phase-2-Implementierung ohne vorherige Datenbereinigung |

**Bewertung:** Nur zulässig bei **garantiert konfliktfreien** Bestandsdaten — im Repository **nicht** als Startannahme.

#### Option B — Partieller Unique Index auf aktive kanonische Items ✅ **Empfohlen**

```sql
-- konzeptionell (Implementierung Phase 2)
CREATE UNIQUE INDEX … ON fertilizer_containers (user_id, saved_product_profile_id, base_unit)
WHERE access_kind = 'authenticated_user'
  AND saved_product_profile_id IS NOT NULL
  AND base_unit IS NOT NULL
  AND archived_at IS NULL
  AND stock_kind = 'product_stock';
```

| Pro | Contra |
|-----|--------|
| Legacy-Duplikate und Gebinde-Items **koexistieren** | Erfordert additive Spalte `stock_kind` |
| Phase 2 kann **sofort** neue kanonische Items schützen | Read-Seite muss Duplikate bis Phase 3 aggregieren/filtern |
| Archivierte Items ausgenommen | Zwei Semantiken auf einer Tabelle dokumentiert halten |

**Bewertung:** **Bevorzugt** — erfüllt Schutz neuer Schreibpfade ohne Phase-3-Vorarbeit.

#### Option C — Constraint erst nach Datenmigration (Phase 3)

| Pro | Contra |
|-----|--------|
| Kein Risiko durch Alt-Duplikate beim Constraint | **Kein** DB-Backstop für parallele Erst-Zugänge in Phase 2 |
| Einfacher Index nach Bereinigung | Race-Schutz nur über Anwendungslogik — **unzureichend** |

**Bewertung:** Als **alleinige** Phase-2-Strategie **verworfen**. Option C bleibt für **Verschärfung** nach Phase 3 (z. B. Entfernen `stock_kind`-Filter wenn nur noch kanonische aktive Zeilen existieren).

#### Empfehlung

**Option B** in Phase 2 implementieren. Option A erst prüfen, wenn Phase-3-Migrationsreport **null** Konflikte auf aktiven kanonischen Zeilen zeigt.

**Additive Spalte:**

| Spalte | Typ | Werte | Default |
|--------|-----|-------|---------|
| `stock_kind` | `text` CHECK | `product_stock`, `physical_package` | `NULL` (= historisches/legacy Item) |

Neue Intake-RPC setzt `stock_kind = 'product_stock'`, `package_size_* = NULL`.

**Voraussichtliche Constraint-Verletzer heute (Analyse, keine DB-Abfrage in diesem Schritt):**

- Mehrere `fertilizer_containers` mit gleichem `(user_id, saved_product_profile_id, base_unit)`, Core-gebunden, nicht archiviert — **erzeugt durch** `create_fertilizer_inventory_core_from_confirmed_packages`
- Legacy-Items ohne `saved_product_profile_id` — **nicht** im Partial Index
- Session-Items — separater Scope

### 25.6 Get-or-Create-Vertrag

**Fachlicher Ablauf** (serverseitig, eine Transaktion):

1. **Domain-Validierung** — spiegelt `validateFertilizerProductStockIntake` (Menge, Reason, IDs); Profil laden (`saved`, Form↔Unit).
2. **Kanonische Identität** — `(user_id, saved_product_profile_id, base_unit)`.
3. **Idempotenz-Receipt** — siehe §25.9; bei Replay → gespeichertes Ergebnis.
4. **Get-or-create Item:**
   - `INSERT INTO fertilizer_containers (…, stock_kind = 'product_stock')`
   - `ON CONFLICT` auf partiellem Unique **DO NOTHING**
   - `SELECT … FROM fertilizer_containers WHERE <Schlüssel> AND <aktiv-Kriterien> FOR UPDATE`
5. **Positive Movement** — über `append_fertilizer_inventory_core_movement` **oder** äquivalentes Inline-Insert mit denselben Checks (Saldo, Unit, Präzision, Immutability).
6. **Ergebnis** — `{ inventoryItemId, movementId, stockIdentity, quantityDelta, reason }` im Receipt persistieren.

**Regeln:**

- Ein Kauf erzeugt **kein** neues Item, wenn kanonisches Item existiert — nur Movement.
- Mehrere Käufe → mehrere Movements, **ein** Item.
- Kein clientseitiges „SELECT dann INSERT“ ohne DB-Schutz.

### 25.7 Race Conditions

| Szenario | Schutz |
|----------|--------|
| Zwei parallele **Erst**-Zugänge desselben Schlüssels | Partieller **Unique Index** + `INSERT … ON CONFLICT DO NOTHING` + anschließendes `SELECT … FOR UPDATE` |
| Parallele Zugänge auf **bestehendem** Item | `FOR UPDATE` auf Item-Zeile (bestehend in `append_*`) |
| Parallele **Idempotenz**-Retries | Receipt Unique + Advisory Lock (Muster Creation-RPC) + Movement Unique auf `inventory_idempotency_key` |
| Unique Violation nach Concurrent Insert | Catch → Re-SELECT Item → Movement append (Idempotenz-Key verhindert Doppel-Movement) |

**Warum clientseitiges Find-then-Insert nicht reicht:** Zwei Clients können beide „nicht gefunden“ lesen und zwei Items einfügen — nur ein **datenbankseitiger** Unique-Backstop verhindert das.

**Locks:** Keine manuelle Advisory-Lock-Pflicht für Get-or-Create **zusätzlich** zum Unique, sofern Transaktion + `FOR UPDATE` auf dem gefundenen Item. **Empfohlen:** Advisory Lock auf `(user_id, idempotency_key)` für Receipt-Serialisierung (bewährt in `create_fertilizer_inventory_core_from_confirmed_packages`).

### 25.8 Positive Zugangsbuchung (Persistenz)

#### Unterstützte Reasons (Domain = DB-Enum heute)

| Domain (Phase 1) | Persistiertes `movement_type` |
|------------------|-------------------------------|
| `initial_stock` | `initial_stock` |
| `purchase` | `purchase` |
| `gift_received` | `gift_received` |

Negative Domain-Namen ([DL-034](../decisions/dl-034.md) Ziel: `application`, `gift_given`, `disposed`) bleiben in Phase 2 **unimplementiert**; DB nutzt weiter `fertilization`, `gifted_away`, `disposal` — **keine Enum-Migration in Phase 2**.

#### Eigenschaften

| Anforderung | Persistenz |
|-------------|------------|
| Strikt positive Menge | RPC-Check `quantity_delta > 0` (analog append) |
| Max. 4 Nachkommastellen | DB + Domain; **keine** Rundung |
| Keine kg↔ml-Umrechnung | Item-Trigger + Movement-Unit-Check |
| Kein Restmengen-Update | Kein UPDATE auf Item-Menge |
| Kein Gebinde/FIFO | Kein `package_size` als Bestand; kein FIFO-Metadatum |
| Eine Movement pro Zugang | 1 Receipt → 1 Movement (Idempotenz) |

#### Spalten-Wiederverwendung

| Spalte | Phase 2 |
|--------|---------|
| `fertilizer_containers.user_id`, `saved_product_profile_id`, `base_unit`, `access_kind` | ✅ weiterverwenden |
| `fertilizer_containers.package_size_*` | Legacy/Gebinde; kanonisch **NULL** |
| `fertilizer_stock_movements.quantity_delta`, `unit`, `movement_type`, `movement_at` | ✅ weiterverwenden |
| `inventory_idempotency_key` | ✅ Movement-Idempotenz |
| `source_event_ref`, `note` | optional |
| **Neu:** `stock_kind` auf Container | ✅ kanonische Markierung |
| **Neu:** `fertilizer_product_stock_intake_receipts` | ✅ Request-Idempotenz |

#### Überholt (fachlich), vorerst beibehalten

- Creation-Schleife „1 Packung = 1 Item“ — Alt-RPC bleibt bis Phase 6/7
- `package_size_*` auf Legacy-Items
- Enum-Namen `fertilization` / `gifted_away` / `disposal`

#### Direction-Spalte

**Nicht erforderlich** — Vorzeichen in `quantity_delta` genügt; Zugangs-RPC akzeptiert nur positive Werte.

#### Zeitstempel

| Feld | Vertrag |
|------|---------|
| `movement_at` | **Pflicht** bei Core-Movements; Default `now()` UTC |
| `created_at` | Insert-Zeitpunkt |
| `movement_date` | Abgeleitet aus `movement_at` (Legacy-Kompatibilität) |

### 25.9 Idempotenzvertrag

#### Schichten

| Ebene | Mechanismus | Scope |
|-------|-------------|-------|
| **Request / Operation** | Neue Tabelle `fertilizer_product_stock_intake_receipts` | `(user_id, idempotency_key)` authenticated |
| **Movement** | `inventory_idempotency_key` Unique (bestehend) | `(user_id, inventory_idempotency_key)` |
| **Payload-Integrität** | `payload_fingerprint` SHA-256 auf kanonischem JSON | pro Receipt |

#### Client-Key

- Vom Client, Conversation-Layer oder Command-Handler erzeugt
- **Nicht** nutzerübergreifend wiederverwendbar
- Empfohlenes Prefix: `product-stock-intake:` + opaque Token
- Max. Länge analog Creation (256)

#### Verhalten

| Fall | Ergebnis |
|------|----------|
| Gleicher Key + gleicher Payload (Fingerprint) | **Replay:** gespeichertes `result_jsonb` zurückgeben; **keine** zweite Movement |
| Gleicher Key + abweichender Payload | **Konflikt:** `INVENTORY_INTAKE_IDEMPOTENCY_CONFLICT` (maschinenlesbar) |
| Verschiedene Keys, gleicher Zugang | **Zwei** Movements (zwei bewusste Buchungen) |

#### Movement-Key-Ableitung

Deterministisch aus Receipt: z. B. `product-stock-intake:{receipt_id}` — verhindert Doppel-Insert innerhalb derselben Operation auch bei partieller Retry.

#### Einheitlicher Vertrag

Onboarding (`initial_stock`), Startseiten-Conversation und Ausrüstungs-Intake **müssen denselben RPC** und dieselbe Receipt-Semantik nutzen — keine UI-only-Idempotenz.

### 25.10 Transaktionsgrenze

**Atomare Einheit (eine PostgreSQL-Transaktion):**

1. Receipt anlegen oder sperren (`FOR UPDATE`)
2. Payload-Fingerprint prüfen
3. Get-or-create kanonisches Item
4. Idempotenz auf Movement-Ebene prüfen
5. Positive Movement einfügen
6. Receipt `result_jsonb` + `completed_at` setzen
7. Commit

**Empfehlung:** **Eine SECURITY DEFINER RPC** `record_fertilizer_product_stock_intake` — analog Creation-RPC, intern ggf. `append_fertilizer_inventory_core_movement` aufrufen.

| Mechanismus | Rolle |
|-------------|-------|
| RPC | Einziger Schreib-Einstieg für Produkt-Intake |
| Transaktion | Alles oder nichts |
| `ON CONFLICT` | Get-or-create Item |
| Partieller Unique | Race-Backstop |
| `FOR UPDATE` | Item-Zeile für Movement-Serialisierung |
| Advisory Lock | Receipt-Scope Serialisierung |
| **Kein** separater Client-Multi-Step | Verboten |

**Locks:** Row-Lock via `FOR UPDATE` ausreichend; **keine** zusätzliche manuelle Lock-Hierarchie ohne Messbedarf.

### 25.11 Bestehende historische Daten — Schutz in Phase 2

Phase 2 **darf nicht:**

| Verbot | Begründung |
|--------|------------|
| Alte Items löschen | Historische FKs (`application_batches`, Journal) |
| Movements umhängen | Ledger immutable ([DL-019](../decisions/dl-019.md)) |
| Mengen zusammenführen | Phase 3 |
| Profile automatisch vereinigen | DL-018 |
| Movement Reasons umbenennen | Enum-Migration Phase 6+ |
| `currentQuantity` übernehmen | Nicht vorhanden / widerspricht DL-025 |
| Legacy-RPC entfernen | Kompatibilität |

**Read-Pfade** dürfen weiter alle Items listen; UI-Aggregation erst Phase 4.

### 25.12 Kompatibilitätsrisiken und Phasenzuordnung

| Konflikt | Beschreibung | Phase |
|----------|--------------|-------|
| **K-1** Mehrere Items pro Schlüssel | Creation-Schleife bei Mehrfachkauf | Phase 3 Konsolidierung |
| **K-2** Legacy ohne Saved Profile | Nicht application-eligible | Phase 3 / Legacy-Upgrade |
| **K-3** `package_size` auf Core-Items | Gebinde-Semantik in Daten | Phase 3 archivieren/supersede |
| **K-4** Saldo-Abweichung Movement-Summe vs. UI-Annahme | Kein `currentQuantity` — Rechenweg prüfen | Phase 4 Read-Model |
| **K-5** Movements ohne `movement_at` | Nicht im Core-Saldo | Phase 3 Legacy-Upgrade ([DL-028](../decisions/dl-028.md)) |
| **K-6** Enum `fertilization` vs. Domain `application` | Benennungslücke | Phase 6 |
| **K-7** Parallele Alt- und Neu-RPC | Doppelte Items wenn Alt-Pfad weiter genutzt | Phase 6 Routing; Phase 7 Entfernung |
| **K-8** Session- vs. User-Bestand | Getrennte Scopes | Phase 2 Spec: authenticated zuerst |
| **K-9** Archivierte Items mit Saldo | Unique ausgeschlossen | Phase 3 |
| **K-10** Inkompatible Form/Unit in Alt-Daten | Trigger verhindert Neues; Alt bleibt | Manuell / Phase 3 |

### 25.13 Nicht Teil von Phase 2

- Migration/Konsolidierung historischer Duplikate (Phase 3)
- UI, Read-Aggregation, Equipment (Phase 4)
- Startseiten-Flow (Phase 5)
- Capture/Creation-Umstellung (Phase 6)
- Negative Bewegungen / Düngungs-RPC-Änderung
- Enum-Umbenennung
- Legacy-RPC-Entfernung (Phase 7)
- FIFO, Gebinde-UI, `currentQuantity`-Spalte

### 25.14 Spätere Implementierungsschritte (nach dieser Spezifikation)

1. Migration: `stock_kind`; partieller Unique; Receipt-Tabelle; RLS/Grants
2. RPC `record_fertilizer_product_stock_intake` + interne Hilfsfunktionen
3. TypeScript: `fertilizerProductStockIntakeRpcCore.ts`, Repository-Anbindung
4. Dev-DB Harness-Tests (Gate C)
5. **Kein** produktiver Client-Umschalter bis Harness grün

### 25.15 Teststrategie (Implementierung)

#### Unique Identity

- Erster Zugang → genau 1 Item (`stock_kind = product_stock`)
- Zweiter Zugang → **dieselbe** `inventory_item_id`
- Anderer User / Profile / Base Unit → anderes Item

#### Concurrency

- Zwei parallele Erst-Zugänge → 1 Item, 2 Movements (wenn verschiedene Idempotency Keys) bzw. 1 Movement (gleicher Key)
- Kein unkontrollierter Unique-Verstoß nach außen

#### Idempotency

- Gleicher Key + Payload → 1 Movement, identisches Ergebnis
- Gleicher Key + anderer Payload → Konflikt
- Verschiedene Keys → verschiedene Movements

#### Quantity

- Positiv gültig; 0/negativ/NaN/∞ abgelehnt; >4 Dezimalstellen abgelehnt; keine Rundung; keine kg↔ml

#### Compatibility

- Legacy-Items unverändert nach Intake-Test
- Historische Movements unverändert
- Keine Journal-/Batch-FK-Änderung

#### Security

- RLS: Nutzer nur eigener Bestand
- Idempotency Key nicht nutzerübergreifend

### 25.16 Gate-Kriterien — Abschluss Phase 2

**Dokumentation (dieser Commit):**

- [x] Ist-Stand und Zielidentität dokumentiert
- [x] Unique-Strategie mit Empfehlung
- [x] Get-or-create + Race + Idempotenz + Transaktion spezifiziert
- [x] Schutz historischer Daten und Risikomatrix
- [x] Teststrategie und Phasengrenze

**Implementierung (folgender Commit):**

- [ ] Migration additive deploybar
- [ ] RPC + Dev-DB Harness grün
- [ ] Gate C Implementierung freigegeben
- [ ] Kein produktiver UI-Switch

---

## 26. Widerspruchsprüfung (Repository)

| Aussage / Ort | Klassifikation | Anmerkung |
|---------------|----------------|-----------|
| „Ein Inventory Item = physisches Gebinde“ — [DL-024](../decisions/dl-024.md), [DL-025](../decisions/dl-025.md), [GA-014](./ga-014.md), [GA-012](./ga-012.md) FIFO | **Historischer Kontext** | DL-033/DL-024 Header: **abgelöst** für v1 |
| „Kauf erzeugt N Items bei N Packungen“ — Creation-RPC, §4.2 | **Produktiver Ist-Widerspruch** | Phase 2 Intake-RPC behebt **Neuschreibpfad**; Alt-RPC bis Phase 6 |
| „Kein currentQuantity“ — DL-025, DL-033, Core, SQL | **Weiterhin korrekt** | |
| „Client Find-then-Insert“ — nicht dokumentiert als Ziel | **Verworfen für Phase 2** | Explizit RPC-only |
| „Mehrere aktive Items pro Produkt gewollt“ — GA-012 FIFO | **Historisch / ersetzt** | DL-033: genau ein Produktbestand |
| „Retry darf zweite Movement erzeugen“ | **Verworfen** | Receipt + Movement-Idempotenz |
| „Idempotenz nur UI“ — Creation-Muster | **Ersetzt durch DB-Receipt** | Phase 2 übernimmt Muster |
| „Historische Movements umhängen“ — nirgends als Ziel | **Verworfen** | Phase 3: Übernahmebewegung, kein Umhängen (§27.7) |
| „Korrektur-Paare auf Legacy“ — früher §13 | **Ersetzt** | §27.6 Übernahmebewegung |
| „Read-Aggregation dauerhaft statt Buchung“ — §12 Übergang | **Historischer Übergang** | Nur Phase-4-Übergang; Phase 3 bucht |
| „Migration ohne Dry Run“ | **Verworfen** | §27.13 |
| „Zweite Übernahme bei Idempotenz-Replay“ | **Verworfen** | §27.9 |
| „Legacy-Saldo mit `initial_stock`/`inventory_correction` übernehmen“ | **Verworfen** | §27.6; [DL-034](../decisions/dl-034.md) `legacy_balance_migration` |
| „Migration Reason nutzerseitig / im Intake-RPC“ | **Verworfen** | Nur Migrations-RPC |
| „kg↔ml Umrechnung“ | **Verworfen** | Trigger + Domain |
| „Automatische Profilzusammenführung“ | **Verworfen** | DL-018 |
| [DL-030](../decisions/dl-030.md) FIFO-Verweis im Header | **Historischer Kontext** | Body verweist auf DL-033 Produktbestand |
| `fertilizerProductStockCore.ts` | **Weiterhin korrekt** | Phase 1 Vertrag |

Historische Dokumente werden **nicht** gelöscht; Widersprüche werden über DL-033/GA-015 Phase-Zuordnung aufgelöst.

---

## 27. Phase 3 — Legacy-Migration bestehender Bestände (Spezifikation)

Dieser Abschnitt ist der **belastbare Migrationsvertrag** vor jeder schreibenden Datenmigration und vor der Read-Model-Umstellung (Phase 4). Er baut auf Phase 2 (`stock_kind`, Intake-RPC, partieller Unique Index) auf.

### 27.1 Ziel

Phase 3 behandelt **bestehende** Inventory Items **konservativ**, ohne historische Wahrheit zu verfälschen:

1. Legacy-Items **klassifizieren** (A–J)
2. Zukünftige **kanonische Produktbestände** pro Migrationsgruppe bestimmen
3. **Automatisch migrierbare** vs. **blockierte** Gruppen trennen
4. Für migrierbare Gruppen: kanonisches Ziel-Item vorbereiten oder wiederverwenden
5. Wirksamen Legacy-Saldo **einmalig und auditierbar** auf kanonisches Item übertragen
6. Legacy-Items **superseden/archivieren** — ohne Löschen, ohne Movement-Umhängen
7. Historische Application-/Journal-Referenzen **unverändert** lassen
8. Read Models (Phase 4) können danach **nur noch kanonische** aktive Bestände summieren
9. Jede Migration **idempotent, wiederholbar, prüfbar** (Dry Run → Schreiblauf)
10. **Datenverlust** und stille Zusammenführungen verhindern

**Nicht Ziel:** Sofortige UI-Umstellung, Legacy-RPC-Entfernung, Profilzusammenführung, Löschen alter Zeilen.

### 27.2 Ist-Risiken

| Risiko | Quelle (Repository) | Phase-3-Antwort |
|--------|---------------------|-----------------|
| Mehrere aktive Items pro `(user_id, profile, base_unit)` | Creation-Schleife `create_fertilizer_inventory_core_from_confirmed_packages` | Gruppierung + kanonisches Ziel |
| Legacy `stock_kind IS NULL` koexistiert mit `product_stock` | Phase-2-Migration | Klassifikation; Partial Index schützt nur `product_stock` |
| `append_fertilizer_inventory_core_movement` prüft **kein** `archived_at` | `20250805_fertilizer_inventory_core.sql` | Schreibschutz in Migrations-RPC + später Append-Erweiterung (§27.11) |
| Read-Liste summiert **alle** nicht archivierten Container | `fetchFertilizerStockList` | Phase 4: nur kanonische/superseded-filterte Summe |
| Movements ohne `movement_at` zählen nicht zum Core-Saldo | Balance-Core, Append-RPC | Explizit ausgeschlossen bei Saldoermittlung |
| Application-Batches FK → `inventory_item_id` | `20250808`/`20250809` | Historische IDs **nie** umschreiben |
| Parallele Alt- und Neu-RPC | K-7 §25.12 | Migration vor breitem Read-Switch; Schreibschutz auf superseded Legacy |
| Automatische Profilzusammenführung | DL-018 | **Verboten** |

### 27.3 Migrationsgruppe

**Definition (verbindlich):**

```text
(user_id, saved_product_profile_id, base_unit)
```

Gilt für `access_kind = 'authenticated_user'` mit gesetztem `saved_product_profile_id` und `base_unit`.

Eine Gruppe kann enthalten:

- genau ein Legacy-Item (`stock_kind IS NULL` oder `legacy_container`)
- mehrere Legacy-Items desselben Schlüssels
- bereits ein aktives `product_stock`-Item
- Legacy- und Product-Stock-Items **gemeinsam**
- archivierte Items (werden bei Klassifikation gesondert behandelt)
- Items ohne wirksame Movements
- widersprüchliche Daten (→ blockiert)

**Verboten als Gruppierungskriterium:**

| Kriterium | Warum verboten |
|-----------|----------------|
| Produktname / Hersteller / ähnliche Schreibweise | Kein Versionsanker ([DL-018](../decisions/dl-018.md)) |
| Package Size | Metadatum, keine Identität ([DL-028](../decisions/dl-028.md)) |
| NPK / Produktfamilie / Heuristik | Falsche Zusammenführung |
| Automatische Profilähnlichkeit | Zwei Saved Profiles = zwei Bestände |

**Session-Scope** (`access_kind = session`): **separater** Migrationspfad — nicht Teil des authenticated Produktbestands in Phase 3 v1.

### 27.4 Klassifikation A–J

| Klasse | Merkmale | Automatisch migrierbar? | Behandlung |
|--------|----------|-------------------------|------------|
| **A** | `stock_kind = 'product_stock'`, aktiv, Schlüssel eindeutig | Nein (bereits kanonisch) | Zielbestand; ggf. Legacy-Gruppe darauf migrieren (Klasse D) |
| **B** | Genau **ein** Legacy-Item; Saved Profile + Base Unit; Form/Unit konsistent; keine konkurrierenden kanonischen Items; wirksame Movements konsistent | **Ja** | Kanonisches Item anlegen/verwenden; bei Saldo > 0: **eine** `legacy_balance_migration`-Bewegung; Legacy supersede |
| **C** | Mehrere Legacy-Items; gleicher Nutzer/Profil/Base Unit; Form/Unit konsistent; Movement-Units konsistent | **Ja** | Ein kanonisches Ziel; **Summe** wirksamer Legacy-Salden → **eine** `legacy_balance_migration`-Bewegung; alle Legacy supersede |
| **D** | Aktives `product_stock`-Item **plus** Legacy-Items gleichen Schlüssels | **Ja** (mit Vorsicht) | Vorhandenes kanonisches Item = Ziel; Legacy-Saldo idempotent per `legacy_balance_migration` übernehmen |
| **E** | Fehlendes `saved_product_profile_id` oder Legacy ohne Core-Bindung | **Nein** | Bleibt Legacy; ggf. separater Pfad `fertilizerInventoryLegacyMigrationCore` / manuelle Profilzuordnung |
| **F** | Fehlende oder widersprüchliche `base_unit` | **Nein** | Keine kg↔ml-Umrechnung; Konfliktbericht |
| **G** | `product_form` widerspricht `base_unit` (Granulat≠kg, Flüssig≠ml) | **Nein** | Konfliktbericht; keine stille Korrektur |
| **H** | Widersprüchliche Movement-Daten (Units, ungültige Mengen, verwaiste Refs, negativer wirksamer Saldo) | **Nein** | Konfliktbericht; manuelle Klärung |
| **I** | Bereits archiviertes / fachlich abgeschlossenes Item | **Nein** (nicht reaktivieren) | Historisch erhalten; nur für Nachvollziehbarkeit |
| **J** | Wirksamer Saldo = 0; keine offene fachliche Relevanz | **Bedingt** | Supersede/Archivierung ohne Übernahmebewegung; **keine** leere kanonische Hülle nur „weil Duplikat existierte“ |

**Automatisch migrierbar:** B, C, D (nach Dry-Run-Validierung). **Blockiert:** E, F, G, H, I (Migration), J (nur Archivierung ohne Saldo).

### 27.5 Kanonisches Ziel-Item

**Ziel-Identität:** dieselbe wie Phase 2 — `(user_id, saved_product_profile_id, base_unit)` mit `stock_kind = 'product_stock'`, `archived_at IS NULL`, `access_kind = 'authenticated_user'`.

**Auswahlregel:**

1. Falls **aktives** `product_stock`-Item existiert → **dieses** Item (Klasse A/D)
2. Sonst: **neues** kanonisches Item via get-or-create (gleiches Muster wie Intake-RPC, **ohne** fachlichen Zugangsgrund zu simulieren — technische Anlage im Migrations-RPC)
3. **Niemals** zwei aktive kanonische Items pro Gruppe — Partial Unique Index ist Backstop

**Legacy-Items:** `stock_kind` **nicht** nachträglich auf `product_stock` setzen. Sie bleiben `NULL`/`legacy_container` bis archiviert.

### 27.6 Saldoübernahme

**Verbindlicher Vertrag:**

| Regel | Detail |
|-------|--------|
| Historische Movements | **Unverändert** am Legacy-`container_id` |
| Kanonisches Item | Erhält **keine** rückwirkend umgehängten Movements |
| Wirksamer Saldo | Summe `quantity_delta` WHERE `movement_at IS NOT NULL` pro Legacy-Item; Gruppensaldo = Summe über Legacy-Items der Gruppe |
| Übernahme | **Genau eine** positive `legacy_balance_migration`-Bewegung pro migrierter Gruppe mit Saldo > 0 ([DL-034](../decisions/dl-034.md)) |
| Audit | Mapping-Receipt verknüpft Gruppe, Legacy-IDs, Saldo, Stichtag, Übernahme-Movement-ID, Fingerprint |
| Idempotenz | Wiederholung liefert dasselbe Ergebnis — **keine** zweite Übernahme |
| `movement_at IS NULL` | **Nicht** in Saldo; ggf. separater Legacy-Upgrade-Pfad ([DL-028](../decisions/dl-028.md), K-5) — nicht still in Übernahme einrechnen |
| Nullsaldo | **Keine** Übernahmebewegung; nur Supersede/Archivierung |
| Negativer Saldo | **Keine** automatische Migration (Klasse H); manuelle Klärung |
| Stichtag | `migration_cutoff_at timestamptz` — Saldo zum Stichtag; Movements danach → Konflikt / erneute Analyse |
| Nachträgliche Legacy-Movements während Lauf | Migrations-RPC muss Gruppe sperren; nach Abschluss Legacy-Schreibschutz (§27.11) |

**Movement Reason — verbindlich entschieden (OD-M1 ✅):**

Der interne technische Movement Reason lautet **`legacy_balance_migration`** ([DL-034](../decisions/dl-034.md)).

| Aspekt | Vertrag |
|--------|---------|
| Bedeutung | Einmalige Übernahme eines geprüften **positiven** Legacy-Saldos auf kanonischen Produktbestand |
| Zulässig durch | Spätere Migrations-RPC / serverseitiger Migrationsprozess **nur** |
| Verboten | Onboarding, Startseite, Ausrüstung, normaler Intake-RPC, Append ohne Migrationskontext |
| Nicht verwenden | `initial_stock`, `purchase`, `gift_received`, `inventory_correction` — kein reales Ereignis, keine Korrektur |
| Mengenvertrag | Menge **> 0**; Base Unit = Gruppe; max. 4 Dezimalstellen; keine Rundung; keine kg↔ml |
| Nullsaldo | **Keine** `legacy_balance_migration`-Bewegung |
| Negativer Saldo | **Blockiert** — Klasse H; keine negative Übernahme |
| Receipt | **Zwingend** — isolierte Bewegung ohne Migration Receipt unzulässig |
| Persistenz | Enum/Constraint/RPC-Unterstützung folgen in Phase-3-**Implementierung**; Intake-RPC lehnt Reason weiterhin ab |
| Anzeige (später) | Sinngemäß „Übernommener früherer Bestand“ — Darstellungsvertrag only |

**Nutzer-Sichtbarkeit:** Übernahmebewegung **nicht** als Kauf/Düngung darstellen; Journal/Startseite ignorieren technische Migrationsbewegungen (Phase 4/6).

**Verworfen:** Korrektur-Paare auf Legacy-Items (früher §13-Entwurf); Read-Aggregation ohne Buchung als dauerhafte Lösung.

### 27.7 Keine historische Movement-Umschreibung

| Verbot | Begründung |
|--------|------------|
| `UPDATE fertilizer_stock_movements SET container_id = …` | Ledger immutable ([DL-019](../decisions/dl-019.md)); Trigger |
| DELETE Movements | Immutability |
| Neuberechnung historischer `quantity_delta` | Audit-Verlust |
| Umschreiben von `application_batch_id` / Journal-FKs | Historische Wahrheit |

Historische Detailansichten (Journal, Batch-Detail) zeigen weiterhin das **ursprüngliche** Item und dessen Movements.

### 27.8 Mapping-/Supersede-Strategie

#### Bewertete Optionen

| Option | Pro | Contra |
|--------|-----|--------|
| **A** — nur `archived_at` | Minimal | Keine Zielbeziehung; Doppelzählungsrisiko im Read Model; schwaches Audit |
| **B** — `archived_at` + `superseded_by_container_id` | Klare Zielkante; Read-Filter einfach | Kein Idempotenz-Receipt; unvollständiges Audit |
| **C** — Mapping-/Receipt-Tabelle ✅ | Idempotenz, Audit, Dry Run, Rollback-Grundlage; Muster Intake/Creation | Zusätzliche Tabelle |

#### Empfehlung ✅

**Option C + minimale Item-Spalte:**

1. **Neue Tabelle** `fertilizer_product_stock_migration_receipts` (Arbeitstitel) — Request-/Gruppen-Idempotenz, analog Intake-Receipts
2. **Additive Spalte** `superseded_by_container_id uuid NULL` auf `fertilizer_containers` — FK → kanonisches Item, **keine** Zyklen (CHECK `id <> superseded_by_container_id`)
3. **`archived_at`** auf superseded Legacy-Items setzen
4. **`archive_reason`** optional text CHECK — z. B. `product_stock_migration_superseded`

**Mapping-Tabelle — Mindestfelder:**

| Feld | Zweck |
|------|-------|
| `id` (PK) | Receipt-/Operations-ID |
| `user_id` | Owner |
| `migration_group_key` | Deterministischer Schlüssel (Hash aus user + profile + base_unit + Stichtag-Policy) |
| `idempotency_key` | Client-/Job-Key; UNIQUE `(user_id, idempotency_key)` |
| `payload_fingerprint` | SHA-256 kanonisches JSON |
| `saved_product_profile_id`, `base_unit` | Gruppe |
| `legacy_container_ids` | jsonb/array — sortierte UUIDs |
| `canonical_container_id` | Ziel-Item |
| `takeover_movement_id` | NULL wenn Saldo 0; sonst FK → Movement mit `movement_type = 'legacy_balance_migration'` |
| `effective_balance` | Übernommener Saldo (4 Dezimalstellen) |
| `migration_cutoff_at` | Stichtag |
| `status` | `pending` / `completed` / `conflict` |
| `result_jsonb` | Replay-Ergebnis |
| `created_at`, `completed_at` | Audit |

RLS: wie Intake-Receipts — **kein** direkter Client-Schreibzugriff.

**`archived_at` allein reicht nicht** — immer in Kombination mit Mapping-Receipt und `superseded_by_container_id`.

### 27.9 Idempotenz und Fingerprint

**Schichten:**

| Ebene | Mechanismus |
|-------|-------------|
| Gruppen-Receipt | `(user_id, idempotency_key)` UNIQUE |
| Movement | `(user_id, inventory_idempotency_key)` — Key deterministisch aus Receipt-ID, z. B. `product-stock-migration:{receipt_id}`; Reason **`legacy_balance_migration`** |
| Advisory Lock | `(user_id, migration_group_key)` — Serialisierung paralleler Läufe |

**Fingerprint-Kanon (SHA-256), mindestens:**

- `userId`
- `savedProductProfileId`
- `baseUnit`
- `legacyContainerIds` (sortiert, stabil)
- `effectiveMovementIds` oder reproduzierbare Movement-Checksum (nur wirksame Movements mit `movement_at`)
- `migrationCutoffAt`
- `computedBalance` (formatiert, 4 Dezimalstellen)
- `movementReason`: `'legacy_balance_migration'` (konstant)
- `canonicalContainerId` (falls bereits vorhanden)

**Verhalten:**

| Fall | Ergebnis |
|------|----------|
| Gleicher Auftrag + unveränderte Quelle | **Replay:** gespeichertes `result_jsonb`; keine zweite Übernahme |
| Gleicher Key + veränderte Quelle | **Konflikt:** `PRODUCT_STOCK_MIGRATION_IDEMPOTENCY_CONFLICT`; erneute Analyse |
| Neue Legacy-Movements nach abgeschlossener Migration | **Kein** Auto-Nachziehen; separates Ereignis oder manueller Re-Run mit neuem Key |

### 27.10 Transaktionsgrenze (atomarer Ablauf)

Pro Migrationsgruppe — **eine PostgreSQL-Transaktion:**

1. Nutzer + Gruppe validieren; Klasse B/C/D bestätigen
2. Advisory Lock + Receipt anlegen oder `FOR UPDATE` auf bestehendem Receipt
3. Fingerprint prüfen
4. Legacy-Items erneut laden; Saldo zum Stichtag berechnen
5. Kanonisches Item finden oder anlegen (`stock_kind = 'product_stock'`)
6. Prüfen: Gruppe bereits `completed` → Replay
7. Falls Saldo > 0: **genau eine** `legacy_balance_migration`-Bewegung auf kanonischem Item
8. Mapping-Receipt `completed`; Legacy-Items: `superseded_by_container_id`, `archived_at`
9. Commit

**Bei Fehler:** vollständiger Rollback — keine archivierten Items ohne Receipt, keine Übernahme ohne Mapping.

### 27.11 Schreibschutz alter Pfade

**Ist-Analyse (Repository):**

| Pfad | Schreibt Legacy? | `archived_at`-Check? |
|------|------------------|----------------------|
| `create_fertilizer_inventory_core_from_confirmed_packages` | Ja — neue Legacy-Items (`stock_kind` NULL) | Nein |
| `record_fertilizer_product_stock_intake` | Nein — nur `product_stock`; lehnt `legacy_balance_migration` ab | — |
| `append_fertilizer_inventory_core_movement` | Ja — beliebiges Item | **Nein** (Stand `20250805`) |
| `apply_fertilizer_inventory_item_to_areas` | Indirekt — negative Movement | Item-Eligibility; kein Supersede-Check |

**Anforderungen vor produktiver Migration:**

1. Migrierte Legacy-Items (`superseded_by_container_id IS NOT NULL` oder Mapping `completed`) dürfen **keine** neuen Movements erhalten
2. Append-RPC (oder Wrapper) muss superseded/archivierte Items **ablehnen** — Implementierung Phase 3/6
3. Creation-RPC darf **keine** neuen Duplikat-Gruppen erzeugen, sobald kanonischer Bestand existiert — Routing Phase 6
4. Application-RPC darf weiter historische Item-IDs lesen; neue Düngungen nur auf **kanonische** Items (Phase 5)

**Reihenfolge:**

```
Dry Run → Schreibmigration Gruppe für Gruppe → Legacy-Schreibschutz aktiv → Read Model Phase 4
```

Ohne Schreibschutz: **kein** breiter Read-Switch.

### 27.12 Konfliktmatrix

| Konflikt | Auto? | Block? | Behandlung | Phase | Audit |
|----------|-------|--------|------------|-------|-------|
| Mehrere Items gleicher Schlüssel | Ja (B/C) | — | Kanonisches Ziel + Übernahme | 3 | Receipt |
| Fehlendes Saved Profile | Nein | Ja | Klasse E; manuell | 3/legacy | Report |
| Fehlende Base Unit | Nein | Ja | Klasse F | 3 | Report |
| Form/Unit-Widerspruch | Nein | Ja | Klasse G | 3 | Report |
| Movement-Unit ≠ Item-Base-Unit | Nein | Ja | Klasse H | 3 | Report |
| `movement_at IS NULL` | Nein | Ja* | Aus Saldo exkl.; Upgrade separat | 3/legacy | Report |
| Negativer Saldo | Nein | Ja | Klasse H; **keine** `legacy_balance_migration` | 3 | Report |
| Nullsaldo | Bedingt | — | Klasse J — Supersede only; **keine** Movement | 3 | Receipt |
| Keine Movements | Bedingt | — | Klasse J | 3 | Receipt |
| Bestehendes kanonisches Item | Ja | — | Klasse D | 3 | Receipt |
| Archiviertes kanonisches Item | Nein | Ja | Neues kanonisches nur wenn Unique erlaubt | 3 | Report |
| Mehrere aktive kanonische (Constraint-Verstoß) | Nein | Ja | Datenfehler; manuell | 3 | Report |
| Legacy bereits archiviert | Nein | — | Klasse I — skip | 3 | Report |
| Verwaiste Movement-Ref | Nein | Ja | Klasse H | 3 | Report |
| Journal/Application-FKs | — | — | **Nicht ändern** | 3 | — |
| Nachträgliche Movements nach Analyse | Nein | Ja | Neuer Dry Run | 3 | Konflikt |
| Doppelte Migration | — | — | Idempotenz-Replay | 3 | Receipt |
| Abgebrochene Teilmigration | — | — | Rollback | 3 | Receipt status |
| Gleicher Produktname, verschiedene Profile | Nein | Ja | Getrennte Gruppen | — | — |
| Gleiche Profile-ID, widersprüchliche Form in DB | Nein | Ja | Datenfehler (Trigger soll verhindern) | 3 | Report |

\* Automatische Migration erst nach Legacy-Upgrade, falls Movements nachgerüstet werden.

### 27.13 Dry Run

**Pflicht vor jeder schreibenden Migration.**

Dry Run = **read-only** Analyse mit identischer Klassifikations- und Saldo-Logik wie Schreiblauf; **keine** INSERT/UPDATE.

**Bericht — Mindestkennzahlen:**

| Kennzahl | Beschreibung |
|----------|--------------|
| `legacy_item_count` | Alle Legacy-Items (authenticated, Core-gebunden) |
| `canonical_item_count` | Aktive `product_stock`-Items |
| `migration_group_count` | Distinct Gruppenschlüssel |
| `auto_migratable_groups` | B + C + D |
| `blocked_groups` | E–H |
| `items_without_saved_profile` | Klasse E |
| `unit_conflicts` | Klasse F/H |
| `form_unit_conflicts` | Klasse G |
| `negative_balances` | Klasse H |
| `zero_balance_groups` | Klasse J |
| `groups_with_existing_canonical` | Klasse D |
| `historical_movement_count` | Gesamt (informativ) |
| `movements_without_movement_at` | K-5 |
| `expected_takeover_movements` | Gruppen mit Saldo > 0 (`legacy_balance_migration`) |
| `expected_superseded_items` | Legacy nach Migration |
| `expected_canonical_creates` | Neue kanonische Items |

**Gate:** Ohne reviewed Dry-Run-Bericht **keine** schreibende Migration in Production.

### 27.14 Teststrategie (Implementierung)

#### Klassifikation

- Jede Klasse A–J mit Fixtures
- Unterschiedliche Saved Profiles → **nie** eine Gruppe
- Package Size / Produktname → **kein** Einfluss auf Gruppierung

#### Saldo

- Nur `movement_at IS NOT NULL`
- Positive, null, negative Salden
- 4 Dezimalstellen; keine Rundung; keine kg↔ml

#### Migration

- Einzelnes Legacy; mehrere Legacy; vorhandenes kanonisches Item
- Genau **eine** `legacy_balance_migration`-Bewegung bei Saldo > 0; **keine** bei Nullsaldo
- Negativer Saldo → Migration blockiert
- Mapping vollständig; Receipt-Verknüpfung zwingend
- Legacy superseded; historische Movements unverändert (byte-identisch)
- Application-Batch-/Journal-FKs unverändert

#### Idempotenz

- Wiederholung → Replay; **keine** zweite `legacy_balance_migration`; veränderte Quelle → Konflikt; Teilfehler → Rollback

#### Concurrency

- Zwei parallele Migrationsversuche → eine erfolgreich; kein Doppel-Movement

#### Legacy-Schreibschutz

- Superseded Item → Append abgelehnt; kanonisches Item weiter beschreibbar

#### Security

- Nur eigene Gruppen; kein fremdes Item/Movement/Receipt

### 27.15 Phasengrenze

#### Phase 3 umfasst (Dokumentation + spätere Implementierung)

- Klassifikation, Dry-Run-Vertrag, Migrationsgruppen
- Mapping-/Supersede-Strategie, Saldoübernahme, Idempotenz, Atomarität
- Konfliktmatrix, Schreibschutzanforderungen, Teststrategie, Gates

#### Phase 3 umfasst **nicht**

- Produktive Massenmigration ohne Gate
- UI / Equipment-Read-Model (Phase 4)
- Startseiten-Flow (Phase 5)
- Creation/Umstellung aller Schreibpfade (Phase 6)
- Movement-Umhängen, Profil-Merge, Item-Löschung, Legacy-RPC-Entfernung (Phase 7)

### 27.16 Klare Architektur-Empfehlung

| # | Frage | Empfehlung |
|---|-------|------------|
| 1 | Historische Movements umhängen? | **Nein** — immutable Ledger |
| 2 | Saldo über neue Movement übernehmen? | **Ja** — genau eine positive Übernahmebewegung pro Gruppe mit Saldo > 0 |
| 3 | Eigener technischer Migration Reason? | **Ja — `legacy_balance_migration`** ([DL-034](../decisions/dl-034.md)); **nicht** `purchase`/`initial_stock`/`inventory_correction` |
| 4 | Mapping-Tabelle? | **Ja** — `fertilizer_product_stock_migration_receipts` |
| 5 | Reicht `archived_at` allein? | **Nein** — plus `superseded_by_container_id` + Receipt |
| 6 | Doppelzählung verhindern? | Read Model summiert **nur** aktive kanonische Items; superseded Legacy ausgeschlossen; Übernahme **einmalig** |
| 7 | Spätes Schreiben auf Legacy? | Append/Creation blockieren superseded/archivierte Legacy-Items |
| 8 | Auto-Migration? | Klassen **B, C, D** nach grünem Dry Run |
| 9 | Blockiert? | **E, F, G, H, I**; **J** nur Archivierung |
| 10 | Gates vor Umsetzung? | Gate B Spec ✅; Dry Run; Dev-DB-Tests; Schreibschutz-Design; **Implementierungsgates §27.17** |

### 27.17 Gate-Kriterien

#### Abschluss Phase 3 (Dokumentation) ✅

- [x] Migrationsgruppe und Klassifikation A–J
- [x] Saldoübernahme ohne Movement-Umhängen
- [x] Movement Reason **`legacy_balance_migration`** verbindlich ([DL-034](../decisions/dl-034.md))
- [x] Mapping-/Supersede-Empfehlung
- [x] Idempotenz, Transaktion, Konfliktmatrix
- [x] Dry Run + Schreibschutz + Teststrategie

#### Freigabe technische Phase-3-Implementierung (noch offen)

- [ ] Persistenz-Enum / CHECK um `legacy_balance_migration` erweitert
- [ ] Reason **nur** in Migrations-RPC zulässig; normaler Intake-RPC lehnt ab
- [ ] Receipt-/Mapping-Verknüpfung zwingend in Migrations-RPC
- [ ] Positiver Saldo → genau eine `legacy_balance_migration` praktisch getestet
- [ ] Nullsaldo → Supersede ohne Movement praktisch getestet
- [ ] Negativer Saldo → blockiert praktisch getestet
- [ ] Idempotenz und Parallelität praktisch getestet
- [ ] Keine doppelte Zählung im Read-Model-Design verifiziert
- [ ] Migration-RPC + Harness + Dev-DB grün
- [ ] Dry-Run-Bericht auf Dev reviewed

### 27.18 Widerspruchsprüfung (Phase 3)

| Aussage | Klassifikation |
|---------|----------------|
| „Movements umhängen“ — §13 früher Korrektur-Paare | **Ersetzt** durch §27.6 Übernahmebewegung |
| „Read-Aggregation reicht dauerhaft“ — §12 Übergang | **Historischer Übergang** Phase 4 only; Phase 3 bucht |
| „Alle Legacy automatisch migrieren“ | **Verworfen** — Klassen E–H blockiert |
| „Produktname gruppiert“ — UX-Prinzipien | **Nicht** für Migration — nur Saved Profile ID |
| „Package Size = Identität“ — DL-028 | **Verworfen** für Gruppierung |
| „Negativer Saldo still übernehmen“ | **Verworfen** |
| „Read summiert Legacy + Kanonisch“ | **Verworfen** nach Migration |
| „Zweite Übernahme bei Replay“ | **Verworfen** — Idempotenz |
| „Migration ohne Dry Run“ | **Verworfen** |
| „Legacy-Saldo via `initial_stock`/`inventory_correction`“ | **Verworfen** — `legacy_balance_migration` |
| „Nullsaldo erzeugt Movement“ | **Verworfen** |
| „Intake-RPC akzeptiert Migration Reason“ | **Verworfen** |
| „Übernahme ohne Receipt“ | **Verworfen** |
| DL-033 „Schema: Gebinde zu Produktbestand zusammenführen“ | **Präzisiert** durch §27 — keine Movement-Umschreibung |

---

## 28. Phase 5 — Startseiten-Düngungsflow (Spezifikation)

Dieser Abschnitt ist der **belastbare Umsetzungsvertrag** für Phase 5. Er baut auf dem veröffentlichten Stand von Phase 1–4 auf ([`fertilizerProductStockCore.ts`](../../src/lib/fertilizerProductStockCore.ts), Intake-/Legacy-Migration-RPCs, [`fertilizerProductStockReadCore.ts`](../../src/lib/fertilizerProductStockReadCore.ts), [`fertilizerApplicationFlowCore.ts`](../../src/lib/fertilizerApplicationFlowCore.ts), [`fertilizerMultiAreaApplication.ts`](../../src/lib/fertilizerMultiAreaApplication.ts)).

**Umsetzungsstatus:** 📋 Spezifikation — **technisch umsetzungsbereit** (keine neue Decision-Log-Datei erforderlich; OD-3 in §28.8 entschieden).

### 28.1 Nutzerproblem und Zweck

| | |
|---|---|
| **Titel** | Startseiten-Düngungsflow |
| **Nutzerproblem** | Der Nutzer kann eine Düngung heute nur aus **Ausrüstung** starten: Route `/ausruestung/duenger/:inventoryItemId/anwenden`, Button „Anwenden“ je Listeneintrag. [DL-033](../decisions/dl-033.md) und [DL-031](../decisions/dl-031.md) verlangen **Startseite als primären Tätigkeitseinstieg** — Produktbestand wählen, dann denselben Mehrflächen-Flow. |
| **Zweck Phase 5** | Sichtbaren Einstieg „Düngung erfassen“ auf der Startseite liefern; **Produktbestand** aus aktivem Read Model wählen; **bestehenden** Mehrflächen-Anwendungsflow wiederverwenden; primären Apply-Einstieg aus Ausrüstung entfernen; Alt-Deep-Link für kanonische Items auf neuen Flow umleiten. |
| **Nicht-Zweck** | Keine Sprach-/NL-Steuerung; kein Capture/Intake; keine neuen Bestands-RPCs; keine Datenmigration; kein E2E-Vollausbau in der Kernphase. |

### 28.2 Ist-Stand nach Phase 4 (Repository-verifiziert)

| Fähigkeit | Stand |
|-----------|-------|
| Produktbestand an Saved Product Profile + Base Unit | ✅ Domain-Core + Unique + Intake-RPC |
| Unveränderliche Movements | ✅ Trigger + append-only Ledger |
| Intake erzeugt/verwendet kanonische `product_stock`-Items | ✅ `record_fertilizer_product_stock_intake` |
| Legacy-Bestände kontrolliert migrierbar | ✅ `migrate_fertilizer_product_stock_group` |
| Aktive Bestandsansicht nur kanonische Items | ✅ `list_active_fertilizer_product_stock`, `get_active_fertilizer_product_stock_item` |
| Superseded/archivierte Legacy ausgeschlossen | ✅ Read-Filter + Core-Mapper |
| Historische Movements/Referenzen erhalten | ✅ Keine Umschreibung |
| Doppelzählung verhindert | ✅ Read nur kanonisch; Legacy-Saldo am Legacy-Container |
| Package Size keine Identität | ✅ Mapper setzt `packageSizeValue/Unit = null` |
| Keine kg↔ml-Umrechnung | ✅ Domain + RPC |
| Unterschiedliche Saved Product Profiles getrennt | ✅ Schlüssel `(user_id, saved_product_profile_id, base_unit)` |
| Düngungs-RPC | ✅ `apply_fertilizer_inventory_item_to_areas` unverändert veröffentlicht |
| Sichtbarer Flow-Einstieg | ❌ noch Ausrüstung + Item-Route; Startseite ohne Düngungs-CTA |
| Apply in Ausrüstung | ❌ `FertilizerCategoryPage` zeigt weiter „Anwenden“ |

### 28.3 Abgrenzung

#### Phase 1–4 (bereits geliefert)

| Phase | Leistung | Phase 5 nutzt |
|-------|----------|---------------|
| 1 | Domain-Schlüssel, Intake-Validierung | Eligibility-Regeln (`balance > 0`, Form/Unit) |
| 2 | Intake-RPC, kanonisches Item | **Kein** neuer Zugang in Phase 5 |
| 3 | Legacy-Migration, Supersede | Item-RPC liefert `null` für superseded Legacy |
| 4 | Aktives Read Model, Ausrüstungsliste | `fetchFertilizerStockList`, `fetchFertilizerStockListItem` |

Phase 5 **schwächt keine** Phase-3-/Phase-4-Verträge ab und führt **keine** zweite Bestandswahrheit ein.

#### Phase 6 (später)

- Capture/Creation-Schreibpfade auf Intake-RPC umstellen
- Kauf, Abgänge, Verschenken, Entsorgung als Movements auf kanonischem Item
- Legacy-Creation-RPC deprecaten (OD-5)
- Onboarding-Initialbestand (optional, sprachgeführt — [CM-014](../playbook/conversation-model.md))

#### Phase 7 (später)

- Alte Route `/ausruestung/duenger/:inventoryItemId/anwenden` entfernen
- `fertilizerApplicationPath` aus Ausrüstung entfernen
- Legacy-Creation-Schleife und Alt-Typen bereinigen
- Gate E

### 28.4 Fachlicher Zielvertrag

| Regel | Vertrag |
|-------|---------|
| Primärer Einstieg | Startseite — deterministischer CTA **„Düngung erfassen“** |
| Produktwahl | Nur **application-eligible** aktive kanonische Product-Stock-Items (`balance > 0`, Saved Profile, `kg`/`ml`) |
| Anwendungslogik | **Ein** Flow, **ein** Mehrflächen-RPC — keine parallele Conversation- oder Bestandslogik |
| Persistenz | **Keine** Schema-Änderung; **keine** neuen Movements außer über bestehenden Application-RPC |
| Ausrüstung | Read-only Bestand; **kein** primärer „Anwenden“-Button nach Gate D |
| Alt-Route | Bleibt bis Phase 7; **Redirect** auf `/duengung/:inventoryItemId` wenn Item im aktiven Read Model |
| Sprache/NL | **Nicht** in Phase 5 — eigener Block nach Gate D ([§11](./ga-015.md#11-startseitenintegration), [home-experience](../playbook/home-experience.md) HE-002) |
| Navigation Bottom-Bar | **Unverändert** — kein neuer Tab |

### 28.5 Nutzerfluss

```
Startseite (/)
  → CTA „Düngung erfassen“
  → /duengung (Produktauswahl)
  → Nutzer wählt einen Produktbestand
  → /duengung/:inventoryItemId (Formular — Flächen, Modus, Menge, Datum, Notiz)
  → Bestätigung (confirm)
  → apply_fertilizer_inventory_item_to_areas
  → Erfolg (success) → Journal/Home
```

**Eingaben (fachlich):**

1. Gewählter Produktbestand (`inventoryItemId` = kanonische Item-ID)
2. Konkrete Flächen (manuell oder über Pflegegruppe — bestehend)
3. Eingabemodus (`rate_per_sqm` | `total_amount_proportional`)
4. Mengeneingabe, Anwendungsdatum, optionale Notiz
5. Idempotency Key (Client — bestehend)

**Ergebnisse (fachlich):**

1. Genau **ein** `fertilizer_application_batches`-Datensatz
2. Genau **eine** negative Movement auf dem gewählten kanonischen Item
3. Je Fläche **eine** Activity + `fertilization_details` (bestehend)
4. Aktualisierter Saldo im Read Model (ableitbar; keine Item-Spalte)

**Leere/fehlerhafte Zustände:**

| Zustand | Verhalten |
|---------|-----------|
| Kein application-eligible Bestand | Produktauswahl zeigt leeren Zustand + Hinweis (kein Capture in Phase 5) |
| Item nicht mehr aktiv (superseded/archiviert) | Item-RPC `null` → Fehlerseite mit Hinweis; kein Submit |
| Unzureichender Saldo beim Submit | RPC `INSUFFICIENT_STOCK` — Eingaben erhalten |
| Idempotenz-Konflikt | Bestehende Fehlerbehandlung |

### 28.6 Wiederverwendung (keine Duplikation)

| Baustein | Wiederverwendung | Nicht duplizieren |
|----------|------------------|-------------------|
| Read Model | `list_active_fertilizer_product_stock`, `get_active_fertilizer_product_stock_item` via `fertilizerInventory.ts` | Keine clientseitige Container-Abfrage; kein paralleles Filtern nach Package Size |
| Application Flow | `fertilizerApplicationFlowCore.ts` (Draft, Validation, Confirmation) | Kein zweiter Formular-Core |
| Submit | `applyFertilizerInventoryItemToAreas` → `apply_fertilizer_inventory_item_to_areas` | Kein Einzelflächen-RPC im sichtbaren Flow |
| Eligibility | `isFertilizerStockListItemApplicationEligible` | Keine abweichende Equipment-Logik |
| Areas/Care Groups | `fetchAreas`, `fetchCareGroupMemberships`, `buildCareGroupSummaries` | — |
| Idempotenz | Batch-Idempotenz im RPC | — |
| Conversation | — | **Keine** `homeVoiceFlow`-Anbindung in Phase 5 |

### 28.7 Neue Logik (minimal, zwingend)

#### Domain-Typen (neu oder erweitert in `fertilizerApplicationFlowCore.ts`)

| Typ | Zweck |
|-----|-------|
| `FertilizerApplicationFlowPhase` | Erweiterung: `'product-select' \| 'form' \| 'confirm' \| 'success'` |
| `FertilizerApplicationProductOption` | Darstellung einer wählbaren Zeile (ID, Label, Balance, Base Unit, Form) |

#### Core-Funktionen (neu)

| Funktion | Vertrag |
|----------|---------|
| `filterApplicationEligibleProductStockItems(items)` | Filtert `inStock`-Liste auf `isFertilizerStockListItemApplicationEligible` |
| `resolveApplicationFlowPhase(hasInventoryItemId, phase)` | Startphase: ohne ID → `product-select`; mit ID → `form` |
| `buildFertilizerApplicationRoute(inventoryItemId?)` | `/duengung` oder `/duengung/:inventoryItemId` |
| `shouldRedirectLegacyApplicationRoute(item)` | `true` wenn aktives Read-Model-Item existiert |
| `formatProductStockSelectionLabel(item)` | Anzeige Produktname + Saldo + Base Unit (max. 4 Nachkommastellen) |

**Keine** neuen Dateien zwingend — Erweiterung des bestehenden Flow-Cores bevorzugt. Optional separater `fertilizerHomeApplicationFlowCore.ts` nur wenn der Flow-Core sonst unübersichtlich wird; **nicht** beides parallel mit divergierender Logik.

#### Adapter

| Datei | Änderung |
|-------|----------|
| `fertilizerRoutes.ts` | Konstanten `homeApplication`, `homeApplicationWithItem(id)`; Redirect-Helfer für Alt-Route |
| `fertilizerInventory.ts` | **Keine** RPC-Änderung — bestehende List/Item-Fetches |

#### UI

| Datei | Änderung |
|-------|----------|
| `HomeScreen.tsx` | Sichtbarer CTA „Düngung erfassen“ → `/duengung` (nur wenn Nutzer eingeloggt und Onboarding abgeschlossen — bestehende Route-Guards) |
| `FertilizerApplicationPage.tsx` | Produktauswahl-Schritt; URL-Parameter optional; Wiederverwendung Formular/Bestätigung/Erfolg; Copy von „Gebinde“ auf „Produktbestand“ wo sichtbar |
| `FertilizerCategoryPage.tsx` | **Entfernen** des Apply-Links und der deaktivierten Apply-Aktion |
| `App.tsx` | Routes `/duengung`, `/duengung/:inventoryItemId`; Redirect-Element für Alt-Route |

#### Persistenz / SQL / Migration

**Keine.** Phase 5 ist rein clientseitige Flow-Umstellung plus Routing.

#### Fehlercodes

Keine neuen RPC-Fehlercodes. Client nutzt bestehende Codes aus `FertilizerMultiAreaApplicationRuntimeError` und Read-Zugriff (`null` = Item nicht gefunden/nicht aktiv).

### 28.8 Routen und Navigation (OD-3)

| Route | Rolle |
|-------|-------|
| `/duengung` | Produktauswahl (parameterlos) |
| `/duengung/:inventoryItemId` | Anwendungsflow ab Formular (nach Wahl oder Deep-Link) |
| `/ausruestung/duenger/:inventoryItemId/anwenden` | **Übergang:** Redirect auf `/duengung/:inventoryItemId` wenn `get_active_fertilizer_product_stock_item` ein Item liefert; sonst bestehende Fehlermeldung |
| `/` | Startseite — CTA-Quelle |

**Begründung OD-3:** Im Repository ist die Startseite **`/`**, nicht `/home/…`. Top-Level-Route `/duengung` ist die kleinste konsistente Ergänzung ohne neues Namespace-Präfix.

**Navigation Bottom-Bar:** unverändert (Journal · Greenkeeper · Ausrüstung). Kein neuer Tab.

### 28.9 Identitäts-, Base-Unit- und Profilregeln

| Regel | Phase 5 |
|-------|---------|
| Aktive Item-Identität | `(user_id, saved_product_profile_id, base_unit)` — nur über Read-RPC |
| Auswahl-Identität | Nutzer wählt **Inventory Item ID** des kanonischen Product-Stock-Items — **nicht** Produktname, Hersteller oder Package Size |
| Base Unit | Nur `kg` oder `ml`; keine Umrechnung |
| Saved Product Profile | Muss `saved` + enrichment + Nutzer-Match sein (Read-RPC-Filter) |
| Legacy/superseded | Erscheinen **nicht** in Produktauswahl; Item-RPC liefert `null` |
| Package Size | **Nicht** in Produktauswahl-Darstellung als Bestand |

### 28.10 Movement-Regeln

Phase 5 erzeugt Movements **ausschließlich** über den bestehenden Application-RPC:

- genau **eine** negative Movement pro bestätigter Anwendung
- Saldo-Prüfung **in der Transaktion** im RPC (bestehend)
- keine neuen Movement Reasons
- keine historische Movement-Umschreibung

### 28.11 Security-Vertrag

| Anforderung | Umsetzung |
|-------------|-----------|
| Nutzerbezug | Supabase-Session; Read-RPCs nutzen `auth.uid()` serverseitig |
| Kein freies `user_id` | Kein Client-Parameter für fremde Bestände |
| Item-Zugriff | Nur Items aus aktivem Read Model; Submit-RPC prüft Ownership und Eligibility |
| RLS | Unverändert — keine Migration |
| Dynamisches SQL | Keine neuen RPCs |

### 28.12 Idempotenz-Vertrag

Unverändert gegenüber [DL-031](../decisions/dl-031.md) und bestehendem Flow:

- Client erzeugt Idempotency Key vor Submit
- Gleicher Key + gleiche Payload → Replay desselben Batches und derselben Movement
- Gleicher Key + andere Payload → Konflikt
- Produktauswahl und Navigation sind **nicht** idempotent persistiert — nur der Submit

### 28.13 Historische Integrität

| Objekt | Phase 5 |
|--------|---------|
| Bestehende Movements | Unverändert |
| Journal-Referenzen | Unverändert |
| Application-Batch-Referenzen | Unverändert |
| Supersede-Beziehungen | Unverändert |
| Legacy-Items | Direkt lesbar; nicht in aktiver Auswahl |
| Alt-Deep-Links | Redirect wenn kanonisch aktiv; sonst Hinweis |

### 28.14 Doppelte Logik verhindern

| Risiko | Gegenmaßnahme |
|--------|---------------|
| Zweite Bestandsliste | Nur `fetchFertilizerStockList()` — keine parallele Abfrage |
| Zweite Eligibility | Nur `isFertilizerStockListItemApplicationEligible` |
| Zweite Conversation Engine | Kein `homeVoiceFlow`-Branch für Düngung in Phase 5 |
| Zweiter Submit-Pfad | Nur `applyFertilizerInventoryItemToAreas` |

### 28.15 Technischer Umsetzungsscope (kleinstes vollständiges Paket)

#### Voraussichtlich geänderte Dateien

| Datei | Art |
|-------|-----|
| `src/pages/HomeScreen.tsx` | UI — CTA |
| `src/pages/HomeScreen.module.css` | UI — CTA-Styling (minimal) |
| `src/pages/FertilizerApplicationPage.tsx` | UI — Produktauswahl + Flow-Einstieg |
| `src/pages/FertilizerApplicationPage.module.css` | UI — Produktauswahl (minimal) |
| `src/pages/FertilizerCategoryPage.tsx` | UI — Apply entfernen |
| `src/pages/FertilizerCategoryPage.module.css` | UI — Apply-Styles entfernen |
| `src/lib/fertilizerApplicationFlowCore.ts` | Domain — Phasen, Filter, Route-Helfer |
| `src/lib/fertilizerApplicationFlowCore.test.ts` | Unit — neue Helfer |
| `src/lib/fertilizerRoutes.ts` | Routes |
| `src/App.tsx` | Routing + Redirect |

#### Voraussichtlich neue Dateien

Keine zwingend. Optional: `src/lib/fertilizerHomeApplicationFlowCore.ts` + Test nur bei klarer Entkopplung — **nicht** parallel zum erweiterten Flow-Core.

#### Unit-Tests (erforderlich)

| Suite | Mindestabdeckung |
|-------|------------------|
| `fertilizerApplicationFlowCore.test.ts` | `filterApplicationEligibleProductStockItems`; Phasenauflösung; Route-Helfer; Eligibility-Grenzfälle (Legacy-Zeilen, `balance <= 0`, fehlendes Profil) |
| Regression | `fertilizerApplicationFlowCore.test.ts` bestehende 31 Tests grün |
| Regression | `fertilizerProductStockReadCore.test.ts` 17/17 grün |
| Regression | `fertilizerMultiAreaApplicationCore.test.ts` / Application-Flow unverändert grün |

#### Datenbanktests (Phase 5)

**Keine neuen DB-Tests** — keine SQL-Änderung. Bestehende Application-DB-Tests bleiben unverändert gültig.

#### Regressionstests (erforderlich)

| Prüfung | Erwartung |
|---------|-----------|
| `npm run build` | Grün |
| Phase-1-Core | 40/40 |
| Phase-2-RPC-Core | 3/3 |
| Phase-3-RPC-Core | 4/4 |
| Phase-4-Read-Core | 17/17 |
| Application-Flow-Core | 31/31 + neue Phase-5-Helfer |

#### E2E

**Nicht** Teil der technischen Kernphase. Optionaler Folgeschritt nach Gate D.

### 28.16 Nicht-Ziele (Phase 5)

- Keine Package- oder Gebindeverwaltung
- Kein FIFO, kein `currentQuantity`
- Keine kg↔ml-Umrechnung
- Keine Gruppierung nach Produktname, Herstellername oder Package Size
- Keine Zusammenführung unterschiedlicher Saved Product Profiles
- Keine historische Movement-Umschreibung
- Keine Änderung bestehender Journal-Referenzen
- Keine Änderung bestehender Application-Referenzen (RPC-Signatur unverändert)
- Keine neue Bottom-Navigation
- Kein E2E-Scope in der Kernphase
- Keine Produktivdatenmigration
- Keine SQL-Migration
- Keine Capture-/Intake-Umstellung (Phase 6)
- Keine Entfernung der Alt-Route (Phase 7 — nur Redirect in Phase 5)
- Keine Sprach-/Mikrofon-Anbindung (`homeVoiceFlow`, `HomeVoiceSection`)
- Kein Entfernen von Gebinde-Metadaten in Ausrüstungsliste (nur Apply-CTA; Gebinde-Text-Bereinigung optional minimal, kein Redesign)

### 28.17 Akzeptanzkriterien (testbar)

#### Fachlicher Kern

| # | Gegeben | Aktion | Erwartet | Ausgeschlossen |
|---|---------|--------|----------|----------------|
| FK-1 | Nutzer mit ≥1 eligible kanonischem Item | Startseite → CTA → Produkt wählen → Flow abschließen | Journal-Einträge + 1 negative Movement | Zweiter Batch |
| FK-2 | Nutzer ohne eligible Items | CTA → Produktauswahl | Leerzustand + Hinweis | Capture-Dialog |
| FK-3 | Superseded Legacy-Item-ID in URL | `/duengung/:id` öffnen | Fehler/Hinweis, kein Submit | Legacy als aktiv behandelt |
| FK-4 | Zwei verschiedene Saved Profiles | Produktauswahl | Zwei getrennte Einträge | Merge nach Name |

#### Persistenz

| # | Gegeben | Aktion | Erwartet | Ausgeschlossen |
|---|---------|--------|----------|----------------|
| P-1 | Erfolgreiche Anwendung | Submit | Genau 1 Batch, 1 Movement | Schema-Änderung |
| P-2 | Replay gleicher Idempotency Key | Submit erneut | Gleiches Ergebnis | Doppel-Abbuchung |

#### Security

| # | Gegeben | Aktion | Erwartet |
|---|---------|--------|----------|
| S-1 | Fremdes Item-ID in URL | Seite laden | Kein Item geladen (`null`) |
| S-2 | Unauthentifizierter Nutzer | `/duengung` | ProtectedRoute-Redirect |

#### Read Model

| # | Gegeben | Aktion | Erwartet |
|---|---------|--------|----------|
| R-1 | Aktive kanonische Items | Produktauswahl | Nur Items aus List-RPC |
| R-2 | Legacy/superseded Item | Produktauswahl | Nicht gelistet |

#### UI-Integration

| # | Gegeben | Aktion | Erwartet |
|---|---------|--------|----------|
| U-1 | Ausrüstung Dünger | Seite öffnen | Kein „Anwenden“-Link |
| U-2 | Kanonisches Item | Alt-Route `/ausruestung/duenger/:id/anwenden` | Redirect `/duengung/:id` |
| U-3 | Startseite | CTA sichtbar | Link nach `/duengung` |

#### Historische Integrität

| # | Gegeben | Aktion | Erwartet |
|---|---------|--------|----------|
| H-1 | Bestehende Journal-Einträge | Phase-5-Deploy | Unverändert lesbar |
| H-2 | Bestehende Application-Batches | Phase-5-Deploy | FKs unverändert |

#### Regression

| # | Prüfung | Erwartet |
|---|---------|----------|
| REG-1 | Build | Grün |
| REG-2 | Application-Flow-Core + Read-Core + Product-Stock-Cores | Grün |

#### Nicht-Ziele

| # | Prüfung | Erwartet |
|---|---------|----------|
| NZ-1 | `supabase/migrations/` Diff | Leer |
| NZ-2 | E2E-Dateien Diff | Leer in Kern-Commit |
| NZ-3 | `homeVoiceFlow.ts` Diff | Leer |

### 28.18 Offene Entscheidungen

| ID | Frage | Status | Blockiert Umsetzung? |
|----|-------|--------|----------------------|
| OD-3 | Exakte Route | ✅ `/duengung` (§28.8) | Nein |
| OD-5 | Deprecation Creation-RPC | Offen — **Phase 6/7** | Nein |
| OD-6 | GA-012 Dokumentanpassung | Offen — Phase 7 | Nein |
| Sprach-Einstieg | NL-Anbindung Startseite | Bewusst **nach** Gate D | Nein |

**Ergebnis:** Phase 5 ist **technisch umsetzungsbereit**. **Keine** neue Decision-Log-Datei erforderlich.

### 28.19 Phasengrenzen und Übergabe

#### Phase 5 liefert vollständig

- Startseiten-CTA „Düngung erfassen“
- Produktauswahl aus aktivem Read Model
- Mehrflächen-Anwendungsflow ab Startseite
- Entfernung Apply-CTA in Ausrüstung
- Redirect Alt-Route für kanonische Items
- Gate-D-Abnahme (§28.17)

#### Übergabekriterien an Phase 6

- [ ] Gate D erfüllt
- [ ] Kein sichtbarer Apply-Pfad in Ausrüstung
- [ ] Neuer Flow nutzt ausschließlich kanonische Item-IDs aus Read Model
- [ ] Application-RPC unverändert produktiv
- [ ] Unit- und Regressionstests grün

#### Phase 6 kann danach

- Intake/Capture auf `record_fertilizer_product_stock_intake` routen
- Abgangs-Movements implementieren
- Legacy-Creation deprecaten

#### Phase 7 kann danach

- Alt-Route entfernen
- Legacy-Typen und `-Path`-Helfer bereinigen
- Gate E

### 28.20 Gate D — Abschlusskriterien

**Gate D ist erfüllt wenn:**

- [ ] CTA auf Startseite führt zu `/duengung`
- [ ] Produktauswahl listet nur application-eligible aktive kanonische Items
- [ ] Vollständiger Mehrflächen-Flow (Formular → Bestätigung → Erfolg) funktioniert ab Startseite
- [ ] Submit nutzt `apply_fertilizer_inventory_item_to_areas` ohne Signaturänderung
- [ ] Apply-Button in `FertilizerCategoryPage` entfernt
- [ ] Alt-Route redirectet kanonische Items auf `/duengung/:inventoryItemId`
- [ ] Fehler- und Erfolgszustände erhalten Eingaben bzw. zeigen Journal-Link
- [ ] Keine Sprach-/NL-Logik eingeführt
- [ ] Build + Unit-/Regressionstests grün
- [ ] Akzeptanzkriterien §28.17 FK-1, U-1, U-2, U-3, NZ-1 erfüllt

### 28.21 Widerspruchsprüfung (Phase 5)

| Prüfpunkt | Ergebnis |
|-----------|----------|
| DL-033 Startseite primär | ✅ CTA + `/duengung` |
| DL-031 ein Flow, ein RPC | ✅ Wiederverwendung |
| Phase-4 Read Model | ✅ Keine zweite Liste |
| Package-Identität | ✅ Ausgeschlossen |
| kg↔ml | ✅ Keine Umrechnung |
| Freies `user_id` | ✅ Keine neuen RPCs |
| Historische Umschreibung | ✅ Keine |
| Conversation-Duplikat | ✅ Kein Voice in Phase 5 |
| CM-015 Scoped vs Open | ✅ Deterministischer CTA, kein NL-Routing |
| Manifest „richtige Funktionen“ | ✅ Löst dokumentiertes Nutzerproblem |

---

## 29. Phase 6 — Kauf, Abgänge und vereinheitlichter Capture-/Intake-Vertrag (Spezifikation)

Dieser Abschnitt ist der **belastbare Umsetzungsvertrag** für Phase 6. Er baut auf dem veröffentlichten Stand von Phase 1–5 auf ([`fertilizerProductStockCore.ts`](../../src/lib/fertilizerProductStockCore.ts), [`fertilizerProductStockIntakeRpcCore.ts`](../../src/lib/fertilizerProductStockIntakeRpcCore.ts), [`fertilizerProductStockReadCore.ts`](../../src/lib/fertilizerProductStockReadCore.ts), [`fertilizerApplicationFlowCore.ts`](../../src/lib/fertilizerApplicationFlowCore.ts), [`fertilizerCaptureInventorySaveCore.ts`](../../src/lib/fertilizerCaptureInventorySaveCore.ts), [`FertilizerCaptureFlow.tsx`](../../src/components/fertilizer/FertilizerCaptureFlow.tsx)).

**Umsetzungsstatus:** ✅ **Abgeschlossen** (`f4f6a66`, `ab667dc`, `4f3254b`) — Phase-7-Bereinigung in §30 spezifiziert.

### 29.1 Nutzerproblem und Zweck

| | |
|---|---|
| **Titel** | Kauf, Abgänge und vereinheitlichter Capture-/Intake-Vertrag |
| **Nutzerproblem** | Bestandszugänge laufen produktiv noch über `create_fertilizer_inventory_core_from_confirmed_packages` und erzeugen **ein Legacy-Item pro Packung** ([`fertilizerCaptureInventorySaveCore.ts`](../../src/lib/fertilizerCaptureInventorySaveCore.ts)). Der kanonische Intake-RPC `record_fertilizer_product_stock_intake` existiert (Phase 2), ist aber **nicht** an Capture gebunden. Reale Abgänge außerhalb der Düngung (`gift_given`, `disposed`) und Korrekturen (`inventory_correction`) sind fachlich in [DL-034](../decisions/dl-034.md) festgelegt, haben aber **keinen** produktiven Outbound-RPC. Onboarding, Startseite und Ausrüstung nutzen damit **nicht** dieselbe Persistenzwahrheit. |
| **Zweck Phase 6** | (1) Positives Capture/Intake auf **einen** kanonischen Produktbestand pro Ereignis routen. (2) Reale Abgänge und Korrekturen als unveränderliche negative bzw. signed Movements auf kanonischen Items persistieren. (3) Strukturierte scoped UI **ausschließlich in Ausrüstung** (Capture, itemgebundener Zugang/Abgang). (4) Creation-RPC produktiv **nicht mehr** für neue Buchungen verwenden (Deprecation — Entfernung Phase 7). |
| **Nicht-Zweck** | Kein Startseiten-CTA „Bestand erfassen“; keine neue Home-Aktionshierarchie; keine Sprach-/NL-Orchestrierung auf der Startseite; kein Onboarding-Voice-Flow (CM-014); keine Enum-Umbenennung in PostgreSQL; keine Legacy-RPC-Löschung; keine Package-/Gebindeverwaltung; kein E2E-Kernscope; keine Journal-UI für reine Bestandsbewegungen. |

### 29.2 Ist-Stand nach Phase 5 (Repository-verifiziert)

| Fähigkeit | Stand |
|-----------|-------|
| Kanonische Produktbestände persistent | ✅ `stock_kind = product_stock'`, Unique `(user_id, saved_product_profile_id, base_unit)` |
| Positive Zugänge per Intake-RPC | ✅ `record_fertilizer_product_stock_intake` — Reasons `initial_stock`, `purchase`, `gift_received` |
| Capture speichert kanonisch | ❌ ruft `createFertilizerInventoryFromCapture` → Creation-RPC, N Items bei N Packungen |
| Runtime-Adapter Intake-RPC | ❌ nur Dev-DB-Harness; kein produktiver TypeScript-Adapter in `fertilizerInventory.ts` |
| Legacy-Migration | ✅ `migrate_fertilizer_product_stock_group`; Reason `legacy_balance_migration` |
| Aktives Read Model | ✅ `list_active_fertilizer_product_stock`, `get_active_fertilizer_product_stock_item` |
| Startseite Düngung | ✅ CTA „Düngung erfassen“ → `/duengung` (`2715b4b`) |
| Ausrüstung | ✅ Bestandsliste read-only; Capture unter `/ausruestung/duenger/erfassen`; kein Apply-CTA |
| Düngungsanwendung | ✅ ausschließlich `apply_fertilizer_inventory_item_to_areas` |
| Reale Abgänge außerhalb Düngung | ❌ kein Outbound-RPC; DB-Enum `gifted_away`, `disposal` vorhanden, ungenutzt produktiv |
| Korrektur | ❌ `inventory_correction` im Enum, kein nutzerseitiger Outbound-Pfad |
| Einheitlicher Capture-/Intake-Vertrag | ❌ Ausrüstung-Capture nutzt noch Creation-RPC statt Intake-RPC |
| Package Size als Identität | ✅ im Read Model ausgeschlossen; Capture-UI sammelt noch Packungsgruppen |
| kg↔ml-Umrechnung | ✅ ausgeschlossen |
| Historische Integrität | ✅ Movements unverändert; Journal-/Application-Referenzen unverändert |

### 29.3 Abgrenzung

#### Phase 1–5 (bereits geliefert)

| Phase | Leistung | Phase 6 nutzt |
|-------|----------|---------------|
| 1 | Domain-Schlüssel, positive Intake-Validierung | `validateFertilizerProductStockIntake`, Identitätsregeln |
| 2 | Intake-RPC, Receipt-Idempotenz | **Wiederverwendung** — kein zweiter Intake-RPC |
| 3 | Legacy-Migration, Supersede | Outbound nur auf **aktiven** kanonischen Items |
| 4 | Read Model, Ausrüstungsliste | Saldo-Anzeige nach Zugang/Abgang |
| 5 | Startseiten-Düngungsflow | `application` **ausschließlich** über Application-RPC — nicht duplizieren |

Phase 6 **schwächt keine** Phase-3-/Phase-4-/Phase-5-Verträge ab.

#### Phase 6 (dieser Vertrag)

- Capture-Speichern → `record_fertilizer_product_stock_intake` (summierte Menge, ein Item)
- Neuer Outbound-RPC für `gift_given`, `disposed`, `inventory_correction`
- Strukturierte scoped UI: **Ausrüstung → Dünger → Erfassen** (alleiniger produktiver Capture-Einstieg) sowie itemgebundene Zugang-/Abgangaktionen
- Creation-RPC produktiv deprecaten (OD-5)
- **Keine** Startseiten-Änderung für Bestandsaufnahme — Phase-5-CTA „Düngung erfassen“ bleibt unverändert

#### Phase 7 (später)

- Entfernung `create_fertilizer_inventory_core_from_confirmed_packages`
- Entfernung Alt-Apply-Route, Legacy-Typen, `-Path`-Helfer
- PostgreSQL-Enum-Ausrichtung `fertilization` → `application`, `gifted_away` → `gift_given`, `disposal` → `disposed` (optional alias-frei)
- GA-012 Mehrgebinde-Dokumentanpassung (OD-6)
- Gate E

#### Bewusst außerhalb Phase 6 (parallel oder Gate F)

- Onboarding-Voice `initial_stock` ([CM-014](../playbook/conversation-model.md#cm-014--sprachgeführter-initialbestand-im-onboarding)) — **gleicher RPC-Vertrag**, eigene UI-Phase
- Startseiten-Open-Conversation-Routing für Kauf/Geschenk/Abgang ([CM-015](../playbook/conversation-model.md#cm-015--offene-unterhaltung-und-geführte-erfassung), [home-experience](../playbook/home-experience.md) HE-002) — **nach** Gate F

### 29.4 Fachlicher Zielvertrag

| Regel | Vertrag |
|-------|---------|
| Single Source of Truth | Summe wirksamer Movements auf kanonischem `product_stock`-Item — kein `currentQuantity` |
| Identität | `(user_id, saved_product_profile_id, base_unit)` — Saved Product Profile ist Versionsanker |
| Positives Ereignis | Genau **eine** positive Movement über Intake-RPC; find-or-create kanonisches Item |
| Negatives Ereignis (ohne Fläche) | Genau **eine** signed Movement über Outbound-RPC; **kein** neues Item |
| Düngung | **Unverändert** Application-RPC — Domain-Reason `application`, persistiert als `fertilization` |
| Korrektur | Signed Movement `inventory_correction` auf **bestehendem** Item — Gegenbewegung, keine Mutation |
| Capture | Erkennung/Enrichment getrennt; Persistenz erst nach bestätigter Menge + Reason |
| Parallele Logik | **Verboten** — kein UI-Schreiben auf Movements/Receipts; kein zweiter Intake-Pfad |
| Sprache | Phase 6: scoped Formulare/Diktat **ohne** Conversation Engine; Open Conversation **nach** Gate F |
| Startseite | **Unverändert** — offene Unterhaltung + bestehender CTA „Düngung erfassen“ (Phase 5); **kein** zusätzlicher Bestands-CTA |
| Navigation | **Keine** neue Bottom-Navigation; **keine** neue Hauptnavigation |

### 29.5 Nutzerflüsse (Phase 6 — implementiert)

#### Fluss A — Neuer Bestand (Capture, Ausrüstung)

```
/ausruestung/duenger/erfassen (bestehend)
  → FertilizerCaptureFlow (Erkennung, Profil, Menge, Reason)
  → Bestätigung
  → record_fertilizer_product_stock_intake
  → Erfolg → Ausrüstungsliste (aktualisierter Saldo)
```

#### Fluss B — Zugang auf bestehendem Produkt (Ausrüstung)

```
/ausruestung/duenger
  → Zeile wählen → „Zugang buchen“
  → /ausruestung/duenger/:inventoryItemId/zugang (neu, scoped)
  → Reason purchase | gift_received | initial_stock (nur wenn Item neu angelegt werden würde — sonst purchase/gift_received)
  → Menge + Base Unit (readonly vom Item)
  → record_fertilizer_product_stock_intake
  → Erfolg
```

#### Fluss C — Abgang oder Korrektur (Ausrüstung)

```
/ausruestung/duenger
  → Zeile wählen → „Abgang buchen“ | „Bestand korrigieren“
  → /ausruestung/duenger/:inventoryItemId/abgang (neu, scoped)
  → Reason gift_given | disposed | inventory_correction
  → Menge (positiv eingegeben; RPC speichert signed delta)
  → record_fertilizer_product_stock_outbound
  → Erfolg (Saldo aktualisiert; bei INSUFFICIENT_STOCK Fehler, Eingaben erhalten)
```

**Nicht in Phase 6:** Startseiten-CTA „Bestand erfassen“; Startseiten-Spracheingabe „Ich habe Dünger gekauft“ → NL-Routing (Gate F); zusätzliche deterministische Home-Aktionen neben „Düngung erfassen“.

### 29.5.1 Entry-Point-Vertrag (Phase 6)

#### Enthalten

- **Ausrüstung → Dünger → Erfassen** — alleiniger produktiver Capture-Einstieg
- bestehender scoped Capture-Pfad `/ausruestung/duenger/erfassen`
- produktiver Zugang über bestehenden `FertilizerCaptureFlow` + Intake-RPC
- scoped itemgebundene Zugangs-/Abgangsaktionen innerhalb Ausrüstung (Fluss B/C)
- bestehender Intake-RPC für Zugänge
- neuer Outbound-RPC für Abgänge und Korrekturen

#### Nicht enthalten

- Startseiten-CTA „Bestand erfassen“
- neue Home-Aktionshierarchie
- zweite gleichrangige Home-Aktion neben „Düngung erfassen“
- Startseiten-Natural-Language-Routing für Bestandsbuchung
- Onboarding-Voice-Flow (CM-014)
- globale Sprachsteuerung
- neue Hauptnavigation
- Bottom-Navigation-Änderung

Ausrüstung bleibt der fachlich eindeutige Bestandsbereich für Capture, Zugang und Abgang in Phase 6. Eine spätere zusätzliche Startseitenaktion für Bestandsaufnahme erfordert eine **bewusste Home-Hierarchie-Entscheidung außerhalb** des Phase-6-Kernscopes (Gate F oder Folgephase).

### 29.6 Capture-/Intake-Vertrag (gemeinsam für alle Einstiege)

| Regel | Vertrag |
|-------|---------|
| Persistenz-RPC | **Ausschließlich** `record_fertilizer_product_stock_intake` für positive Zugänge |
| Reasons | `initial_stock`, `purchase`, `gift_received` |
| Product Profile | Muss `saved` sein; Form↔Unit-Kompatibilität serverseitig |
| Menge | Strikt positiv; max. 4 Nachkommastellen; Base Unit `kg` oder `ml` |
| Package Size | Nur Eingabehilfe in Capture-UI; **Summe** der bestätigten Mengen → **eine** Intake-Menge; **nicht** persistiert als Identität |
| Packungsanzahl | UI darf mehrere Packungen erfassen; Mapper `buildProductStockIntakeQuantityFromCaptureDraft` summiert zu **einer** Menge |
| Idempotenz | Client-Key + Receipt-Fingerprint (bestehend Phase 2); Movement-Key deterministisch aus Receipt |
| Enrichment | Vor Intake unverändert (`startFertilizerEnrichmentFromCapture`, `saveFertilizerProductProfileFromCapture`) |
| Erkennung | Erzeugt/verknüpft Saved Product Profile — **keine** zweite Produkterkennung im Intake-RPC |
| Direktes Schreiben | **Verboten** auf `fertilizer_containers`, `fertilizer_stock_movements`, Receipt-Tabellen |
| Wiederholung | Gleicher Key + gleicher Payload → Replay; kein zweites Item, keine zweite Movement |

**Einstiege und Phase-6-UI:**

| Einstieg | Phase-6-Implementierung | Später (Gate F / CM-014) |
|----------|-------------------------|---------------------------|
| Ausrüstung → Dünger → Erfassen | ✅ Fluss A — **alleiniger produktiver Capture-Einstieg** | — |
| Ausrüstung → Zugang/Abgang | ✅ Fluss B/C | — |
| Startseite Bestandsaufnahme | **Nicht** in Phase 6 | NL-Orchestrierung / optionaler Home-CTA nach separater Entscheidung |
| Onboarding | Vertrag gilt; **keine** UI | CM-014 Voice |

### 29.7 Bestandsereignisse und Movement-Vertrag

#### Domain-Reasons (TypeScript / Fachvertrag — [DL-034](../decisions/dl-034.md))

| Reason | Vorzeichen | Nutzerseitig | Persistiert als `movement_type` (Ist-Enum) | RPC |
|--------|------------|--------------|---------------------------------------------|-----|
| `initial_stock` | + | Ja | `initial_stock` | Intake |
| `purchase` | + | Ja | `purchase` | Intake |
| `gift_received` | + | Ja | `gift_received` | Intake |
| `application` | − | Ja (Startseite Phase 5) | `fertilization` | Application (**nicht** Outbound) |
| `gift_given` | − | Ja | `gifted_away` | Outbound |
| `disposed` | − | Ja | `disposal` | Outbound |
| `inventory_correction` | ± | Ja | `inventory_correction` | Outbound |
| `legacy_balance_migration` | + | **Nein** | `legacy_balance_migration` | Migration-RPC only |

**Enum-Ausrichtung:** Domain-Namen in TypeScript folgen DL-034. PostgreSQL behält Ist-Bezeichner bis Phase 7; Outbound-RPC mappt intern (`gift_given` → `gifted_away`, `disposed` → `disposal`). **Keine** Enum-Migration in Phase 6.

#### Abgrenzungen (verbindlich)

| Reason | Verwenden für | Nicht verwenden für |
|--------|---------------|---------------------|
| `initial_stock` | Erstmalige Erfassung bereits vorhandenen Bestands | Kauf, Geschenk, Korrektur, Migration |
| `purchase` | Realer Kaufzugang | Initialbestand, Korrektur |
| `gift_received` | Realer Geschenkzugang | Kauf, Korrektur |
| `gift_given` | Verschenken/Weitergeben | Korrektur, Entsorgung, Düngung |
| `disposed` | Entsorgung/Verlust ohne Fläche | Korrektur, Düngung |
| `inventory_correction` | Falscher Saldo in Greenkeeper | Reale Abgänge, Initialbestand, Migration |
| `legacy_balance_migration` | Legacy-Saldoübernahme Phase 3 | Jeder nutzerseitige Flow |

#### Validierung je Reason

| Reason | Item muss existieren | Find-or-create | Bestandsprüfung | Nullmenge |
|--------|---------------------|----------------|-----------------|-----------|
| `initial_stock` | Nein | Ja | Nein | Abgelehnt |
| `purchase` | Nein | Ja | Nein | Abgelehnt |
| `gift_received` | Nein | Ja | Nein | Abgelehnt |
| `gift_given` | Ja (kanonisch aktiv) | Nein | Ja — Saldo ≥ Menge | Abgelehnt |
| `disposed` | Ja | Nein | Ja | Abgelehnt |
| `inventory_correction` | Ja | Nein | Ja bei negativem Delta | Abgelehnt |
| `application` | Ja | Nein | Ja (Application-RPC) | — |

#### UI-Labels (Deutsch)

| Reason | Label |
|--------|-------|
| `initial_stock` | Bereits vorhanden |
| `purchase` | Gekauft |
| `gift_received` | Geschenkt erhalten |
| `gift_given` | Verschenkt / abgegeben |
| `disposed` | Entsorgt / verloren |
| `inventory_correction` | Bestand korrigieren |

Keine feineren Abgangsgründe (`damaged`, `expired`, `lost`) in Phase 6 — [DL-034](../decisions/dl-034.md) §19.

### 29.8 Wiederverwendung (keine Duplikation)

| Baustein | Wiederverwendung | Nicht duplizieren |
|----------|------------------|-------------------|
| Positive Persistenz | `record_fertilizer_product_stock_intake`, `fertilizerProductStockIntakeRpcCore.ts` | Kein zweiter Intake-RPC |
| Read Model | `fetchFertilizerStockList`, `fetchFertilizerStockListItem` | Keine clientseitige Saldo-Berechnung |
| Capture UI | `FertilizerCaptureFlow`, Enrichment/Profil-Pipeline | Kein paralleles Capture-Formular |
| Application | `apply_fertilizer_inventory_item_to_areas` | Kein Outbound für Düngung |
| Domain Intake | `fertilizerProductStockCore.ts` | Keine zweite Positive-Validierung |
| Idempotenz Intake | Receipt + Movement-Key aus Phase 2 | — |
| Conversation | — | Keine `homeVoiceFlow`-Persistenz |

### 29.9 Neue Logik (minimal, zwingend)

#### Domain — Erweiterung [`fertilizerProductStockCore.ts`](../../src/lib/fertilizerProductStockCore.ts) oder neue Datei [`fertilizerProductStockOutboundCore.ts`](../../src/lib/fertilizerProductStockOutboundCore.ts)

| Typ / Funktion | Zweck |
|----------------|-------|
| `FertilizerProductStockOutboundReason` | `'gift_given' \| 'disposed' \| 'inventory_correction'` |
| `validateFertilizerProductStockOutbound(input)` | Menge > 0, Reason, signed `quantityDelta`, Item-Kontext |
| `mapOutboundReasonToMovementType(reason)` | Domain → DB-Enum |
| `buildProductStockIntakeQuantityFromCaptureDraft(draft)` | Summe bestätigter Capture-Mengen → eine Intake-Menge |
| `resolveStockEventFlowPhase(...)` in [`fertilizerStockEventFlowCore.ts`](../../src/lib/fertilizerStockEventFlowCore.ts) | Phasen `product-select` (optional) \| `reason` \| `quantity` \| `confirm` \| `success` für Zugang/Abgang |

#### Persistenz — neu

| Artefakt | Vertrag |
|----------|---------|
| Migration (additiv) | Tabelle `fertilizer_product_stock_outbound_receipts`; RPC `record_fertilizer_product_stock_outbound` |
| Outbound-RPC Input | `p_inventory_item_id`, `p_quantity` (positiv), `p_reason`, `p_idempotency_key`, optional `p_note`, `p_movement_at` |
| Outbound-RPC Intern | Item `FOR UPDATE`; Saldo prüfen bei negativem Delta; **eine** Movement; Receipt |
| Korrektur | `inventory_correction`: signed delta = Vorzeichen aus UI (Mehrbestand +, Wenigerbestand −) |
| Schreibschutz | Superseded/archivierte/legacy (`stock_kind IS NULL`) Items **ablehnen** |

#### Runtime-Adapter — neu [`fertilizerProductStockIntake.ts`](../../src/lib/fertilizerProductStockIntake.ts)

| Funktion | Vertrag |
|----------|---------|
| `recordFertilizerProductStockIntake(params)` | Ruft Intake-RPC via Supabase; mappt Fehler |
| `recordFertilizerProductStockOutbound(params)` | Ruft Outbound-RPC via Supabase; mappt Fehler |

#### Capture — Änderung [`fertilizerCaptureInventorySaveCore.ts`](../../src/lib/fertilizerCaptureInventorySaveCore.ts)

| Änderung | Vertrag |
|----------|---------|
| Ersetze `createFertilizerInventoryFromCapture` | Durch `recordFertilizerProductStockIntake` nach Profil-Save |
| Ergebnis-Mapping | Ein `inventoryItemId`, eine Movement, `totalInitialQuantity` = Intake-Menge |
| Legacy Creation | **Nicht mehr** für neue Capture-Saves aufrufen |

#### Routes — Erweiterung [`fertilizerRoutes.ts`](../../src/lib/fertilizerRoutes.ts)

| Pfad | Zweck |
|------|-------|
| `/ausruestung/duenger/erfassen` | Unverändert (Capture) |
| `/ausruestung/duenger/:inventoryItemId/zugang` | Scoped Zugang |
| `/ausruestung/duenger/:inventoryItemId/abgang` | Scoped Abgang/Korrektur |

Alle über `ProtectedRoute`. Keine Hardcodierung außerhalb `fertilizerRoutes.ts` + `App.tsx`.

### 29.10 Security-Vertrag

| Regel | Vertrag |
|-------|---------|
| Nutzerbezug | Ausschließlich `auth.uid()` in RPCs — kein `p_user_id` |
| Items | Nur eigene aktive kanonische `product_stock`-Items |
| Profile | Nur eigene Saved Product Profiles |
| Schreiben | Kein Client-INSERT/UPDATE auf Movements, Containers, Receipts |
| RPC | `SECURITY DEFINER`, `search_path = public`, minimale Grants |
| RLS | Receipt-Tabellen service_role only; RPC prüft Ownership |
| Legacy | Kein Intake/Outbound auf superseded/archivierten oder `stock_kind IS NULL` Items |
| Fremde IDs | Item-RPC/Outbound → `null` oder `ACCESS_DENIED` — kein optimistisches Buchen |

### 29.11 Idempotenz-Vertrag

| Vorgang | Mechanismus |
|---------|-------------|
| Intake | Bestehend: `(user_id, idempotency_key)` auf `fertilizer_product_stock_intake_receipts` + Movement-Key `product-stock-intake:{receipt_id}` |
| Outbound | Neu: `(user_id, idempotency_key)` auf `fertilizer_product_stock_outbound_receipts` + Movement-Key `product-stock-outbound:{receipt_id}` |
| Replay | Gleicher Nutzer + Key + Fingerprint → identisches Ergebnis, keine zweite Movement |
| Konflikt | Gleicher Key + anderer Payload → `INVENTORY_OUTBOUND_IDEMPOTENCY_CONFLICT` / Intake-Analog |
| Nutzerübergreifend | Gleicher Key bei verschiedenen Nutzern **erlaubt** |
| Application | Unverändert Batch-Idempotenz — **kein** Ersatz |

Client-Key-Empfehlung (entscheidet OD-P6-2): Prefix `product-stock-intake:` bzw. `product-stock-outbound:` + opaque Token; max. 256 Zeichen; pro bestätigter Buchung neu.

### 29.12 Historische Integrität

| Regel | Vertrag |
|-------|---------|
| Bestehende Movements | Unverändert — kein UPDATE/DELETE |
| Korrekturen | Nur Gegenbewegungen |
| Journal-/Application-FKs | Unverändert |
| Legacy-Items | Historisch lesbar; nicht reaktivierbar |
| Phase-3-Migration | Unverändert |
| Phase-4-Read-Model | Saldo nach Buchung ableitbar; Filter unverändert |
| Phase-5-Düngung | Unverändert |

### 29.13 Sprache, Diktat und Conversation

| Enthalten in Phase 6 | Nicht enthalten |
|----------------------|-----------------|
| Scoped Formularfelder mit optionaler Diktierhilfe (Browser-API → Textfeld) in Ausrüstung | Open Conversation auf Startseite für Bestandsbuchung |
| Bestätigung vor Persistenz in Capture/Zugang/Abgang | Automatische Buchung aus Transkript |
| — | Startseiten-CTA „Bestand erfassen“ |
| — | CM-014 Onboarding-Voice |
| — | Zweite Conversation Engine |
| — | Globale Mikrofonsteuerung |
| — | `homeVoiceFlow`-Anbindung an Intake/Outbound |

**Home-/Conversation-Vertrag (unverändert):** Startseite bleibt offene Unterhaltung mit Greenkeeper; bestehender CTA „Düngung erfassen“ (Phase 5) bleibt der **einzige** deterministische Home-CTA; Phase 6 ergänzt **keinen** weiteren deterministischen CTA auf der Startseite.

**Gate F (nach Phase 6):** Sprach-/NL-Orchestrierung auf Startseite und Onboarding darf **denselben** Intake-/Outbound-RPC und Flow-Core nutzen — siehe §22 Empfohlene Reihenfolge. Ein zusätzlicher Startseiten-CTA für Bestand erfordert dabei eine **separate** Home-Hierarchie-Entscheidung.

### 29.14 Technischer Umsetzungsscope (kleinstes vollständiges Paket)

#### Additive Migration

| Datei | Inhalt |
|-------|--------|
| `supabase/migrations/20250813_fertilizer_product_stock_outbound.sql` | Receipt-Tabelle; RPC `record_fertilizer_product_stock_outbound`; Grants/RLS |

**Keine** Änderung an `20250810_fertilizer_product_stock_intake.sql`.

#### Neue Dateien

| Datei | Zweck |
|-------|-------|
| `src/lib/fertilizerProductStockIntake.ts` | Produktiver Intake-Adapter |
| `src/lib/fertilizerProductStockOutboundCore.ts` | Outbound-Domain |
| `src/lib/fertilizerProductStockOutboundCore.test.ts` | Unit |
| `src/lib/fertilizerProductStockOutboundRpcCore.ts` | RPC-Parameter, Fehlercodes, Mapping |
| `src/lib/fertilizerProductStockOutboundRpcCore.test.ts` | Unit |
| `src/lib/fertilizerStockEventFlowCore.ts` | Flow-Phasen Zugang/Abgang |
| `src/lib/fertilizerStockEventFlowCore.test.ts` | Unit |
| `src/lib/fertilizerProductStockIntakeFromCaptureCore.ts` | Capture → Intake-Menge |
| `src/lib/fertilizerProductStockIntakeFromCaptureCore.test.ts` | Unit |
| `src/lib/fertilizerProductStockOutboundDatabase.test.ts` | DB (Outbound) |
| `src/pages/FertilizerStockIntakePage.tsx` | Scoped Zugang |
| `src/pages/FertilizerStockOutboundPage.tsx` | Scoped Abgang/Korrektur |

#### Geänderte Dateien

| Datei | Änderung |
|-------|----------|
| `src/lib/fertilizerCaptureInventorySaveCore.ts` | Intake statt Creation |
| `src/lib/fertilizerCaptureInventorySaveCore.test.ts` | Mock Intake |
| `src/lib/fertilizerRoutes.ts` | Zugang/Abgang-Pfade |
| `src/App.tsx` | Routes + ProtectedRoute |
| `src/pages/FertilizerCategoryPage.tsx` | Aktionen Zugang/Abgang; Link Erfassen |
| `src/components/fertilizer/FertilizerCaptureFlow.tsx` | Erfolgsnavigation Read Model |
| `src/pages/FertilizerCapturePage.tsx` | Unveränderte Route; Intake-Persistenz |

#### Unverändert produktiv (Deprecation only)

| Datei | Phase 6 | Phase 7 |
|-------|---------|---------|
| `fertilizerInventoryCreation*.ts` | Deprecation-Kommentar; keine neuen Aufrufe | Entfernung |
| `create_fertilizer_inventory_core_from_confirmed_packages` | Nicht für neue Saves | DROP |

#### Fehlercodes Outbound (maschinenlesbar)

`INVENTORY_OUTBOUND_ACCESS_DENIED`, `INVENTORY_OUTBOUND_ITEM_NOT_FOUND`, `INVENTORY_OUTBOUND_ITEM_INACTIVE`, `INVENTORY_OUTBOUND_QUANTITY_INVALID`, `INVENTORY_OUTBOUND_REASON_INVALID`, `INVENTORY_OUTBOUND_INSUFFICIENT_STOCK`, `INVENTORY_OUTBOUND_IDEMPOTENCY_INVALID`, `INVENTORY_OUTBOUND_IDEMPOTENCY_CONFLICT`, `INVENTORY_OUTBOUND_FAILED`

### 29.15 Nicht-Ziele

- Package-/Gebindeverwaltung, FIFO, `currentQuantity`, kg↔ml-Umrechnung
- Gruppierung nach Produktname, Herstellername oder Package Size
- Zusammenführung unterschiedlicher Saved Product Profiles
- Historische Movement-Umschreibung; Journal-/Application-Referenz-Änderung
- Zweite Bestandswahrheit; zweite Intake-Persistenz; zweite Conversation Engine
- Globale Mikrofonlogik; unbestätigte Sprachbuchung
- Startseiten-CTA „Bestand erfassen“; zweite gleichrangige Home-Aktion neben „Düngung erfassen“
- Änderungen an `HomeScreen.tsx` / Home-CSS für Bestandsaufnahme
- Neue Bottom-/Hauptnavigation; Phase-7-Bereinigung; produktive Massenmigration
- E2E-Browsertests im Kern-Commit; Enum-Rename in PostgreSQL
- `sale` als Reason; feinere Abgangsgründe ohne DL-Entscheidung
- Journal-Einträge für reine Bestandsbewegungen
- Sichtbarkeits-UI für Bewegungshistorie (offene Produktfrage DL-034 §20 — nicht blockierend)

### 29.16 Akzeptanzkriterien (testbar)

#### Fachlicher Kern

| # | Gegeben | Aktion | Erwartet | Ausgeschlossen |
|---|---------|--------|----------|----------------|
| FK-1 | Nutzer mit kompatiblem Profil | Capture → purchase 5 kg | +5 kg auf kanonischem Item | Zweites Item bei zweitem Packungseintrag |
| FK-2 | Bestehendes Item 10 kg | Zugang purchase 3 kg | 13 kg; eine Movement | Creation-RPC |
| FK-3 | Item 5 kg | Abgang gift_given 2 kg | 3 kg | inventory_correction |
| FK-4 | Item 5 kg | disposed 5 kg | 0 kg | Teilbuchung |
| FK-5 | Saldo 15 kg, tatsächlich 12 kg | Korrektur −3 kg | 12 kg | disposed |
| FK-6 | Verschüttet 1 kg | disposed 1 kg | −1 kg Movement | inventory_correction |

#### Capture/Intake

| # | Gegeben | Aktion | Erwartet | Ausgeschlossen |
|---|---------|--------|----------|----------------|
| CI-1 | Capture mit 2×5 kg + Rest | Speichern purchase | Intake 10+ Rest **eine** Summe, **ein** Item | N Creation-Items |
| CI-2 | Gleicher Idempotency-Key | Replay | Gleiches Ergebnis | Doppel-Movement |
| CI-3 | initial_stock Reason | Intake | Positive Movement initial_stock | purchase |

#### Persistenz

| # | Gegeben | Aktion | Erwartet | Ausgeschlossen |
|---|---------|--------|----------|----------------|
| P-1 | Erfolgreicher Outbound | Submit | Genau 1 Movement | Schema-Änderung an Intake |
| P-2 | Replay Outbound-Key | Submit erneut | Gleiches Ergebnis | Doppel-Abbuchung |

#### Security

| # | Gegeben | Aktion | Erwartet |
|---|---------|--------|----------|
| S-1 | Fremde Item-ID | Outbound | Abgelehnt |
| S-2 | Superseded Item | Intake/Outbound | Abgelehnt |
| S-3 | Unauthentifiziert | Scoped Route | ProtectedRoute-Redirect |

#### Idempotenz

| # | Gegeben | Aktion | Erwartet |
|---|---------|--------|----------|
| I-1 | Gleicher Key + Payload Intake | Zweimal | 1 Movement |
| I-2 | Gleicher Key + anderer Payload | Zweimal | Konflikt |

#### UI-Integration

| # | Gegeben | Aktion | Erwartet |
|---|---------|--------|----------|
| U-1 | Ausrüstung Dünger | Liste | Aktionen Zugang/Abgang sichtbar |
| U-2 | Ausrüstung → Dünger → Erfassen | Capture starten | Route `/ausruestung/duenger/erfassen`; Intake-RPC |
| U-3 | Startseite Phase 5 | CTA „Düngung erfassen“ | Unverändert `/duengung`; **kein** Bestands-CTA |

#### Sprache/Diktat

| # | Gegeben | Aktion | Erwartet |
|---|---------|--------|----------|
| V-1 | Scoped Formular | Diktat ins Feld | Text im Feld; **keine** Auto-Speicherung |
| V-2 | Startseite Voice | „Gekauft“ | **Keine** Buchung in Phase 6 |

#### Historische Integrität / Regression

| # | Prüfung | Erwartet |
|---|---------|----------|
| H-1 | Bestehende Application-Batches | Unverändert |
| R-1 | Phase 1–5 Unit-Cores | Grün |
| R-2 | Intake-DB-Tests Phase 2 | Grün |
| NZ-1 | `homeVoiceFlow.ts` Diff im Kern-Commit | Leer |

### 29.17 Testvertrag

#### Unit (neu/erweitert)

- Outbound Reason-/Vorzeichenvalidierung; INSUFFICIENT_STOCK-Fälle
- Base-Unit- und Profil-Identitätsregeln
- `buildProductStockIntakeQuantityFromCaptureDraft` — Summe, keine Packungs-Identität
- `fertilizerStockEventFlowCore` — Phasen, Reason-Gates
- Route-Helfer Zugang/Abgang
- Idempotenz-Payload-Mapping Outbound
- Keine kg↔ml-Umrechnung; Korrektur als signed Gegenbewegung

#### Datenbank (Outbound-Migration)

- Positives Intake unverändert (Regression)
- Negatives Outbound; Korrektur ±
- Nullmenge abgelehnt; falsches Reason abgelehnt
- INSUFFICIENT_STOCK; archiviert/superseded/fremd abgelehnt
- Idempotenz Replay/Konflikt; Parallelität; RLS/Grants
- Keine historische Movement-Mutation

#### Regression

- `fertilizerProductStockCore.test.ts` (40/40)
- `fertilizerProductStockIntakeRpcCore.test.ts` + Intake DB
- `fertilizerProductStockReadCore.test.ts` + DB
- `fertilizerApplicationFlowCore.test.ts` (40/40)
- `fertilizerMultiAreaApplicationCore.test.ts` (29/29)
- Build/Typecheck; `git diff --check`

**Keine E2E** im Kern-Commit.

### 29.18 Offene Entscheidungen

| ID | Frage | Status | Blockiert Phase 6? |
|----|-------|--------|-------------------|
| OD-5 | Wann Creation-RPC deprecaten | ✅ **Phase 6:** produktiv nicht mehr für neue Saves; **Phase 7:** Entfernung | Nein |
| OD-6 | GA-012 Mehrgebinde-Dokumentanpassung | Offen — **Phase 7** | Nein |
| OD-P6-1 | Enum-Rename PostgreSQL vs. RPC-Mapping | ✅ **Entschieden:** Mapping in Phase 6; Rename Phase 7 optional | Nein |
| OD-P6-2 | Idempotency-Key-Format Outbound | ✅ **Entschieden:** analog Intake-Receipt | Nein |
| OD-P6-3 | Sichtbarkeit reiner Bestandsbewegungen | Offen ([DL-034](../decisions/dl-034.md) §20) — Saldo in Ausrüstung reicht für Phase 6 | Nein |
| OD-P6-4 | Onboarding-Voice CM-014 | Bewusst **parallel/Gate F** — nicht Phase 6 | Nein |
| OD-P6-5 | Startseiten-NL für Kauf/Abgang | Bewusst **nach Gate F** | Nein |
| OD-P6-6 | Feinere Abgangsgründe | **Nicht** eingeführt — `disposed` ausreichend v1 | Nein |

**Home-CTA-Hierarchie:** Für Phase 6 **nicht erforderlich** — Startseiten-CTA „Bestand erfassen“ ist **nicht** Teil des Kernscopes; Ausrüstung ist alleiniger produktiver Capture-Einstieg. Eine spätere Startseitenaktion für Bestand erfordert eine **separate bewusste Entscheidung** (Gate F oder Folgephase).

**Ergebnis:** Phase 6 ist **technisch umsetzungsbereit**. **Keine** neue Decision-Log-Datei erforderlich.

### 29.19 Phasengrenzen und Übergabe

#### Phase 6 liefert vollständig

- Capture → Intake-RPC (summierte Menge, ein kanonisches Item)
- Outbound-RPC für `gift_given`, `disposed`, `inventory_correction`
- Scoped UI Ausrüstung (Capture, Zugang, Abgang) — **ohne** Startseiten-Bestands-CTA
- Deprecation Creation-RPC für neue Buchungen
- Gate-F-Spezifikation (Sprache) als Follow-up dokumentiert

#### Übergabekriterien an Phase 7

- [x] Kein produktiver Aufruf Creation-RPC für neue Bestandsereignisse
- [x] Intake- und Outbound-DB-Tests grün
- [x] Capture-Unit-Tests auf Intake umgestellt
- [x] Phase 1–5 Regression grün

#### Phase 7 kann danach

- Creation-RPC und Package-Creation-Core entfernen
- Alt-Apply-Route entfernen
- Enum-Ausrichtung und GA-012-Doku (OD-6)
- Gate E

### 29.20 Gate F — Abschlusskriterien Phase 6

**Gate F ist erfüllt wenn:**

- [ ] Capture-Speichern nutzt `record_fertilizer_product_stock_intake` — kein neues Item pro Packung
- [ ] Outbound-RPC persistiert `gift_given`, `disposed`, `inventory_correction` mit Bestandsprüfung
- [ ] Düngung weiterhin **nur** Application-RPC
- [ ] Ausrüstung: Capture über **Dünger → Erfassen**; Zugang/Abgang scoped erreichbar; Liste zeigt aktualisierten Saldo
- [ ] Startseite: **keine** Phase-6-Änderung; CTA „Düngung erfassen“ unverändert
- [ ] Keine Sprach-/NL-Persistenz eingeführt
- [ ] Build + Unit-/DB-/Regressionstests grün
- [ ] Akzeptanzkriterien §29.16 FK-1, CI-1, U-2, U-3, V-2, NZ-1 erfüllt

### 29.21 Widerspruchsprüfung (Phase 6)

| Prüfpunkt | Ergebnis |
|-----------|----------|
| DL-033 Produktbestand | ✅ Ein Item pro Schlüssel |
| DL-034 Bewegungsgründe | ✅ Vollständig abgedeckt außer Gate-F-Sprache |
| DL-029 initial vs. correction | ✅ Getrennt |
| Phase-5-Düngung | ✅ Nicht dupliziert |
| Package-Identität | ✅ Summe, kein Gebinde |
| kg↔ml | ✅ Keine Umrechnung |
| Freies `user_id` | ✅ RPC-only auth.uid() |
| Historische Umschreibung | ✅ Keine |
| Conversation-Duplikat | ✅ Kein Voice-Persistenz in Phase 6 |
| Home-Hierarchie | ✅ Kein zweiter Home-CTA; Ausrüstung alleiniger Capture-Einstieg |
| Phase-5-CTA | ✅ „Düngung erfassen“ unverändert |
| K-6 Enum | ✅ Mapping Phase 6; Rename Phase 7 |
| K-7 Alt/Neu-RPC | ✅ Routing Phase 6; Entfernung Phase 7 |

---

## 30. Phase 7 — Bereinigung veralteter RPCs, Routen und Package-Restpfade (Spezifikation)

Dieser Abschnitt ist der **belastbare Umsetzungsvertrag** für Phase 7. Er baut auf dem **veröffentlichten** Phase-6-Stand auf (`f4f6a66`, `ab667dc`, `4f3254b`) und definiert ausschließlich **technische Bereinigung** — keine neue Produktfunktion.

**Umsetzungsstatus:** ✅ **Freigegebener Cleanup-Umfang abgeschlossen** (§30.17) — Schritt A (`ebc1b70`), B1 (`150c87d`), D (`fe0f1af`) veröffentlicht auf `fe0f1af`. Schritt C (Creation-RPC-Drop), B2/B3 (Creation-Stack/Harness) und Enum-Rename **nicht** freigegeben. **Keine** neue Decision-Log-Datei erforderlich, solange keine Enum-Umbenennung (OD-P7-4) freigegeben wird.

### 30.1 Zielbild

| | |
|---|---|
| **Primäres Ziel** | Obsolete technische Pfade entfernen, doppelte API-Flächen schließen, produktive Entry Points vereinheitlichen, Wartungsrisiko reduzieren |
| **Schutzziel** | Phase-6-Vertrag (Capture→Intake, Outbound, Read Model, Application, historische Lesbarkeit) **unverändert** |
| **Art der Phase** | Reine Cleanup-Phase — **keine** neue Nutzerfunktion, **keine** Datenmigration, **keine** historische Mutation |

#### Phase 7 darf

- Toten TypeScript-Code, Adapter, Re-Exports, Wrapper und Tests entfernen, die **nachweislich** nicht mehr produktiv referenziert sind
- Alt-Apply-Route durch **einen** Redirect auf `/duengung/:inventoryItemId` ersetzen und danach Legacy-Entry-Point-Logik entfernen
- Obsolete DB-Funktionen per **additiver Drop-Migration** entfernen, wenn repositoryweiter Nachweis und lokale DB-Regression grün sind

#### Phase 7 darf nicht

- Home, `Düngung erfassen`, Conversation, Voice, Onboarding oder Navigation ändern
- Neue Bestandswahrheit, Intake-Persistenz, Package-Verwaltung, FIFO oder `currentQuantity` einführen
- Historische Movements, Receipts, Journal-/Application-Referenzen oder Legacy-Daten ändern oder löschen
- Veröffentlichte Migrationen (`20250806` … `20250814`) **ändern**
- Phase-6-RPCs (`record_fertilizer_product_stock_intake`, `record_fertilizer_product_stock_outbound`) ersetzen oder aufweichen
- Phase-4-Read-Model-Eligibility ändern
- Enum-Werte umbenennen ohne **separate** Architekturentscheidung (OD-P7-4)
- `DROP … CASCADE` verwenden

### 30.2 Geschützter Phase-6-Vertrag (nicht anfassen)

| Bereich | Vertrag | Phase 7 |
|---------|---------|---------|
| Capture-Speichern | `saveFertilizerCaptureToInventoryCore` → `recordFertilizerProductStockIntake` | **retain_productive** |
| Positives Zugang | Intake-RPC; Reasons `initial_stock`, `purchase`, `gift_received` | **retain_productive** |
| Abgang/Korrektur | Outbound-RPC; Reasons `gift_given`, `disposed`, `inventory_correction` | **retain_productive** |
| Düngung | `/duengung`, `/duengung/:inventoryItemId` → `apply_fertilizer_inventory_item_to_areas` | **retain_productive** |
| Identität | `(user_id, saved_product_profile_id, base_unit)`; genau ein kanonisches Item pro Ereignis | **retain_productive** |
| Saldo | Summe wirksamer Movements; kein `currentQuantity` | **retain_productive** |
| Ausrüstung | Capture `/ausruestung/duenger/erfassen`; Zugang/Abgang scoped | **retain_productive** |
| Read Model | `list_active_fertilizer_product_stock`, `get_active_fertilizer_product_stock_item` | **retain_productive** |
| Legacy-Migration | `migrate_fertilizer_product_stock_group`; Reason `legacy_balance_migration` | **retain_productive** |
| Profilvalidierung Outbound | Phase-4-Eligibility serverseitig (`20250814`) | **retain_productive** |

### 30.3 Inventar — Cleanup-Kandidaten

Jeder Kandidat ist **repository-verifiziert** (Stand `4f3254b`). Kategorie: `remove_now` | `deprecate_then_remove` | `retain_historical` | `retain_productive` | `blocked`.

#### 30.3.1 Alter Creation-RPC (Packungs-Creation)

| Feld | Wert |
|------|------|
| **Symbol (DB)** | `public.create_fertilizer_inventory_core_from_confirmed_packages` |
| **Eingeführt** | `supabase/migrations/20250806_fertilizer_inventory_creation_core.sql` |
| **Signatur** | `(uuid, text, uuid, text, text, text, text, jsonb)` — `p_saved_product_profile_id`, `p_access_kind`, `p_user_id`, `p_session_access_hash`, `p_creation_reason`, `p_idempotency_key`, `p_source_event_ref`, `p_packages` |
| **Zweck (ursprünglich)** | N Inventory Items + N initiale Movements pro bestätigter Packungsgruppe |
| **Ersetzt durch** | `record_fertilizer_product_stock_intake` (Phase 2/6) — **eine** summierte Menge, **ein** kanonisches Item |
| **Produktive TS-Aufrufe** | **Keine** — Capture nutzt `recordFertilizerProductStockIntake` ([`fertilizerCaptureInventorySaveCore.ts`](../../src/lib/fertilizerCaptureInventorySaveCore.ts)); UI [`FertilizerCaptureFlow.tsx`](../../src/components/fertilizer/FertilizerCaptureFlow.tsx) |
| **Indirekte TS-Pfade** | [`createFertilizerInventoryFromCapture`](../../src/lib/fertilizerInventoryCreation.ts) — **nur** von Creation-Unit-/DB-Tests aufgerufen; [`createInventoryItemsWithInitialMovements`](../../src/lib/fertilizerInventoryRepositoryPersistentCore.ts) — **nur** Repository-/Creation-Tests |
| **Testreferenzen** | `fertilizerInventoryCreation.test.ts`, `fertilizerInventoryCreationCore.test.ts`, `fertilizerInventoryCreationRpcCore.test.ts`, `fertilizerInventoryCreationDatabase.test.ts`, `fertilizerInventoryRepositoryCore.test.ts`, `fertilizerInventoryCreationRpcCore.test.ts`, `fertilizerMultiAreaApplicationDatabaseTestHarness.ts` (Fixture-Setup) |
| **Historische Referenzen** | `fertilizer_inventory_creation_receipts`; historische Creation-Movements auf Legacy-/vor-kanonischen Items |
| **Öffentlich** | Ja — `GRANT EXECUTE` an `authenticated`, `service_role` |
| **Kategorie** | **`deprecate_then_remove`** (DB-Funktion) — Drop erst nach Schritt A + additive Migration + voller DB-Regression |
| **Entfernungsvoraussetzung** | Repositoryweite Symbolsuche = 0 produktive Aufrufe; Netlify/Edge = 0; lokale DB bestätigt Funktion fehlt nach Drop; Phase 2–6 DB grün |
| **Risiko** | Externe Client-Skripte oder manuelle RPC-Aufrufe außerhalb des Repos — **nicht belegbar ausgeschlossen** ohne OD-P7-1 |

**Begleitende TS-Dateien (nach Drop freigegeben):**

| Datei | Kategorie | Aktion |
|-------|-----------|--------|
| [`fertilizerInventoryCreation.ts`](../../src/lib/fertilizerInventoryCreation.ts) | `remove_now` (nach RPC-Drop) | löschen |
| [`fertilizerInventoryCreationCore.ts`](../../src/lib/fertilizerInventoryCreationCore.ts) | `remove_now` (nach RPC-Drop) | löschen — sofern keine historischen Typen mehr benötigt |
| [`fertilizerInventoryCreationRpcCore.ts`](../../src/lib/fertilizerInventoryCreationRpcCore.ts) | `remove_now` (nach RPC-Drop) | löschen |
| [`fertilizerInventoryCreation*.test.ts`](../../src/lib/) | `remove_now` (nach RPC-Drop) | löschen |
| [`fertilizerInventoryCreationDatabaseTestHarness.ts`](../../src/lib/fertilizerInventoryCreationDatabaseTestHarness.ts) | `remove_now` (nach RPC-Drop) | löschen |
| `createInventoryItemsWithInitialMovements` in [`fertilizerInventoryRepositoryPersistentCore.ts`](../../src/lib/fertilizerInventoryRepositoryPersistentCore.ts) | `remove_now` (nach RPC-Drop) | Methode + Interface in [`fertilizerInventoryRepositoryCore.ts`](../../src/lib/fertilizerInventoryRepositoryCore.ts) entfernen |
| Tabelle `fertilizer_inventory_creation_receipts` | `retain_historical` | **behalten** — Receipts historisch lesbar; kein Drop in Phase 7 |

#### 30.3.2 Legacy-Capture-RPC `save_fertilizer_capture`

| Feld | Wert |
|------|------|
| **Symbol (DB)** | `public.save_fertilizer_capture` |
| **Eingeführt** | `20250731_fertilizer_inventory.sql`; überarbeitet `20250801`, `20250802` |
| **Zweck (ursprünglich)** | Monolithischer Capture-Save ohne Saved-Product-Profile-Intake-Vertrag |
| **Ersetzt durch** | Enrichment → Profil-Save → `record_fertilizer_product_stock_intake` |
| **Produktive UI-Aufrufe** | **Keine** — [`FertilizerCaptureFlow.tsx`](../../src/components/fertilizer/FertilizerCaptureFlow.tsx) nutzt `saveFertilizerCaptureToInventoryCore` |
| **TS-Wrapper** | ~~`saveFertilizerCapture`~~ — **entfernt** in Schritt B1 (`150c87d`); DB-RPC `save_fertilizer_capture` **technisch unverändert** |
| **Skript-Referenzen** | `scripts/verify-product-profiles-migration.mjs`, `scripts/verify-fertilizer-inventory-core-migration.mjs` — **historische Migrationsverifikation**, nicht produktiv |
| **Kategorie** | **`deprecate_then_remove`** — Drop **nach** Creation-RPC-Drop und bestätigtem Null-Verweis; **`blocked`** bis OD-P7-1 geklärt |
| **Risiko** | Externe Nutzung des RPC außerhalb Repository — nicht nachweisbar ausgeschlossen |

#### 30.3.3 Alte Apply-Route (Ausrüstung)

| Feld | Wert |
|------|------|
| **Legacy-URL (Kompatibilität)** | `/ausruestung/duenger/:inventoryItemId/anwenden` — **registriert als Redirect** (Schritt D, `fe0f1af`) |
| **Redirect-Komponente** | `LegacyFertilizerApplicationRedirect` in [`App.tsx`](../../src/App.tsx) |
| **Redirect-Ziel** | [`resolveLegacyApplicationRedirectPath`](../../src/lib/fertilizerRoutes.ts) → `/duengung/:inventoryItemId`; ungültige/fehlende ID → `/duengung` |
| **Replace-Semantik** | `<Navigate … replace />` — keine Datenzugriffe, keine Persistenz |
| **Entfernt (Schritt D)** | `entryPoint="legacy"`, `FertilizerApplicationEntryPoint`, `shouldRedirectLegacyApplicationRoute`, `fertilizerLegacyApplicationPath`, `fertilizerApplicationPath`, Legacy-Parser/Pattern |
| **Kanonische Routes** | `/duengung`, `/duengung/:inventoryItemId` → [`FertilizerApplicationPage`](../../src/pages/FertilizerApplicationPage.tsx) ohne Entry-Point-Prop |
| **Erhaltene Helfer** | `fertilizerHomeApplicationPath`, `FERTILIZER_ROUTES.homeApplication`, `buildFertilizerApplicationRoute`, `resolveLegacyApplicationRedirectPath` |
| **Produktive UI-Links auf Legacy-Pfad** | **Keine** |
| **Produktiver Application-Einstieg** | **ausschließlich** `/duengung` — unverändert |
| **Kategorie** | Legacy-URL: **`retain_productive`** (Kompatibilitätsredirect); obsolete Helfer: **entfernt** |

**Weitere Redirect-Routen (bereits bereinigt, retain_productive):**

| Pfad | Verhalten | Kategorie |
|------|-----------|-----------|
| `/ausruestung/duenger/bestand` | → `/ausruestung/duenger` | **retain_productive** |
| `/ausruestung/duenger/katalog` | → `/ausruestung/duenger` | **retain_productive** |

#### 30.3.4 Package-orientierte Restpfade

##### Zulässige Capture-Eingabe (retain_productive)

| Symbol / Feld | Datei | Zweck |
|---------------|-------|-------|
| `packageCount`, `selectedPackageQuantity`, `previousRemainder` | [`fertilizerCaptureCore.ts`](../../src/lib/fertilizerCaptureCore.ts) | Nutzereingabe |
| `applyPackageCount`, `applyPackageClarification`, `applyFreeQuantityEntry` | Capture-Flow | UI-Schritte |
| `resolveCaptureInventoryBaseUnit` | [`fertilizerCaptureInventoryPackagesCore.ts`](../../src/lib/fertilizerCaptureInventoryPackagesCore.ts) | kg/ml-Ableitung (keine kg↔ml-Umrechnung) |
| `buildProductStockIntakeQuantityFromCaptureDraft` | [`fertilizerProductStockIntakeFromCaptureCore.ts`](../../src/lib/fertilizerProductStockIntakeFromCaptureCore.ts) | **Summenbildung → genau eine Intake-Menge** |
| `packageCount` in Save-Result | [`fertilizerCaptureInventorySaveCore.ts`](../../src/lib/fertilizerCaptureInventorySaveCore.ts) | Anzeige/Metadaten — **keine** Identität |

##### Obsolete Package-Persistenz (remove_now nach Referenznachweis)

| Symbol | Datei | Referenzen | Kategorie |
|--------|-------|------------|-----------|
| `buildConfirmedPackageGroupsFromCaptureDraft` | [`fertilizerCaptureInventoryPackagesCore.ts`](../../src/lib/fertilizerCaptureInventoryPackagesCore.ts) | **Keine** Importe im Repository | **`remove_now`** |
| `createFertilizerInventoryFromCapture` | [`fertilizerInventoryCreation.ts`](../../src/lib/fertilizerInventoryCreation.ts) | Nur Creation-Tests | **`remove_now`** (mit Creation-Stack) |
| `FertilizerConfirmedPackageGroupInput`, Packungs-RPC-Payload-Builder | [`fertilizerInventoryCreationCore.ts`](../../src/lib/fertilizerInventoryCreationCore.ts) | Nur Creation-Stack | **`remove_now`** (mit Creation-Stack) |
| N-Items-pro-Packung-Erwartungen | Creation-Unit-/DB-Tests | Test-only | **`remove_now`** (mit Creation-Stack) |

**Explizit nicht entfernen:** `package_size_value` / `package_size_unit` auf `fertilizer_containers` — historisches/optionales Item-Metadatum, **keine** Phase-7-Identität; Read Model nutzt es nicht als Schlüssel.

#### 30.3.5 Kompatibilitätsadapter und Re-Exports

| Symbol | Datei | Kategorie | Aktion |
|--------|-------|-----------|--------|
| `fertilizerApplicationPath` | `fertilizerRoutes.ts` | `remove_now` | löschen (Alias auf Legacy-Pfad; `@deprecated`) |
| `export { CREATE_FERTILIZER_INVENTORY_CORE_FROM_CONFIRMED_PACKAGES_RPC }` | `fertilizerInventoryCreation.ts` | `remove_now` | mit Datei löschen |
| `saveFertilizerCapture` | `fertilizerInventory.ts` | `deprecate_then_remove` | löschen wenn `save_fertilizer_capture` gedroppt |
| `parseSaveResult` (Legacy-Capture-Form) | `fertilizerInventory.ts` | `deprecate_then_remove` | mit `saveFertilizerCapture` löschen |
| `entryPoint: 'legacy'` | `FertilizerApplicationPage.tsx` | `deprecate_then_remove` | nach Redirect-Route entfernen |
| `apply_fertilizer_inventory_item_to_area` (Einzel-RPC) | DB `20250808` | `retain_historical` | **behalten** — [DL-031](../decisions/dl-031.md) Kompatibilitätsvertrag; sichtbarer Flow nutzt Mehrflächen-RPC |

#### 30.3.6 Historische / Legacy-Pfade (Schutzliste — nicht als „obsolete“ klassifizieren)

| Pfad / Symbol | Grund | Kategorie |
|---------------|-------|-----------|
| Alle Migrationen `20250731` … `20250814` | Unveränderliche Historie | **retain_historical** |
| Legacy-Container (`stock_kind IS NULL`, Recognition-Bindung) | Historische Lesbarkeit | **retain_historical** |
| Superseded Items + `legacy_balance_migration` | Phase 3 | **retain_historical** |
| `fertilizer_inventory_creation_receipts` | Idempotenz-Nachweis alter Saves | **retain_historical** |
| `fertilizer_product_stock_intake_receipts` / Outbound-Receipts | Phase 2/6 | **retain_productive** |
| Application-Batches, Activities, Journal-Snapshots | Phase 5 | **retain_productive** |
| `migrate_fertilizer_product_stock_group` | Phase 3 | **retain_productive** |
| Movement-Immutability-Trigger | Integrität | **retain_productive** |

### 30.4 Alter Creation-RPC — verbindlicher Entfernungsvertrag

1. **Phase 6** hat produktive Aufrufe entfernt — Capture speichert über Intake ([`f4f6a66`](../../)).
2. **Technische DB-Funktion** bleibt bis Phase-7-Drop-Migration vorhanden.
3. **Entfernung nur** per additive Migration (z. B. `20250815_drop_fertilizer_inventory_creation_rpc.sql`) — **keine** Änderung an `20250806`.
4. **Vor Drop** repositoryweit nachweisen:
   - keine produktiven TS-Aufrufe (`createFertilizerInventoryFromCapture`, `createInventoryItemsWithInitialMovements`, RPC-Konstante in Nicht-Test-Code)
   - keine Route/UI/Adapter/Fallback/Edge-Function-Aufrufe
   - keine Tests, die **produktive** Nutzung voraussetzen (Creation-Testdateien werden **mit** entfernt)
5. **Drop-Scope:** `DROP FUNCTION public.create_fertilizer_inventory_core_from_confirmed_packages(uuid, text, uuid, text, text, text, text, jsonb);` — exakte Signatur aus `20250806`; **kein** `CASCADE`.
6. **Grants:** Revoke bereits in Ursprungsmigration; Drop entfernt Execute-Rechte.
7. **Keine Alias-Funktion** — endgültige Entfernung, kein Wrapper-RPC.
8. **Regression:** Phase 2 Intake, Phase 3 Migration, Phase 4 Read, Phase 6 Outbound (inkl. Profilvalidierung); Capture→Intake-Unit-Tests.
9. **Rückfall:** Revert Drop-Migration-Commit; historische Receipts/Movements unberührt.

**Sofortige Entfernung freigegeben?** **Nein** — Kategorie `deprecate_then_remove` wegen OD-P7-1 (externe RPC-Nutzung nicht belegbar ausgeschlossen).

### 30.5 Alte Apply-Routen — Einzelbewertung

| Route | Ziel heute | Referenzen | Redirect? | Entfernung |
|-------|------------|------------|-----------|------------|
| `/ausruestung/duenger/:inventoryItemId/anwenden` | `FertilizerApplicationPage` legacy | `App.tsx`; indirekt Bookmarks | **Ja** → `/duengung/:inventoryItemId` | Nach Redirect: Legacy-Helfer + `entryPoint` löschen |
| `/duengung` | Home-Application | Phase 5/6 produktiv | — | **retain_productive** |
| `/duengung/:inventoryItemId` | Home-Application mit Vorauswahl | Phase 5/6 produktiv | — | **retain_productive** |

**Grundsatz:** Keine itemgebundene Apply-**Persistenz**; Redirect erzeugt **keine** zweite fachliche Wahrheit — nur URL-Kompatibilität.

### 30.6 Package-Restpfade — Trennung

| Schicht | Phase 7 |
|---------|---------|
| **Capture-Eingabe** (Count, Quantity, Remainder) | **Behalten** — Summe via `buildProductStockIntakeQuantityFromCaptureDraft` |
| **Domain-Identität** | **Keine** Package-Identität — `(user_id, saved_product_profile_id, base_unit)` |
| **Persistenz** | **Kein** Item je Packung — Intake-RPC only |
| **Tote Mapper** | `buildConfirmedPackageGroupsFromCaptureDraft` → **entfernen** |
| **Creation-Payloads** | `p_packages jsonb` am Creation-RPC → **entfernen** mit RPC-Drop |

### 30.7 Historische Integrität (verbindlich)

- Bestehende Migrationen: **unverändert**
- Movements, Receipts, Journal-, Application-, Batch-, Activity-Referenzen: **unverändert**
- Legacy-/superseded Items: historisch lesbar; Outbound/Intake auf inactive Items weiterhin **gesperrt**
- Keine Reparenting-, Datenlöschungs- oder Package-Normalisierungs-Migration
- Phase 7 = **Code- und API-Cleanup**, keine Datenbereinigung

### 30.8 Migrationsstrategie

| Regel | Vertrag |
|-------|---------|
| Nur **additive** Migrationen für Drops | `DROP FUNCTION` mit exakter Signatur |
| Nie veröffentlichte Migrationen ändern | Ja |
| Kein `CASCADE` | Ja — Migration muss fehlschlagen bei unerwarteten Abhängigkeiten |
| Keine Tabellen-Drops in Phase 7 | `fertilizer_inventory_creation_receipts` **behalten** |
| Kein Enum-Rename | Separate Entscheidung OD-P7-4 |
| Keine RLS-Aufweichung / Grant-Ausweitung | Ja |
| Lokaler DB-Test | Bestätigt Funktion fehlt; Phase 2–6 Suites 0 failed / 0 skipped |

**Geplante Drop-Reihenfolge:**

1. (Optional, wenn freigegeben) `save_fertilizer_capture`
2. `create_fertilizer_inventory_core_from_confirmed_packages`

Hilfsfunktionen `_inventory_creation_*` **nur** droppen, wenn nach Abhängigkeitsprüfung **keine** Referenz mehr durch Application-/Multi-Area-Migrationen (`_inventory_creation_format_quantity`, `_inventory_creation_compute_fingerprint` werden noch von Application-RPCs genutzt) — **retain_historical** bis separate Analyse.

### 30.9 Kleinster sicherer Implementierungsscope

#### Schritt A — Referenz- und Dead-Code-Nachweis (Pflicht, ohne Codeänderung)

Repositoryweite Suche nach:

- `create_fertilizer_inventory_core_from_confirmed_packages`
- `createFertilizerInventoryFromCapture`
- `createInventoryItemsWithInitialMovements`
- `save_fertilizer_capture` / `saveFertilizerCapture`
- `fertilizerLegacyApplicationPath` / `fertilizerApplicationPath`
- `buildConfirmedPackageGroupsFromCaptureDraft`

Dokumentierte Ergebnisliste; Abweichung → Kandidat **`blocked`**.

#### Schritt B — Code-Cleanup (TS only)

Nachweislich tote Symbole aus §30.3.4–30.3.5 entfernen; Creation-Stack-Dateien **erst** nach Schritt C oder parallel wenn RPC noch existiert aber TS unreferenziert.

**Commit-Vorschlag:** `chore: remove dead fertilizer creation client paths`

#### Schritt C — additive RPC-Drop-Migration

Nur wenn OD-P7-1 erfüllt: Drop Creation-RPC.

**Commit-Vorschlag:** `fix: drop obsolete fertilizer inventory creation rpc`

#### Schritt D — Redirect-Cleanup

`/ausruestung/duenger/:inventoryItemId/anwenden` → `<Navigate …>`; Legacy-Parser/Helfer/`entryPoint="legacy"` entfernen.

**Commit-Vorschlag:** `chore: redirect legacy fertilizer apply route to home flow`

#### Schritt E — vollständige Regression

Unit (Capture→Intake, Routes, Application-Flow, Outbound-Core), lokale DB Phase 2–6, Build, `git diff --check`.

**Empfohlene Commit-Anzahl:** **3–4** (B, C, D getrennt; E ohne Commit). Nicht alles in einem Commit.

### 30.10 Konkrete Datei- und Symbolliste (Implementierung)

| Datei | Symbol / Bereich | Aktion | Kategorie | Test |
|-------|------------------|--------|-----------|------|
| `fertilizerCaptureInventoryPackagesCore.ts` | `buildConfirmedPackageGroupsFromCaptureDraft` | löschen | remove_now | `fertilizerProductStockIntakeFromCaptureCore.test.ts` |
| `fertilizerCaptureInventoryPackagesCore.ts` | `resolveCaptureInventoryBaseUnit` | behalten | retain_productive | Capture/Intake-Tests |
| `fertilizerInventoryCreation.ts` | gesamte Datei | löschen | remove_now (post-Drop) | — |
| `fertilizerInventoryCreationCore.ts` | gesamte Datei | löschen | remove_now (post-Drop) | — |
| `fertilizerInventoryCreationRpcCore.ts` | gesamte Datei | löschen | remove_now (post-Drop) | — |
| `fertilizerInventoryCreation*.test.ts` | gesamte Dateien | löschen | remove_now (post-Drop) | — |
| `fertilizerInventoryCreationDatabaseTestHarness.ts` | gesamte Datei | löschen | remove_now (post-Drop) | — |
| `fertilizerInventoryRepositoryPersistentCore.ts` | `createInventoryItemsWithInitialMovements` | löschen | remove_now (post-Drop) | Repository-Tests anpassen |
| `fertilizerInventoryRepositoryCore.ts` | Interface-Methode | löschen | remove_now (post-Drop) | Repository-Tests anpassen |
| `fertilizerInventory.ts` | `saveFertilizerCapture`, `parseSaveResult` | löschen | deprecate_then_remove | Integration-Test anpassen |
| `fertilizerRoutes.ts` | `fertilizerApplicationPath`, `fertilizerLegacyApplicationPath`, Legacy-Parser | löschen/redirect | deprecate_then_remove | `fertilizerRoutes.test.ts`, `fertilizerApplicationFlowCore.test.ts` |
| `App.tsx` | Route `/anwenden` | redirect | deprecate_then_remove | Route-Tests |
| `FertilizerApplicationPage.tsx` | `entryPoint="legacy"`, Redirect-Logik | vereinfachen | deprecate_then_remove | Application-Flow-Tests |
| `supabase/migrations/20250815_…sql` | `DROP FUNCTION create_fertilizer_inventory_core…` | neu | deprecate_then_remove | DB: Funktion absent |
| `supabase/migrations/20250806_…sql` | — | **nicht ändern** | retain_historical | — |
| Phase-6-Dateien (`fertilizerProductStock*`, Outbound, Intake adapter) | — | **nicht anfassen** | retain_productive | Phase 2–6 DB |

### 30.11 Testvertrag Phase 7

#### Creation-RPC-Cleanup

- Capture speichert weiter über `recordFertilizerProductStockIntake`
- Nach Drop: RPC nicht ausführbar; kein produktiver TS-Import
- Historische Creation-Receipts/Movements lesbar (Read-/Journal-Tests unverändert grün)

#### Route-Cleanup

- `/duengung` produktiver Application-Einstieg
- Alt-Pfad `/anwenden` → Redirect oder 404 **bewusst** (nach Schritt D: Redirect)
- Keine Home-Änderung

#### Package-Cleanup

- `buildProductStockIntakeQuantityFromCaptureDraft` — Summe korrekt bei Count > 1
- Genau ein Intake, ein Item, ein positives Movement
- kg/ml getrennt; keine kg↔ml-Umrechnung

#### DB-Regression (Pflicht)

- Phase 2 Intake, Phase 3 Migration, Phase 4 Read, Phase 6 Outbound (15 Tests), Profilvalidierung, RLS, Idempotenz, Parallelität, negativer Bestand

#### Historische Regression

- Legacy/superseded lesbar; Journals/Applications/Batches/Activities lesbar; Movements unverändert

### 30.12 Abnahmekriterien (Gate E — Phase 7)

**Freigegebener Umfang (Schritt A, B1, D):**

- [x] Entfernte TS-Symbole repositoryweit unreferenziert (Schritt A + B1 + D)
- [x] Keine Phase-6-Produktfunktion verändert
- [x] Capture → Intake; Outbound unverändert; Application-Persistenz unverändert; Read Model unverändert
- [x] Keine Home-/Conversation-/Voice-Änderung
- [x] Keine zweite Bestandswahrheit; keine Package-Persistenz
- [x] Historische Daten unverändert
- [x] Keine Migration geändert; kein Enum-Rename
- [x] Unit + Build + `git diff --check` grün (B1, D)
- [x] Legacy-URL redirectet ausschließlich auf kanonischen Pfad (D)

**Weiterhin offen (blockierte Kandidaten — nicht Teil des freigegebenen Umfangs):**

- [ ] Additive Drop-Migrationen für Creation-RPC / `save_fertilizer_capture` (Schritt C; OD-P7-1)
- [ ] Vollständiger Creation-Stack-/Harness-Cleanup (B2/B3)
- [ ] `_inventory_creation_*`-Entkopplung (OD-P7-3)
- [ ] Lokale DB-Suiten Phase 2–6 nach RPC-Drop: **0 failed, 0 skipped**

### 30.13 Offene Entscheidungen (Phase 7)

| ID | Frage | Evidenz | Risiko | Standardentscheidung | Blockiert? | Benötigter Nachweis |
|----|-------|---------|--------|----------------------|------------|---------------------|
| **OD-P7-1** | Externe Nutzung Creation-RPC / `save_fertilizer_capture`? | Repo: 0 produktive UI-Aufrufe; Skripte nur Verify | Manueller/externer Client | Drop **erst** nach expliziter Freigabe oder Deprecation-Wartezeit | **Ja** (Schritt C) | Repo-Suche + ggf. Produktionslog/Stakeholder |
| **OD-P7-2** | Deep Links auf `/anwenden`? | **Umgesetzt (D):** Redirect auf `/duengung/:id` | Bookmark bricht nicht hart | **Redirect** statt 404 | Nein | Optional Analytics |
| **OD-P7-3** | `_inventory_creation_*` Hilfsfunktionen droppen? | Application-RPCs nutzen `_inventory_creation_format_quantity`, Fingerprint | Drop bricht Application | **Nicht** in Phase 7 droppen | Ja (Hilfsfunktionen) | Dependency-Query in DB |
| **OD-P7-4** | Enum-Ausrichtung (`fertilization`→`application`, …)? | In §29 als Phase-7-optional genannt | Breaking für SQL/Reports | **Separate** Entscheidung; **nicht** Phase-7-Kernscope | Ja (Enum) | Eigenes DL + Migrationsplan |
| **OD-P7-5** | GA-012 Mehrgebinde-Doku (OD-6)? | Dokumentation only | Verwirrung | Nur Doku-Anpassung, **kein** Code | Nein | Redaktion GA-012 |

**Kein neuer Decision Log** für OD-P7-1–P7-3/P7-5 — Evidenz und Gate E in GA-015 ausreichend. **OD-P7-4** erfordert bei Freigabe separates DL.

### 30.14 Nicht Teil von Phase 7

Wie §24 und §29.15, zusätzlich explizit: neue Düngerfunktion; Home-CTA; Conversation/Voice; Onboarding; Navigation; neue Bestandsarten/Movement-Reasons; Verkauf; Package-/Gebindeverwaltung; FIFO; `currentQuantity`; kg↔ml-Umrechnung; Profilzusammenführung; historische Package-Datenmigration; E2E-Neuaufbau; Application-/Read-Model-Neugestaltung; Enum-Rename als Nebenprodukt; historische Datenlöschung; `_inventory_creation_*`-Drops solange Application abhängig.

### 30.15 Übergabe von Phase 6

Phase 6 (**veröffentlicht** `4f3254b`) erfüllt die Übergabe an Phase 7 laut §29.19:

- [x] Kein produktiver Capture-Aufruf Creation-RPC
- [x] Intake- und Outbound-DB-Tests grün (15 + Phase 2–4)
- [x] Capture-Unit-Tests auf Intake
- [x] Phase 1–5 Regression grün

Phase 7 **freigegebener Cleanup** abgeschlossen mit Schritt A, B1, D (§30.17).

### 30.16 Schritt A — Referenz- und Dead-Code-Nachweis (ausgeführt)

| | |
|---|---|
| **Datum** | 2026-08-03 |
| **Repository-Stand** | `5054e058a863c21451fa9c1765c299e3bfa475ac` (`origin/main`) |
| **Methoden** | `git grep`, ripgrep über `src/`, `supabase/`, `scripts/`, `netlify/`, `e2e/`, `docs/` |
| **Nicht durchsucht** | Nicht versionierte Laufzeitumgebungen, Produktionslogs, externe Clients, Bookmarks außerhalb des Repos |

**Fakten vs. Schlussfolgerungen:** Trefferzahlen beziehen sich auf den genannten Stand. „Kein produktiver Aufruf“ bedeutet: **kein Import/Aufruf in `src/` außerhalb von Tests und Harness** — **nicht** „keine externe Nutzung“.

#### 30.16.1 Creation-RPC

| Feld | Nachweis |
|------|----------|
| **Signatur** | `create_fertilizer_inventory_core_from_confirmed_packages(uuid, text, uuid, text, text, text, text, jsonb)` |
| **Definition** | `supabase/migrations/20250806_fertilizer_inventory_creation_core.sql` (Zeile 160) |
| **Grants** | `REVOKE ALL FROM public`; `GRANT EXECUTE TO authenticated, service_role` |
| **Produktive TS-Aufrufe** | **0** — [`fertilizerInventoryRepositoryPersistentCore.ts`](../../src/lib/fertilizerInventoryRepositoryPersistentCore.ts) `createInventoryItemsWithInitialMovements` wird **nur** von Tests/Harness aufgerufen |
| **Test-/Harness-Aufrufe** | `fertilizerInventoryCreation.test.ts`, `fertilizerInventoryCreationDatabase.test.ts`, `fertilizerInventoryCreationRpcCore.test.ts`, `fertilizerInventoryRepositoryCore.test.ts`, `fertilizerInventoryCreationDatabaseTestHarness.ts`, `fertilizerMultiAreaApplicationDatabaseTestHarness.ts` (Fixture-Setup), `fertilizerApplicationDatabaseTestHarness.ts` (Schema-Assertion) |
| **Skript-Referenzen** | `scripts/verify-fertilizer-inventory-creation-migration.mjs`, `scripts/verify-fertilizer-inventory-legacy-core-upgrade-migration.mjs` |
| **Dokumentation** | `ga-015.md` (Ist-Beschreibung Phase 1–5), `ga-014.md` |
| **Netlify/E2E** | **0** Treffer in `netlify/functions/`, **0** in `e2e/` |
| **Externe Nutzung ausgeschlossen?** | **Nein** — RPC bleibt mit `GRANT EXECUTE` für `authenticated` deployt |

#### 30.16.2 Legacy-Capture-RPC

| Feld | Nachweis |
|------|----------|
| **DB-Signatur** | `save_fertilizer_capture(text, uuid, jsonb, numeric, text, numeric, integer, text)` |
| **Definitionen** | `20250731_fertilizer_inventory.sql`, `20250801_product_profiles.sql`, `20250802_save_fertilizer_capture_replay_product_profile.sql` |
| **Grants** | `GRANT EXECUTE TO authenticated` |
| **TS-Wrapper** | [`saveFertilizerCapture`](../../src/lib/fertilizerInventory.ts) — ruft RPC per String-Literal auf |
| **Produktive Imports von `saveFertilizerCapture`** | **0** — Capture nutzt [`saveFertilizerCaptureToInventoryCore`](../../src/lib/fertilizerCaptureInventorySaveCore.ts) → Intake |
| **Test-/Integrationsreferenzen** | `fertilizerInventoryCoreIntegration.test.ts` (Existenz-Assertion), Negativ-Assertions in `fertilizerInventoryAtomicMovementCore.test.ts`, `fertilizerInventoryRepositoryPersistentCore.test.ts`, `fertilizerApplication.test.ts`, `fertilizerInventoryCreation.test.ts` |
| **Skript-RPC-Aufrufe** | `scripts/verify-product-profiles-migration.mjs` (Live-RPC-Tests gegen Migration) |
| **Drop unabhängig von Creation-RPC?** | **Nein** empfohlen — historisch gekoppelter Legacy-Pfad; beide `deprecate_then_remove`, gemeinsame OD-P7-1-Freigabe |

#### 30.16.3 Creation-TypeScript-Stack (9 Dateien)

| Datei | Produktive Referenz? | Kategorie nach Nachweis |
|-------|----------------------|-------------------------|
| [`fertilizerInventoryCreation.ts`](../../src/lib/fertilizerInventoryCreation.ts) | **Nein** — nur `fertilizerInventoryCreation.test.ts` | `remove_now` **nach** RPC-Drop und Typ-Extraktion aus Core |
| [`fertilizerInventoryCreationRpcCore.ts`](../../src/lib/fertilizerInventoryCreationRpcCore.ts) | **Nein** — nur Creation-Tests + Repository-Persistenz-Pfad (selbst test-only) | `remove_now` post-Drop |
| [`fertilizerInventoryCreationCore.ts`](../../src/lib/fertilizerInventoryCreationCore.ts) | **Ja** — siehe Symbole unten | **Teilweise `retain_productive`** — nicht vollständig löschbar |
| [`fertilizerInventoryCreation*.test.ts`](../../src/lib/) (4 Dateien) | Test-only | `remove_now` post-Drop (mit RPC-DB-Tests) |
| [`fertilizerInventoryCreationDatabaseTestHarness.ts`](../../src/lib/fertilizerInventoryCreationDatabaseTestHarness.ts) | **Kein Produktivcode** — aber **verpflichtende DB-Fixtures** für Phase 2–6, Application-, Legacy-, Outbound-DB-Tests | **`retain_historical` / Test-Fixture** bis Harness-Umstellung |
| [`fertilizerInventoryRepositoryPersistentCore.ts`](../../src/lib/fertilizerInventoryRepositoryPersistentCore.ts) `createInventoryItemsWithInitialMovements` | **Nein** — nur Creation-DB-/Rpc-Tests | `remove_now` post-Drop |
| [`fertilizerInventoryRepositoryCore.ts`](../../src/lib/fertilizerInventoryRepositoryCore.ts) Interface + In-Memory-Implementierung | **Nein** produktiv — In-Memory nur Unit-Tests | `remove_now` post-Drop (Methoden); Core-Typen ggf. extrahieren |

**Aus `fertilizerInventoryCreationCore.ts` weiterhin produktiv genutzte Symbole:**

| Symbol | Produktive Importe |
|--------|-------------------|
| `FertilizerInventoryCreationReason` | `fertilizerCaptureCore.ts`, `fertilizerCaptureInventorySaveCore.ts`, `fertilizerInventoryLegacyMigrationCore.ts` |
| `FERTILIZER_INVENTORY_CREATION_MAX_IDEMPOTENCY_KEY_LENGTH` | `fertilizerProductStockIntakeRpcCore.ts`, `fertilizerProductStockOutboundRpcCore.ts` |
| `FertilizerConfirmedPackageGroupInput` | **nur** [`fertilizerCaptureInventoryPackagesCore.ts`](../../src/lib/fertilizerCaptureInventoryPackagesCore.ts) → tote Funktion |
| `buildCanonicalFertilizerInventoryCreationPayload` | **nur** `fertilizerInventoryRepositoryCore.ts` (In-Memory-Repository für Tests) |

**Schlussfolgerung:** Der **gesamte** Creation-Stack ist **nicht** pauschal `remove_now`. Zuerst Shared-Typen (`FertilizerInventoryCreationReason`, Idempotency-Max-Length) auslagern, dann Runtime-Dateien entfernen.

#### 30.16.4 Package-Restpfade

| Symbol / Feld | Produktiv | Kategorie | Begründung |
|---------------|-----------|-----------|------------|
| `packageCount`, `selectedPackageQuantity`, `previousRemainder`, `applyPackageCount` | Ja (Capture-UI/Core) | `retain_productive` | Summenbildung via `buildProductStockIntakeQuantityFromCaptureDraft` |
| `buildProductStockIntakeQuantityFromCaptureDraft` | Ja | `retain_productive` | Ein Intake, eine Menge |
| `resolveCaptureInventoryBaseUnit` | Ja | `retain_productive` | kg/ml-Ableitung |
| `inventoryItemIds: [singleId]` in Save-Result | Ja (Phase 6) | `retain_productive` | Genau ein Item |
| `buildConfirmedPackageGroupsFromCaptureDraft` | **Nein** — **0 Importe** | **`remove_now`** | Tot; einziger Importeur wäre Creation-Pfad |
| `p_packages` (Creation-RPC) | Nur DB + Harness | `deprecate_then_remove` | Mit RPC-Drop |
| `package_size_value` / `package_size_unit` (Container-Spalten) | Metadatum | `retain_historical` | Kein Identitätsschlüssel |
| N-Items-Tests | `fertilizerInventoryCreation*.test.ts`, Creation-DB-Tests | Test-Fixture | Entfernbar **mit** Creation-Testdateien, nicht isoliert vor RPC-Drop |

#### 30.16.5 Legacy-Apply-Pfad

| Element | Nachweis | Kategorie |
|---------|----------|-----------|
| Route `/ausruestung/duenger/:inventoryItemId/anwenden` | [`App.tsx`](../../src/App.tsx) — **Redirect** (`fe0f1af`) | **✅ Kompatibilitätsredirect** |
| `/duengung`, `/duengung/:inventoryItemId` | `App.tsx` — produktiv | `retain_productive` |
| Produktive UI-Links auf `/anwenden` | **0** | — |
| Redirect | `LegacyFertilizerApplicationRedirect` + `resolveLegacyApplicationRedirectPath` → `/duengung/:id` | **✅ umgesetzt (D)** |
| Persistenz | **Identisch** — kanonische Page → `apply_fertilizer_inventory_item_to_areas`; Legacy-URL **keine** Persistenz | Keine zweite Persistenz |
| Obsolete Helfer | `fertilizerApplicationPath`, Legacy-Parser, `entryPoint="legacy"` | **✅ entfernt (D)** |
| E2E | **0** Treffer | — |

#### 30.16.6 `_inventory_creation_*` und historische Abhängigkeiten

| Funktion | Definiert in | Referenziert von |
|----------|--------------|------------------|
| `_inventory_creation_format_quantity` | `20250806` | `20250808` Application-RPC, `20250809` Multi-Area-RPC |
| `_inventory_creation_compute_fingerprint` | `20250806` | `20250807` Legacy-Core-Upgrade, `20250808`, `20250809` |
| `_inventory_creation_advisory_lock_key` | `20250806` | nur Creation-RPC selbst |
| `_inventory_creation_movement_idempotency_key` | `20250806` | nur Creation-RPC selbst |
| `apply_fertilizer_inventory_item_to_area` | `20250808` | Unit-Test `fertilizerApplication.test.ts`; sichtbarer Flow nutzt **Mehrflächen-RPC** ([DL-031](../decisions/dl-031.md)) |
| `fertilizer_inventory_creation_receipts` | `20250806` | Creation-DB-Tests, Harness-Cleanup |

**Schlussfolgerung OD-P7-3:** `_inventory_creation_format_quantity` und `_inventory_creation_compute_fingerprint` sind **produktiv** für Application — **Drop blockiert**.

#### 30.16.7 Enum- und Fehlercode-Nachweis

| Thema | Befund | Kategorie |
|-------|--------|-----------|
| Movement-Enum DB-Werte (`fertilization`, `gifted_away`, `disposal`, …) | In historischen Movements und Phase-6-Mapping verwendet | `retain_historical` / OD-P7-4 **separat** |
| `INVENTORY_CREATION_*` RPC-Fehlercodes | Nur Creation-RPC + Creation-Tests + `fertilizerInventoryCreation.ts` Message-Map | `deprecate_then_remove` mit RPC-Drop |
| `creation_reason_invalid` (Client) | `fertilizerCaptureInventorySaveCore.ts` — Intake-Pfad, **nicht** Creation-RPC | `retain_productive` (Intake-Validierung) |

#### 30.16.8 Evidenzmatrix (Kandidaten → Kategorie nach Nachweis)

| Kandidat | Prod. Ref. | Test/Harness | Hist. | Ext. ausgeschlossen? | Vorher | **Nach Nachweis** | Nächster Schritt |
|----------|------------|--------------|-------|----------------------|--------|-------------------|------------------|
| Creation-RPC (DB) | 0 intern | Ja (6+ Dateien) | Ja (Receipts) | **Nein** | deprecate | **`deprecate_then_remove`** | Schritt C nach OD-P7-1 |
| `save_fertilizer_capture` | 0 UI | Ja (Skripte/Tests) | Ja | **Nein** | deprecate | **`deprecate_then_remove`** | Mit OD-P7-1 |
| `saveFertilizerCapture` | 0 | — | — | — | remove_now | **✅ entfernt (B1)** | — |
| `buildConfirmedPackageGroupsFromCaptureDraft` | **0** | **0** | — | — | remove_now | **✅ entfernt (B1)** | — |
| Creation-TS-Runtime (`creation.ts`, RpcCore, Persist-Methode) | 0 | Ja | — | — | remove_now | **`remove_now` post-Drop** | Nach Typ-Extraktion |
| `fertilizerInventoryCreationCore.ts` (gesamt) | **Teilweise** | Ja | — | — | remove_now | **`retain_productive` (Teil) + extrahieren** | Typ-Split vor Stack-Löschung |
| Creation-DB-Harness | 0 prod | **Phase 2–6 DB** | Ja | — | remove_now | **`retain_historical` (Fixture)** | Ersetzen wenn RPC weg |
| Legacy-Apply-Route | Redirect | Tests | — | Unklar (Bookmarks) | deprecate | **✅ Redirect (D)** | Kompatibilität erhalten |
| Legacy-Parser/Helfer | 0 prod | — | — | — | remove_now | **✅ entfernt (D)** | — |
| `_inventory_creation_*` (shared) | Application-RPC | Creation | Ja | — | retain | **`retain_historical`** | OD-P7-3 blockiert Drop |
| Package-Eingabe-Felder | Ja | Ja | — | — | retain | **`retain_productive`** | — |
| `apply_fertilizer_inventory_item_to_area` | 0 sichtbar | Ja | DL-031 | — | retain | **`retain_historical`** | — |

#### 30.16.9 Open Decisions — Update nach Schritt A

| ID | Status | Neue Evidenz | Unsicherheit | Entscheidung / Standard | Blockiert | Benötigter Nachweis |
|----|--------|--------------|--------------|-------------------------|-----------|---------------------|
| **OD-P7-1** | **Weiterhin blockiert** | 0 produktive interne Aufrufe von Creation-RPC und `save_fertilizer_capture`; Grants weiterhin `authenticated` | Externe/manuelle RPC-Nutzung, ältere Clients | Drop **erst** nach expliziter Freigabe | **Schritt C** | Stakeholder/Log/Deprecation-Fenster |
| **OD-P7-2** | **Umgesetzt** (Schritt D, `fe0f1af`) | Legacy-URL → Redirect `/duengung/:id`; Item-ID unverändert; Replace-Semantik | Bookmark-Tiefe unbekannt | **Redirect** statt 404 | **Nein** | Optional Analytics |
| **OD-P7-3** | **Weiterhin blockiert** | `_inventory_creation_format_quantity` + `_inventory_creation_compute_fingerprint` in `20250808`/`20250809` | Vollständige Dependency-Query | Hilfsfunktionen **behalten** | **Hilfsfunktion-Drop** | DB-Abhängigkeitsanalyse + Application-Regression |
| **OD-P7-4** | **Separat offen** | Enum-Werte in historischen Movements | Rename bricht Reports | **Nicht** Phase-7-Kernscope | Enum-Rename | Eigenes DL |
| **OD-P7-5** | **Offen, nicht blockierend** | GA-012/ga-014 beschreiben teils Legacy-Capture als aktuell | Dokumentationsdrift | Redaktion GA-012/veraltete Docs | Nein | Redaktionspass |

#### 30.16.10 Implementierungsfreigaben nach Schritt A

| Freigabe | Status | Scope | Ergebnis |
|----------|--------|-------|----------|
| **B1 — TS-Dead-Code** | **✅ Umgesetzt** (`150c87d`) | Tote Capture-Kompatibilitätssymbole | 3 Dateien; Capture→Intake unverändert |
| **B2 — Creation-Stack-Dateien löschen** | **Nicht freigegeben** | Blocker: `fertilizerInventoryCreationCore.ts` produktiv | Typ-Extraktion zuerst |
| **B3 — Creation-DB-Tests/Harness entfernen** | **Nicht freigegeben** | Phase 2–6 DB-Suiten nutzen Harness-Fixtures | Erst nach RPC-Drop |
| **C — Creation-RPC-Drop** | **Nicht freigegeben** | OD-P7-1 | Externe-Nutzungs-Freigabe |
| **D — Legacy-Apply-Redirect** | **✅ Umgesetzt** (`fe0f1af`) | Redirect + Route-Cleanup | 6 Dateien; eine Application-Persistenz |

#### 30.16.11 Kleinster nächster Implementierungsscope (historisch)

**Stand nach B1/D:** Kein weiterer automatisch freigegebener Implementierungsscope. Siehe §30.17.

---

### 30.17 Freigegebener Phase-7-Cleanup — Abschluss (veröffentlicht)

| | |
|---|---|
| **Datum** | 2026-08-03 |
| **Repository-Stand** | `fe0f1afd5bd7fc0a8fbbdcf7327b9005079f970e` (`origin/main`) |
| **Phase-7-Commits** | `5054e05` (Scope), `ebc1b70` (Schritt A), `150c87d` (B1), `fe0f1af` (D) |

**Fakten vs. Schlussfolgerungen:** Dieser Abschnitt dokumentiert **ausschließlich** den evidenzbasiert freigegebenen und umgesetzten Cleanup-Umfang. Phase 7 ist **nicht** vollständig technisch bereinigt — blockierte RPC-, Harness- und Hilfsfunktions-Kandidaten bleiben bewusst erhalten.

#### 30.17.1 Umgesetzte Schritte

| Schritt | Commit | Status |
|---------|--------|--------|
| **A — Referenz- und Dead-Code-Nachweis** | `ebc1b70b26d77cac3c0f7362fffb4e86a500eea9` | ✅ veröffentlicht (§30.16) |
| **B1 — Toter TypeScript-Kompatibilitätscode** | `150c87de7136d870148cc45c24a01c299064d152` | ✅ veröffentlicht |
| **D — Legacy-Apply-Redirect und Route-Cleanup** | `fe0f1afd5bd7fc0a8fbbdcf7327b9005079f970e` | ✅ veröffentlicht |

**Nicht umgesetzt (blockiert oder nicht freigegeben):** Schritt B2, B3, C; Enum-Rename (OD-P7-4); `_inventory_creation_*`-Drop (OD-P7-3).

#### 30.17.2 Schritt B1 — Ergebnis (`150c87d`)

| Entfernt | Datei |
|----------|-------|
| `buildConfirmedPackageGroupsFromCaptureDraft` | [`fertilizerCaptureInventoryPackagesCore.ts`](../../src/lib/fertilizerCaptureInventoryPackagesCore.ts) |
| `resolveNominalPackageSize` (nur vom toten Mapper genutzt) | dieselbe Datei |
| Import `FertilizerConfirmedPackageGroupInput` | dieselbe Datei |
| `saveFertilizerCapture`, `parseSaveResult` | [`fertilizerInventory.ts`](../../src/lib/fertilizerInventory.ts) |
| Typ-Imports `FertilizerCaptureSaveResult`, `FertilizerRecognitionCandidatePayload` | dieselbe Datei |
| Integrationstest-Assertion auf toten Wrapper | [`fertilizerInventoryCoreIntegration.test.ts`](../../src/lib/fertilizerInventoryCoreIntegration.test.ts) — ersetzt durch Prüfung von `saveFertilizerCaptureToInventoryCore` |

**Unverändert erhalten:**

- [`fertilizerInventoryCreationCore.ts`](../../src/lib/fertilizerInventoryCreationCore.ts) — u. a. `FertilizerInventoryCreationReason`, `FERTILIZER_INVENTORY_CREATION_MAX_IDEMPOTENCY_KEY_LENGTH` (produktiv in Intake/Outbound/Capture)
- DB-RPC `save_fertilizer_capture` und `create_fertilizer_inventory_core_from_confirmed_packages` — **technisch unverändert**
- Capture→Intake über `saveFertilizerCaptureToInventoryCore` → `recordFertilizerProductStockIntake`
- Package-Eingaben (`packageCount`, `selectedPackageQuantity`, …) — **nur Summenbildung**; keine N-Items-pro-Packung

**Verifikation (veröffentlicht):** Capture→Intake 6/6; Capture-Save-Core 6/6; Integration 15/15; Build grün; `git diff --check` grün.

#### 30.17.3 Schritt D — Ergebnis (`fe0f1af`)

**Kanonischer Routingvertrag (produktiv):**

| Route | Verhalten |
|-------|-----------|
| `/duengung` | [`FertilizerApplicationPage`](../../src/pages/FertilizerApplicationPage.tsx) — Produktauswahl |
| `/duengung/:inventoryItemId` | dieselbe Page — itemgebundener Application-Flow |

**Legacy-Kompatibilität:**

| Legacy-URL | Verhalten |
|------------|-----------|
| `/ausruestung/duenger/:inventoryItemId/anwenden` | `LegacyFertilizerApplicationRedirect` → `/duengung/:inventoryItemId` (gültige UUID) oder `/duengung` (ungültig/fehlend); `<Navigate replace />`; **keine** Application-Page, **keine** Datenzugriffe, **keine** Persistenz; `ProtectedRoute` erhalten |

**Vollständig entfernt:**

- `entryPoint="legacy"`, `FertilizerApplicationEntryPoint`, Legacy-Props/Branches
- Legacy-Redirect-Effect in `FertilizerApplicationPage`
- `shouldRedirectLegacyApplicationRoute`
- `fertilizerLegacyApplicationPath`, `fertilizerApplicationPath`
- `isLegacyFertilizerApplicationPath`, `parseLegacyFertilizerApplicationInventoryItemId`, `LEGACY_APPLICATION_PATH_PATTERN`

**Erhaltene Route-Helfer:**

- `fertilizerHomeApplicationPath`, `FERTILIZER_ROUTES.homeApplication`
- `buildFertilizerApplicationRoute` (in [`fertilizerApplicationFlowCore.ts`](../../src/lib/fertilizerApplicationFlowCore.ts))
- `resolveLegacyApplicationRedirectPath` (neu — reine Redirect-Auflösung, keine Persistenz)

**Verifikation (veröffentlicht):** Routes 7/7; Application Flow Core 39/39; Multi-Area 49/49; Integration 15/15; weitere Application-Tests 26/26; Build grün; `git diff --check` grün.

#### 30.17.4 Verbleibende blockierte Kandidaten

| Kandidat | Blocker | Kategorie |
|----------|---------|-----------|
| `create_fertilizer_inventory_core_from_confirmed_packages` | **OD-P7-1** — externe Nutzung nicht belegbar ausgeschlossen | `deprecate_then_remove` |
| `save_fertilizer_capture` | **OD-P7-1** — gleiche externe Nutzungsunsicherheit | `deprecate_then_remove` |
| Creation-TS-Runtime (`fertilizerInventoryCreation.ts`, RpcCore, Persist-Methode, Tests) | RPC noch deployt; B2/B3 nicht freigegeben | `remove_now` post-Drop |
| `fertilizerInventoryCreationCore.ts` (Teile) | Produktiv für Reasons/Idempotency-Limit | `retain_productive` (Teil) |
| Creation-DB-Harness | Phase 2–6 DB-Regressionen | `retain_historical` |
| `_inventory_creation_format_quantity`, `_inventory_creation_compute_fingerprint` | **OD-P7-3** — Application-RPC-Abhängigkeit | `retain_historical` |
| Movement-Enum-Rename | **OD-P7-4** — außerhalb Kernscope | separat |
| GA-012/Legacy-Doku-Drift | **OD-P7-5** — redaktionell, nicht blockierend | Redaktion |

**Wichtig:** Repository-Abwesenheit interner Aufrufe beweist **nicht** Abwesenheit externer RPC-Clients.

#### 30.17.5 Kein weiterer Implementierungsschritt freigegeben

- Schritt **B1** und **D** sind abgeschlossen und veröffentlicht.
- Es gibt **aktuell keinen** weiteren automatisch freigegebenen Phase-7-Implementierungsscope.
- Ein weiterer Cleanup erfordert **neue Evidenz** oder eine **ausdrückliche Entscheidung** (insbesondere OD-P7-1, OD-P7-3, OD-P7-4, OD-P7-5).

**Mögliche nächste Entscheidungsarbeit (keine Implementierungsfreigabe):**

| ID | Thema |
|----|-------|
| **OD-P7-1** | Externe Nutzung von Creation-RPC und `save_fertilizer_capture` klären |
| **OD-P7-3** | Langfristige Entkopplung produktiver Application-Helfer von `_inventory_creation_*` |
| **OD-P7-5** | Redaktionelle Bereinigung veralteter Fehlercode- und API-Dokumentation (GA-012 u. a.) |

#### 30.17.6 Phase-7-Abschlussstatus

**Der aktuell freigegebene Phase-7-Cleanup-Umfang ist abgeschlossen.**

Phase 7 ist **nicht** vollständig technisch bereinigt: verbleibende RPCs, Creation-Stack, Harness und shared `_inventory_creation_*`-Helfer sind **bewusst blockiert oder historisch erforderlich**. Phase-6-Produktverträge, historische Persistenz und Datenintegrität sind **unverändert**. Keine Datenmigration, kein RPC-Drop, kein Enum-Rename, keine Home-/Conversation-/Voice-Änderung.

---

## Verwandte Dokumente

- [DL-033 — Produktbasierter Düngerbestand](../decisions/dl-033.md)
- [DL-031 — Mehrflächen-Düngeranwendung](../decisions/dl-031.md)
- [GA-012 — Berechneter Bestand](./ga-012.md) (Mehrgebinde/FIFO: **nicht** v1-Richtung)
- [GM-008 — Vier Ebenen](../model/gm-008.md)
- [home-experience](../playbook/home-experience.md)
